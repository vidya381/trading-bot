# Decision Log — moved

This log grew past 4000 lines, so it was split into one file per major
build-order step. Nothing was reworded; it was a pure file split.

See **[docs/decision-log/](decision-log/README.md)** for the index, then open the
file for the step you need (for example, `decision-log/07.md` for Step 7).
