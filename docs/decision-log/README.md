# Decision Log

A running record of decisions made while building this system, one entry per
build-order step (see section 19 of the technical specification). Append only.

---

This log was split from a single `docs/decision-log.md` file into one file per
major build-order step, so a session can read only the range it needs instead of
the whole history. No entry was reworded — this was a pure file split. Sub-steps
live in the same file as their parent step.

Order is preserved as written (append-only): within Step 10, entry **10.7**
appears before **10.6**, matching the order the work happened.

**Adding new entries:** append to the `NN.md` matching the step number (sub-steps
go in their parent's file); start a new `NN.md` for a new major step and add it to
the index below. Do not write to the old `docs/decision-log.md` stub.

## Index

| File | Steps | Title(s) |
| --- | --- | --- |
| [01.md](01.md) | 1 | Repository setup |
| [02.md](02.md) | 2 | Shared infrastructure modules |
| [03.md](03.md) | 3, 3.1, 3.2, 3.3, 3.4 | Exchange integration layer; `cancelOrder` returns the cancelled order; real credentials provider and testnet exchange wiring; temporary live reachability check from the deployed Worker; Gemini exchange implementation (second `RestExchangeClient`) |
| [04.md](04.md) | 4, 4.1 | D1 schema and migrations; D1 databases provisioned |
| [05.md](05.md) | 5 | Capital ledger and bot-creation validation |
| [06.md](06.md) | 6 | DCA BotInstance Durable Object |
| [07.md](07.md) | 7 | Reconciliation Cron Worker |
| [08.md](08.md) | 8 | RateLimiter Durable Object |
| [09.md](09.md) | 9 | Grid strategy |
| [10-backend.md](10-backend.md) | 10, 10.1–10.5, 10.7 | Alerts and outbound notification; no-schema guards; Discord timestamp investigation; human liquidation and the global kill switch; dashboard backend API; dashboard deploy guard |
| [10-frontend.md](10-frontend.md) | 10.6, 10.8, 10.9, 10.10, 10.11, 10.12, 10.13, 10.14 | Dashboard frontend scaffolding and bot list; bot detail view; liquidate action; global kill switch control; create-bot form; cross-bot alert feed; manual-adjustment form; **10.14** — derived position figures and read-only configuration on the bot detail view: the DCA take-profit PRICE (average entry x (1 + tp%)), unrealized mark-to-market profit for both strategies, and a read-only Configuration block per strategy. UI-only and purely additive. `src/shared/money.ts` becomes the **second file the dashboard imports across the seam** after `alert-types.ts`, earning it on the same terms (dependency-free by contract, verified by building) and for the same reason — a re-derived decimal parser would not throw, it would quietly disagree by a rounding step on the intervention screen. Importing `dca.ts` itself to call the bot's own `takeProfitPrice` was **tried and rejected** for a concrete reason: the dashboard's `noUnusedLocals` is stricter than the Worker's and fails on a dead `mulInt` import in `dca.ts`, so `applyPercent` is a documented one-line mirror instead — pinned by an 18-test parity suite that runs BOTH implementations over the same awkward inputs. Every convention taken from backend source rather than taste: **ceil** (the gain must be at least what was configured), **one** rounding step not two, and `mul(price, qty, "half-even")` — the same call and mode the books use for cost. The figure is **gross** and labelled so, because `cost` is the bare notional and `feeAmount` never enters the position. The percentage is of **cost basis**, not price-vs-average-entry: exact against money actually spent, and the only form grid can use since a ladder stores no average entry. Mutation testing caught **two** faults in sequence — the value test's product was exact so every mode agreed on it (fixed with two half-way tie cases that break in opposite directions), and then the harness itself, where `perl -0p` without `/g` had been mutating a docblock instead of the code line; the re-run verifies the mutation landed before trusting the result. 9 mutants all caught; 1416 → **1434 pass**. **Two corrections landed after the fact, both from checking live rather than assuming:** the session's verification plan claimed both bots were halted and unrealized PnL would read static — false, they had been resumed ~12.6h earlier (D1: `status = running`, `bot.resumed` 08-01 01:39 UTC), because the session **read step 18's append-only entry as current state**; the general rule now recorded is that anything which can change after an entry is written must be read from the system, never quoted from this log. And a **new open question 4**: `halt_manual`/`halt_order_rejected` alerts have **no lifecycle at all** — `#halt` writes them with an unconditional `#alert`, not step 18's `raiseStandingAlert`, and `resume` never resolves them, so a running, recovered bot still shows stale unresolved criticals in the alert feed and detail view. Same shape as step 16's bug 3, one layer out |
| [11.md](11.md) | 11, 11.1 | Account registry (`accounts` table), live tradable-pair listing (`listTradablePairs` + KV cache), and the Binance-vs-Gemini exchange dispatch deferred from step 3.4; wiring the create-bot form's Account/Exchange/Pair fields to that registry (dropdown, read-only exchange, searchable pair typeahead) |
| [12.md](12.md) | 12, 12.1 | Start-bot action: `POST /api/bots/:id/start` (thin wrapper over `BotInstance.start`) and the dashboard `StartAction` control; scoped to the honest `created -> running` status-flip, since `start` placed no order until the execution path was wired; **12.1** — that caveat **deleted, not softened**, once the price-feed arc landed: with step 13's exchange attachment and 14.1–14.5's feed (verified live at **14.6 Tier 0** and **14.7 Tier 1**, both PASS 2026-07-30), "no order will be placed yet" had become a false reassurance on a capital-committing dialog, so the dialog, the success banner, and the three stale docblocks now say starting drives a real order on the next closed 1-minute candle; the fail-closed feed subscribe means `invalid_status` is no longer `start`'s only failure (generic branch already covers it) |
| [13.md](13.md) | 13 | Execution wiring, part 1 of 2: attaching a live exchange client to `BotInstance` in production via `resolveExchangeForAccount` (lazy in `#exchange`, injected-wins for tests, fail-closed on missing credentials); the price feed / hibernation redesign is deferred to part 2 |
| [14.md](14.md) | 14, 14.1–14.10 | Price feed design (execution wiring, part 2 of 2) — the outbound-WebSocket-does-not-hibernate correction (spec §4.6 deviation), shared `PriceFeed` DO keyed by (exchange, pair), `subscribeToPriceFeed`/`WebSocketHandle` deletion + `getCandles` addition, closed-1m-candle→Price, Gemini-first, and single-alarm reconnect/backoff/backfill with an alert-only blind-feed policy; the Gemini-WS reachability probe (PASS: `candles_1m_updates`, no closed flag, OHLCV as JSON numbers); **14.1 Session A** — the interface reshape + `getCandles`/`parseCandles`/`parseKlines` build; **14.2 Session B** — the pure `GeminiPriceFeedCodec` (`socketUrl`/`subscribeMessage`/`parseMessage`, `FeedEvent` 4-way, reusing `parseCandles`) tested against real captured payloads; the deliberate multi-boundary probe (Q1 **CURRENT-ONLY** 6/6, Q2 **~5s heartbeat**); and **14.3 Session C1** — the `PriceFeed` Durable Object (CURRENT-ONLY stream engine, outbound socket lifecycle, single multiplexed alarm, backoff, **WS-batch gap-backfill deviating from decision 5**, blind-feed + 30-min escalation), 15 tests; **14.4 Session C2** — the durable SQLite subscriber registry + `allSettled` fan-out (`subscribe` carries config, `idFromName` cross-DO calls, first-open/last-close gating, `stopFeed` watermark reset, no-auto-prune on fanout failure), 20 tests total; **14.5 Session D** — wiring `BotInstance` status transitions to the feed (subscribe fail-closed in `start`/`resume`, unsubscribe best-effort in `#halt`/`close`, DCA auto-restart stays subscribed), 7 wiring tests. **The price-feed arc (14.1–14.5) is complete**; a live end-to-end sandbox run remains a deployed, manual step; **14.6** — the temporary `GET /api/debug/ws-check` **Tier 0** edge→Gemini WebSocket reachability diagnostic: confirmed **PASS** live (2026-07-30, `candles_1m_updates` received) and removed the same day, but it caught a real feed-transport bug (`wss://` passed to `fetch`) whose fix — the shared `httpUrlForWebSocket` scheme translation + handshake tests in `price-feed.ts` — stays permanently; **14.7** — the temporary `GET /api/debug/feed-check` **Tier 1** diagnostic verifying the REAL `PriceFeed` DO live (throwaway `debug-check` subscriber, watermark-advance as fan-out evidence via a temporary read-only `debugSnapshot()`, `price_feed_fanout_failed` in D1 as per-subscriber attribution, 120s bounded wait with an `inconclusive` verdict for a quiet market, unsubscribe in a `finally`), 14 tests: confirmed **PASS** live (2026-07-30, watermark 1785463079999→1785463139999 — exactly one 60s candle — with D1's `price_feed_fanout_failed` corroborating the fan-out to `debug-check`) and fully removed the same day, route AND read path, leaving the DO exactly as it was. **The feed itself is now proven live**; only feed→bot→order and the ~15-minute eviction ceiling remain unobserved; **14.8** — that feed→bot→order run **attempted live (2026-07-31)**: the DCA base order reached Gemini and was refused with `MissingAccounts`, the bot halting correctly. Gemini's docs confirm the field is **`account` (singular, a top-level string, the account nickname), required for MASTER keys only** and refused on account-level keys (`AccountsOnGroupOnlyApi`) — so it is added once in `GeminiClient.#request` for every signed endpoint (order/new, order/status, order/cancel, orders, balances — all five had the same gap, balances marking it required), derived from the key's `master-`/`account-` prefix via the pure `resolveAccountField`, configured by `GEMINI_ACCOUNT_NAME`, and failing closed at both the resolver (one `not_attached` halt) and the client (non-retryable, nothing sent). Tests use hand-written-to-spec + `openssl` payload vectors and whole-payload `toEqual` (the old `toMatchObject` assertion stayed green through the live failure), mutation-checked against three near-miss implementations. **Verified against docs and tests, NOT yet against Gemini**; the bot stays halted; **14.9** — two pre-existing `verify-gemini.ts` faults found re-running it, neither related to 14.8: Tier 0's 404 is the **sandbox carrying no ticker data for real pairs** (`'btcusd' does not have available data yet`, plain text; `/v1/pricefeed` lists only EFILFIL/GUSDUSD) and NOT a stale path — `/v1/pubticker/{symbol}` is current and works on production — so Tier 0 now uses symbol-details + `/v2/candles` (sandbox-served, real BTCUSD prices) and reports the ticker 404 as a known venue gap; **consequence: the human-liquidation path reads `getCurrentPrice`, so liquidation cannot be exercised end to end on Gemini testnet** (degrades to `liquidation_no_price` by design). And Tier 1's "not set" was the script's own documented `VAR=... esbuild && node` invocation scoping the variables to `esbuild`, not a reading bug — now documented as two commands with the environment on `node`, with a skip message naming the trap; **14.10** — the first live signed call got past `MissingAccounts` to `InvalidAccountName` ("Expected a JSON **array** with valid accounts, instead got: Primary"), which **looks like** it contradicts 14.8 but does not: a machine-extracted re-check of Gemini's whole reference (305 pages from the sitemap) finds **65 endpoints typing `account` as `string`, zero typing it `array`, zero fields named `accounts`, zero array request examples**. The real cause is that a Gemini account has **two names** — display `name` `"Primary"` vs API nickname `account` `"primary"`, with a documented derivation — and `GEMINI_ACCOUNT_NAME` held the display name; the plural/array wording names the error code and Gemini's internal list validation, not the wire type (cf. `MoreThanOneAccount`). **Payload shape unchanged.** Added a local pre-send nickname check (`[a-z0-9-]+`, refusing with a derived suggestion and deliberately NOT auto-normalising), `listMasterGroupAccounts()` + a `groupLevel` spec flag so `/v1/account/list` correctly sends no `account`, and a Tier 1 pre-step printing `name` beside `account`. Tests assert against **Gemini's own published name→account pairs**; six mutants all caught, including "send an array" and "rename to accounts". Also: the 14.9 sandbox ticker gap **closed on its own** mid-session (404 → live quotes), confirming that diagnosis |
| [19.md](19.md) | 19 | **Observing fills on resting orders, part 1 of 3.** Root cause behind both `bot-44400a` and the step-17/18 three-fills incident: **nothing observes a fill on an order that RESTS.** `onFill` has four callers, all order-placement sites, each only draining `result.fills` from the placement RESPONSE; there is no user-trade socket and no status poll. For DCA that is a **liveness** failure, not a latency one — `decide` returns early on `position.quantity <= ZERO` before it reaches the stop-loss or take-profit branches, so a bot whose base order rests sits in `hold` forever while genuinely holding coin, until reconciliation halts it. **One premise checked and corrected before designing:** "reconciliation only notices hours later" is FALSE — `balance_snapshots` shows unbroken 5-minute passes across the incident (`08:20:47` clean, `08:25:47` finding the drift); the order rested ~4h55m because the price never reached it. So this is not about detection latency; §9 stays a competent backstop that halts, alerts and **never auto-corrects**, and this path is what keeps a RUNNING bot's books current so the backstop has less to find. Built: `#pollOpenOrders` reading each `openOrderIds` entry via `getOrderStatus` at **`routine`** priority (keeping books current must not draw on §5.4's risk-exit reserve), folding unseen executions through the ordinary live path — `applyFill`, `applyEntry`/`planFill`, `#mirrorOrderUpdate`, `#mirrorTrade` reused verbatim, so **no new arithmetic exists** and a poll-recorded position cannot disagree with a pushed one; idempotency is step 18's `fillId` identity check, unchanged. **`placeReplacement` is derived, not hardcoded** — step 18 passes `false` (a repair must not trade from a halted bot), the instinct here is `true` (a resting grid buy that fills SHOULD place its paired sell — the grid working, not a back-door resume), and the actual rule is `fresh.status === "running"`, since a poll on a halted bot must still place nothing; hardcoding `true` passes every running-case test and silently breaks the halted one, which a mutant confirms. **Two exchanges, two capabilities:** Gemini's `getOrderStatus` always sent `include_trades` so real `tid`s are available and a Gemini bot fully repairs, but **Binance's `GET /api/v3/order` carries no fills array at all** — so "more filled than recorded, no id behind it" is real, and is alerted (`unattributable_fill`) and SKIPPED rather than given a synthesised id, for the reason `#recordCancellation` already documents. `#foldTerminalState` closes orders the exchange ended (clearing the ladder slot for a grid, since the ladder owns `openOrderIds`) **gated on the fill quantities agreeing** — an order that ended with more filled than recorded is left OPEN, because terminal states have empty successor lists and `applyFill` throws `fill_after_terminal`, making the understatement **permanent**; that is exactly how `v1-bot-44400a-0`'s 0.00031777 BTC became unattributable when the halt sweep closed it. `bot.open_orders_checked` audited **only when something moved** (at 30s a row per pass would measure uptime, not events). 13 tests, 1440 → **1453**, four mutants caught — and the fold-gate mutant is recorded because **it survived the first test written for it**: that test set up the bot-44400a shape faithfully and asserted the order stayed open, which was true, but reached it through the `unattributable_fill` `continue` and never executed the gate; the replacement drives a venue that DID report per-fill detail whose fill overfills the order. **Deliberately not done:** not scheduled (no alarm, no production caller — step 20), `applyMissedFills` and the halt-sweep interaction untouched at the operator's explicit direction, and **this does not recover bot-44400a's BTC**. Open questions: the 0.00031777 BTC has no in-system home (`manual_adjustments` is account+asset scoped, no `bot_instance_id`/price/fill id, so it can make capital accounting truthful but cannot reconstruct a DCA position — operator's call, not a code change); **alert lifecycle is a precondition for step 20** (`raiseStandingAlert` is module-private to `reconcile.ts`, so until it is shared an `unattributable_fill` would write ~2,880 rows/bot/day, step 18's problem in a new place); and 30s is a decision, not a measurement |
| [18.md](18.md) | 18, 18.1 | **The order-state-drift correction path, and standing alerts.** Two consequences of 17 now that reconciliation can see the exchange. **`applyMissedFills`** — `POST /api/bots/:id/apply-missed-fills` → `BotInstance.applyMissedFills(actor)`, a thin wrapper like start/resume/liquidate, human-initiated because §9 halts on order-state drift and **deliberately never auto-corrects** it. Two existing constraints shaped it more than convenience: (1) **fill ids are never synthesised** — `#recordCancellation`'s docblock already established that `applyFill` dedupes on `fillId` so a made-up id makes the real fill "either double-count or be silently swallowed", so the repair uses Gemini's own `tid`s read via `getOrderStatus` (which always sent `include_trades`), which also makes it **idempotent for free** via the same identity check the live path uses, not a flag; (2) **a grid fill normally PLACES the paired sell**, and `#halt` never clears the ladder (confirmed — `#cancelOpenOrders` only trims `openOrderIds`, and every cancellation failed on 07-31), so running it unmodified would put live orders on the exchange from a HALTED bot — resuming trading by the back door. `placeReplacement: false` suppresses only that; verified clean rather than a fudge because `planFill` clears the filled level itself and returns the replacement as a separate intent, so **no phantom order** is left (asserted, since a phantom would make the next pass raise fresh drift). Everything else reused verbatim — `applyFill`, `applyEntry`/`planFill`, `#mirrorOrderUpdate`, `#mirrorTrade` — so **no new arithmetic exists** and a repaired position cannot disagree with a normally-recorded one. Refuses on a running bot, never changes status, reports unreadable orders rather than assuming, and distinguishes "no `trades` reported" from "no executions". **Standing alerts** — the same 3 findings had written **186 identical unresolved criticals in 4 hours** (~864/day). Only half unintended: §10's KV cooldown throttles the outbound *ping* correctly and `notifications/cooldown.ts` is explicit that recording and notifying are separate concerns; the unthrottled part was the D1 row. Unconditional insert is right for discrete events, wrong for a condition re-detected on a schedule and never auto-corrected, because the row count then measures duration rather than what is wrong and "how many unresolved criticals" stops meaning anything. `raiseStandingAlert` writes one row per open incident keyed `(alert_type, bot_instance_id)` — the cooldown's own key, message deliberately excluded so a drifting run id is still the same incident. **Nothing historical is lost**: every run's full findings list is already in `audit_log.details_json`, so `alerts` becomes what `resolved` always implied. `resolveClearedAlerts` closes them, **gated on the pass having actually read something** — a skipped-read run found nothing because it saw nothing, and resolving on that would clear a live incident on the strength of an outage (§5.6 applied to the alert lifecycle). 15 tests, 4 mutants caught. **Bots deliberately NOT resumed and the correction NOT yet run** — invoking it is the operator's action; note the grid's paired sell will not be placed by the repair nor on resume (`#placeInitialLadder` runs once), so `bot-328qgw` returns one rung short, truthfully represented. *(Since verified live, 2026-08-01: the operator did both — missed fills applied 01:29 UTC, both bots resumed 01:39 UTC, drift criticals now resolved; both read `status = running` in D1. **This row records a 2026-07-31 decision, not current state** — see 10.14 open question 1 for how treating it as current state went wrong.)* **18.1** — the dashboard control for that endpoint, built once the raw API had been used in anger (closing 18's open question 1): it renders **only while the bot has an UNRESOLVED order-state-drift alert** (five types, built tier×kind; the `severe_` pair unreachable under `TIER_CEILING` but listed so a future change cannot silently hide the button), reusing the alerts already on `GET /api/bots/:id`. `cancel_fill_discrepancy`/`cancel_failed` deliberately **excluded** — the repair reads `openOrderIds` only, so it provably cannot reach a cancelled order (18's own open question 2), and a button that returns an empty `applied` is worse than no button. Refuses a non-halted bot in **two layers** (the button is replaced by copy, not shown-and-disabled; the 409 branch remains for the load-to-click race). The grid dialog states three separate facts — the paired sell is not placed, the rung stays empty and **resume will not re-place it** (`#placeInitialLadder` runs once), and this is deliberate because placing a live order from a correction path on a halted bot resumes trading by the back door — plus the honest upside that no **phantom** order is left. Outcomes are the real `MissedFillsResult` in four shapes, `applied` as a table down to the exchange's own fill ids, `skipped` **verbatim** (the backend's "detail not reported" ≠ "nothing filled" distinction lives in its prose and is not re-derived by string-matching), and a dropped request as "outcome unknown" **plus** the useful fact that retrying is safe because the repair dedupes on fill id. The cross-bot feed's open drift rows link straight to `#apply-missed-fills` and scroll to it — they never open the dialog, since a confirmation a link can open is not a confirmation. **Six endpoint tests** added (the DO had 15, the endpoint had none), each asserting a claim the UI makes to an operator; 1403 → **1409**. **Addendum (same day):** 18.1's open question 2 — the `reconciliation_{tier}_{kind}` string built independently on both sides — **closed rather than carried**, because unlike every other hand-mirrored contract here (a renamed API field surfaces as a runtime `undefined`) this one fails **silently**: the repair button just stops appearing on a bot that has drift, indistinguishable on screen from having none. It was worse than recorded — **four** copies, not two (both raise sites, `resolveClearedAlerts`'s two `startsWith` checks, and the dashboard). `src/shared/alert-types.ts` is now the only spelling of the format, and **the one file the dashboard imports from the Worker's source tree** — dependency-free by contract so both toolchains compile it despite the deliberate TS 5.7/7 split; `vite dev` serving it over `/@fs/` was checked with a real request, not assumed. Since it cannot import `findings.ts`, the tie runs the other way as two type assertions that do real work (`ORDER_STATE_DRIFT_FINDING_KINDS`, `ALERTING_DRIFT_TIERS`), so renaming a `FindingKind` fails the typecheck instead of emptying a set. **Seven tests with derived lists, never hand-maintained** (a second copied list would reproduce the bug): six in `shared/alert-types.test.ts` importing the dashboard's own predicate, sweeping every kind from `TIER_FLOOR`'s keys in both directions, plus one in `reconcile.test.ts` driving real detection → real `raiseStandingAlert` → real `alertView` → the dashboard gate. Four mutants caught, including a typecheck failure before any test runs; 1409 → **1416** |
| [17.md](17.md) | 17 | **Two Gemini parsing bugs, uncovered by deploying step 16's dispatch fix** — both were UNREACHABLE while reconciliation talked to Binance, so the fix did not create them, it was the first code ever to exercise these parsers against a real Gemini response. Deploying 16 halted both bots within one pass; that was progress, not regression. **Bug 1** — `asRecord` refused arrays with `expected an object, got ${typeof value}`, and `typeof []` is `"object"`, producing the useless **`expected an object, got object`**. The real shape could not be looked up (no raw body retained, docs JS-rendered, endpoint signed) so the honest `describeShape` message was deployed FIRST as its own step purely to learn it, and the next scheduled run reported it in its own alert text: **a one-element array** wrapping the documented order-status object, keys `[avg_execution_price, client_order_id, exchange, executed_amount, id, is_cancelled, is_hidden, is_live, options, order_id, original_amount, price, remaining_amount, side, symbol, timestamp, timestampms, trades, type, was_forced]`. `unwrapSingleOrder` accepts BOTH shapes (same 14.10 lesson: establish the wire shape from a real response, not the prose) and deliberately does NOT do `body[0]` — empty and multi-element arrays are refused with specific messages, since silently reporting "no such order" is exactly what §9 exists to catch. **Bug 2** — Gemini returned USD `"99829.54180779832"` (11 dp) and `fromDecimalString`'s "rounding must be explicit" made the whole account unreadable. Strictness is right for values we SEND, wrong for values an exchange REPORTS; added `fromDecimalStringRounded` reusing the module's existing `Rounding` union (not a parallel vocabulary), `"exact"` still throws, balances use **`"floor"`** because rounding up would overstate funds and let the ledger believe in money that is not there. Own test caught an ordering bug: `"exact"` wrongly threw on `"1.5000000000000"`, which loses nothing. All tests use the REAL captured values; 3 mutants caught. **Audit answer: these are NOT provably the only two** — every `requireMoney` in `parse.ts` reads a reported value, with `avg_execution_price` (a computed average) the most likely to fire next; deliberately left unchanged for want of a real payload to test against. **Live result at 20:40Z: `skipped: 0` on both accounts**, `gemini-main` showing 6 real Gemini assets vs `main`'s 445 Binance ones — and reconciliation immediately found **3 of the 5 resting orders are FILLED on Gemini** while the bots still believed them `pending`, retiring the "quiet market" theory the whole investigation opened with. Also: `main` (Binance) had **never been reconciled at all** before step 16 (no bot, no ledger row); it is now, cleanly — at ~128k snapshot rows/day, giving `balance_snapshots` retention a real number |
| [16.md](16.md) | 16 | **Two production bugs found live, plus one UI-correctness reversal.** Found while answering a smaller question (is the hanging Gemini sandbox UI our fault?) — `verify-gemini.ts` Tier 0 PASS proved Gemini healthy, and the D1 comparison that followed found `balance_snapshots` holding **445 assets under `gemini-main`** including `1MBABYDOGE`/`1000CHEEMS`/`0G`, none of which exist in Gemini's 392-symbol sandbox roster (confirmed by unsigned public call). **Bug 1** — `reconciliation.ts` called `resolveDefaultExchange` (Binance) once above the account loop and used it for every account regardless of registered exchange, while `BotInstance` had dispatched correctly since step 11: a Gemini account's orders went to Gemini and its balances were read from Binance. It survived because it initially *worked*, and because `internal_calculated_balance` is the **previous run's** exchange figure — so two reads of the same wrong venue always differ by zero and all 176,354 rows read `discrepancy: 0`. **A differential check cannot detect a constant error in its own input.** Fixed by per-account dispatch through `resolveExchangeForAccount`, registry-then-bot-row derived, refusing (not defaulting) an account with no known venue. The old tests asserted only that *an* exchange client was called; the seam now carries the registered exchange (`TestExchangeFactory`, kept separate from the resolvers' label-only `ExchangeFactory`) and 4 tests assert routing, one on the real dispatch path with no network. **Bug 2** — from 07:25:21Z Binance geo-blocked the Worker's egress and every read failed; `reconcile.ts` correctly refused to invent balances (§5.6) but `skipped` went ONLY to `audit_log.details_json` and a `console.log`, so **143 consecutive passes read nothing and alerted nothing**. Note the first diagnosis was **wrong** — the cron never stopped, only its writes did; `audit_log` corrected what `balance_snapshots` implied. Fixed with `auditBlindness`: one standing `critical` alert per incident past `BLIND_AFTER_MS` (16 min), auto-resolved on recovery; explicitly does NOT cover "the cron never fired" (needs an external prober). **Bug 3** — `resume` never cleared `halt_reason`/`halted_at`, which **step 15 recorded as deliberate**; reversed, since the CHECK permits but does not require it and a `running` bot was advertising an already-fixed `MissingAccounts` failure. History moves to the `bot.resumed` audit entry. 14 new tests, all mutation-verified. **Deployment hazard found before deploying and NOT yet resolved:** the stale Binance baselines would make the first corrected pass see a large unexplained `USD`/`BTC` drift, and `balance_drift` is the one kind whose magnitude reaches `severe` → circuit breaker → all bots halted. Remedy is to invalidate the 176,354 wrong-venue rows so the first run takes the "adopt, do not accuse" path — destructive, so **nothing deployed and no live data touched** pending the operator's decision |
| [15.md](15.md) | 15 | Resume-bot action: `POST /api/bots/:id/resume` (thin wrapper over `BotInstance.resume`, confirmed absent from `ROUTES` first) and the dashboard `ResumeAction` control — section 7.2 step 5's human-only path out of `halted`, designed into the DO at step 6 but until now callable only from tests. **Resume's failure surface is WIDER than `start`'s**, confirmed from source rather than assumed by analogy: it asserts BOTH risk latches (`assertGlobalArmed`, `assertAccountArmed`) where `start` asserts neither, so `globally_tripped`/`account_tripped` (409) join `invalid_status` (409) and `not_attached` (503), each with its own copy naming the right reset; all are raised **before** the status flip, so every failure truthfully says the bot is still halted. **No branch was built for "feed/exchange unreachable" because it cannot fire** — `PriceFeed.#ensureConnected` catches a failed connect, schedules a backoff reconnect and returns, so that case is a 200 with the bot running blind and `price_feed_blind` as the signal; the success banner says so rather than a dead error path implying a successful resume proves the feed is up. Point 8 (re-subscription) verified end to end: halt unsubscribed → last subscriber → `stopFeed` closed the socket and reset the watermark → `resume`'s fail-closed subscribe hits the first-subscriber branch → `startFeed` reconnects and re-primes. The dialog shows the **real halt reason** (read while the bot is still halted; step 16 REVERSED the "never cleared by `resume`" decision recorded here -- halt_reason/halted_at are now cleared on resume, with the history moved to the `bot.resumed` audit entry) and deliberately does **not** reuse `StartAction`'s "what it will place": a resumed bot is not flat by definition, `decide()` branches on the current position, and a `stop_loss` halt with an unrecovered price re-halts on the first candle. Four tests, mutation-checked with four mutants all caught (including latches-after-the-flip, which a code-only assertion would miss) |
| [20.md](20.md) | 20 | **Arming the poll, part 2 of 3: the shared standing-alert mechanism, and the alarm.** Step 19 built `#pollOpenOrders` and deliberately did not schedule it, recording one **precondition** for doing so: `raiseStandingAlert` was module-private to `reconcile.ts`, so a 30-second poll raising `unattributable_fill` would write ~2,880 rows/bot/day — step 18's measured 186-identical-criticals problem at ~60x the 5-minute cron's rate. Done first, and as a **move rather than a second implementation**, because a parallel copy passes its own tests right up until the two lifecycles disagree about what "the same alert" is — 18.1's addendum's failure mode, applied to the alert LIFECYCLE instead of the alert-type FORMAT. **`/src/alerts/standing.ts` is now the one implementation, and BOTH halves moved**: step 18 established that dedup without resolution is *strictly worse* than an unconditional insert (one row that never clears, suppressing that alert for that bot forever), so handing the new caller only the raise half would have handed it that bug. `reconcile.ts` keeps thin adapters supplying what is genuinely its own (every finding `critical`/`trading`, `reconciliation_{tier}_{kind}` rows, the account's bots); behaviour byte-for-byte unchanged. **Not `/src/shared`** — that folder's contract is no I/O, no clock, no storage, and `alert-types.ts` earns its place by being dependency-free for the dashboard's separate toolchain; this writes D1 rows and crosses a boundary (cron Worker ↔ Durable Object) that is inside one bundle. **The one real judgement call: `source` scopes BOTH halves** — not a second key but **ownership**, since a writer may only close an incident it could observe, and a writer that cannot resolve a row must not dedup against it either or it suppresses its own alert behind a row it can never clear. The poll owns exactly three types (`unattributable_fill`, `poll_blind`, `poll_blind_escalated`); every other bot alert stays an unconditional insert because it marks a **discrete event**, and three of them are reconciliation's to ingest and close — a poll resolving `order_state_drift` would also make 18.1's "Apply missed fills" control vanish from a bot that still needs it, the same silent-gap-on-a-correction-surface 18.1 refused to defer. **The alarm: one, multiplexed from day one** (`PriceFeed`'s own lesson — two concerns each calling `setAlarm` means the second silently cancels the first), with `#nextAlarmAt` folding every concern into the single instant and `alarm()` checking whether the poll is actually due rather than assuming it is why the object woke. **Arming hangs off `#putState`**, the one choke point every `openOrderIds` mutation already passes through, because arming at each lifecycle method works until someone adds a seventh and forgets — and that failure is silent: a resting order with no timer, the exact condition step 19 exists to end. The firing is **consumed before the pass runs**, since everything the pass writes re-arms, and re-arming from the instant that just fired sets an alarm in the past that fires forever (a mutant that skips this fails 4 tests). `halted` is **still polled** (observing costs nothing; a halt whose cancel failed leaves live orders while a human decides about those books) and never places — step 19's `placeReplacement = status === "running"` is what defends that, not a second check here; `stopped` excluded explicitly; `alarm()` **never throws**, because a throwing handler is retried by the runtime and would double up on a poll already failing. Backoff 30→60→120→240→300s, blind after 5 consecutive unreadable passes (one `warning`), escalated past 30 minutes (one `critical`), slow retry forever — `PriceFeed`'s policy, but routed through the **standing** path as belt and braces: the episode flags already prevent a re-raise, yet they are this object's own bookkeeping, and the shared dedup makes a duplicate impossible independently of them while giving both alerts a real resolution. A successful pass clears the blind state in `#observeOpenOrders` (so a human's `checkOpenOrders` also ends the episode, keeping flags and rows in lockstep), and **disarming deliberately does NOT resolve `poll_blind`** — "there is nothing to read any more" is not evidence that reading works. The lifecycle is gated on reads/failed-reads counted separately, never on `skipped`, which also holds fills read perfectly well and then refused. **A test-infrastructure fault this step exposed:** the pool really does fire DO alarms, and every fake clock in the suite sat in the past, so each armed alarm was overdue the instant it was written and raced its own test — surfacing as two contradictory failures in one run (an alarm reading `null`, and an alarm holding a real-world 2026 timestamp). Clock origins moved forward in the eight files that drive a real `BotInstance`; the suite passing beforehand was luck, not isolation. **41 tests (1453 → 1494), 9 mutants all caught** — and the cross-caller test is recorded because **its first version proved nothing**: it gave each writer its own bot, so every cross-writer assertion passed with the ownership checks deleted. Rewritten so both writers hold incidents against the **same** `bot_instance_id` (which is what production looks like — reconciliation reconciles the account its bots are polling), one real resting order read by the real poll as an unattributable fill and by the real cron as meaningful drift; and the resolve side plants a *stale* reconciliation-owned row on that same bot, so one pass closes one and leaves the other rather than "run a pass, check nothing happened". A source-level guard in `no-raw-d1.test.ts`'s shape pins that the raise/resolve pair is declared in exactly one file and that no re-detected condition is raised through `#alert`. **Deliberately not done:** no pipeline integration — poll results are not wired into `onPriceUpdate` or the decision path, and that interleaving is step 21's whole subject, now genuinely reachable since the alarm and the feed can both enter this object; no dashboard surface; not deployed and not run against the live bots. Open questions: 30s and the 5-failure threshold remain decisions rather than measurements (a full grid ladder polled every 30s is the case that changes the arithmetic); `poll_blind` can outlive its condition by design; and the two writers are kept apart by three independent checks where only two have a mutant isolating them |
| [21.md](21.md) | 21 | **Making the poll safe to interleave, part 3 of 3.** Step 20 armed a 30s alarm and recorded that the alarm and the price feed can now both reach this object at once; this makes that safe, and deliberately does not change what an observed fill DOES (step 22). **Section 0 first, because it decided how much of the rest was real.** Since step 8 this project has carried one sentence about the runtime — `rate-limiter.test.ts`'s "a Durable Object serialises nothing across an await" — which is the only thing here that LOOKS like evidence and is not: that test drives its competitor as a direct in-process call from inside `runInDurableObject`, never through the stub, so it proves the lost-update arithmetic and nothing about delivery. Measured before anything was built (`concurrency-model.test.ts`): an **RPC is delivered and runs to completion** while the object is suspended on an exchange call; an **alarm is delivered on the same terms** (two `getOrderStatus` calls outstanding at once, on a bot with one open order, so the second can only be the alarm's); and a **storage-only read-modify-write survived a two-pass race on one `fillId`**. So: defend everything spanning an exchange call or a D1 write, treat storage-only sequences as probably atomic but do not build on it — **`beginAttempt` stays untouched, measured sufficient rather than assumed**. **A methodological trap, avoided then walked into anyway:** suspending on a hand-made promise would measure microtask ordering, not gate semantics, and would "prove" the object unserialised whatever is true — so the suspensions are genuine (every exchange call is a real cross-object RPC to `RateLimiter`) and the probes observe ARRIVALS, not orderings. Probe 3's first version then used a FULL fill and **passed with both identity checks deleted**, because a full fill is guarded twice more (`fill_after_terminal`, then `overfill`) — four guards, and the probe could not see past the outer two; at HALF the order neither fires and the `fillId` check is genuinely alone. The probe also **segfaulted `workerd`** with the full suite while passing alone: a real alarm firing a pass that outlived the test's D1 binding, and `settle()` (a fixed 10ms) used as a barrier — a fixed delay is never a barrier, only a condition wait is. **Built:** `#mutateState` on every `#putState`, re-reading AFTER the await and taking a delta, because `{ ...snapshotTakenEarlier, oneField }` is a whole-state overwrite that reverts a concurrent pass's position, ladder, `openOrderIds` or `status`. Two sites it fixes needed no alarm to bite and are in the DEPLOYED build: **`#placeTakeProfitSell` assigned `openOrderIds: [exitId]`**, discarding an order `#cancelOpenOrders` had just failed to cancel — still live on the exchange, never polled or cancelled again, on the one path where the bot is getting flat, and **one failed cancel was enough**; and **`#cancelOpenOrders` assigned the list it started with** after N throttled cancels, erasing a grid replacement a poll added mid-sweep. `#allocateSequence` follows from the same rule — the sequence IS the `clientOrderId`, and `beginAttempt` catching a collision means the second order is SKIPPED, so a rung silently vanishes until the next tick. **The point of no return:** `#mutateState` fixes writes that revert each other but cannot fix a DECISION taken against state that has since changed, so every send site re-reads status immediately before sending — which is what finally makes step 19's `placeReplacement: fresh.status === "running"` true when it matters, since that gate reads the status and then crosses two D1 writes and a filter refresh before `#placeGridOrder` sends. The gate was right; it was reading too early. Cancellation is deliberately NOT gated — refusing to cancel because the bot changed state is how orders get left live. **The poll yields, one way**, on a property step 19 established rather than a general principle: everything the poll does is RE-DERIVABLE, so it is the one writer that can always come back in 30s. `#outsidePoll` counts non-poll passes in memory; the poll stands aside before its loop and **again after each `getOrderStatus` returns**, the check that matters because that read is the measured arrival window. One-way deliberately: a symmetric lock would reintroduce at the scheduling layer exactly the head-of-line blocking §5.4's reserved slice exists to prevent, which is why `blockConcurrencyWhile` is not used. **`deferred` is a third outcome** — neither `unreadable` (the venue answered) nor `skipped` (nothing was read and refused) — because counted as a failure it reports `poll_blind` on a reachable venue, and counted as a success it resolves alerts it never looked for. **Two mutants survived the first version and both were right to:** the post-read yield did `reads -= 1`, forcing `reads` to zero, which is what BOTH downstream guards already treat as "nothing to read" — a deferred pass was indistinguishable from an empty one, so neither guard could be observed working. Removing it exposed that **`#runScheduledPoll`'s explicit `deferred` branch was dead and worse than dead**: a pass that failed to read A then deferred on B has real evidence of an unreachable venue, and returning early threw it away, suppressing backoff and `poll_blind` for exactly the bot that needed them. The post-read deferral also cannot be tested in-process at all (a competitor driven to completion inside a seam means the poll defers BEFORE its loop, `reads === 0`); it needs a still-running competitor and the stub harness, and that test is the only one exercising `!pass.deferred`. **10 tests (1494 → 1504), 6 mutants all caught.** **Deployment fact, checked against Cloudflare rather than the log:** last testnet deploy `2026-08-03T19:53:17Z` of commit `1d4d39e`, **16 minutes before step 19 was committed** and 63 before step 20; production last deployed `2026-07-24`. So the poll-dependent races were dormant, not live — the two exceptions being pre-existing and reachable today without any poll. **Deliberately not done:** a fill still does not drive a decision, nothing verifies a tick is actually coming, `bot.open_orders_checked` still writes nothing on a refusal, and `checkOpenOrders` is still unreachable from the API — all step 22. Open questions: poll-vs-poll is unguarded by design; `#passesInFlight` is per-instance and nothing asserts that; probe 3's result is weak evidence and `beginAttempt` is the one thing resting on it |
| [22.md](22.md) | 22 | **The observability half: verifying the tick, recording the refusal, reaching the pass.** Steps 19–21 built the fill-observation path and made it concurrency-safe; this closes the three things 21 assigned to 22. **The deployment fact is at the top this time because it inverted:** step 21 recorded 19/20 as "merged and deployed nowhere" with the races "dormant, not live" — checked against Cloudflare, testnet deployed `2026-08-04T03:44:56Z` and step 21's commit `6e575e7` landed **22 seconds earlier**, so the whole 19–21 arc is live on testnet, the 30s alarm is firing against real bots, and "wait for the next tick" was an unverified assumption in running code. Production untouched since `2026-07-24`. **1. `lastPriceAt` read for the first time.** Step 21 decided a poll-observed fill waits for the next tick (the poll has no price, and `liquidatePosition` already refuses `lastPrice` as a fallback) and recorded the gap plainly: "`lastPriceAt` is written and still read by nothing". `#checkPriceFreshness` runs FIRST in `#pollOpenOrders` — before the empty-list return and before the yield — since it is derived from this object's own state and clock and needs no exchange call, so even a deferred pass observes the tick clock. **Not a second copy of `price_feed_blind`, in four structural ways:** the feed's staleness clock advances on ANY inbound frame and counts **heartbeats**, so a socket heartbeating every ~5s while delivering no candles is *permanently fresh* to it (step 14's deployed check has a named verdict for exactly that: `connectionOpened: true, candlesUpdateReceived: false`); a stale subscriber's `"ignored"` is a SUCCESSFUL RPC, so fan-out failure is invisible from there; a bot missing from the registry gets nothing while the feed is well; and `price_feed_blind` carries `bot_instance_id: null` by design (the column is a foreign key), so it can never appear on a bot's page. Every existing check is per-socket and account-level; this is the only end-to-end, per-bot one. **The threshold is 600_000 ms, derived from step 14's live probe rather than guessed:** heartbeats ~5.0s (min 4.8/max 5.2 over 71) carrying no price; candle frames only on activity, **35–70s apart**; Q1 unanimous 6/6 CURRENT-ONLY, so a candle is forwarded only when a frame with a newer `openTime` arrives. Worst-case gap between forwards is the same arithmetic that sized the deployed 120s live-check window — ~60s to the boundary plus up to ~70s of quiet ≈ **130s**. Ten minutes is 4.6× that, the same headroom discipline `STALENESS_MS` uses (20s = ~4 missed heartbeats), and sits deliberately **above the feed's own recovery envelope** (20s detection + five reconnects over ~31s), so it fires only on what the feed's machinery did not fix. Generous because the costs are asymmetric: a late alert costs minutes against a live backstop, a false one costs step 18's measured 186-identical-criticals fatigue. `warning`/`system`, matching `poll_blind`. **Running-only**, because `#onPriceUpdatePass` returns `ignored` before writing `lastPriceAt` for any other status — a halted bot's clock is frozen BY DESIGN and checking one would alert on every halted bot forever. The resolution half falls out free, but **the test for it failed first and the failure was the useful part**: a clean halt cancels everything, empties `openOrderIds` and disarms the poll, and the resolve half is gated on `reads > 0` — so the row can outlive the condition exactly as 20.2 records for `poll_blind`, and the source comment that overclaimed was corrected with it. Null `lastPriceAt` raises nothing (no `startedAt` to measure an age against, and unreachable from the scheduled path since every placement sits inside `#onPriceUpdatePass`). **2. The audit gate.** `applied \|\| closed` meant a pass hitting `#foldTerminalState`'s quantity gate — the exact `bot-44400a` condition — wrote **nothing durable at all**, as did an `OrderStateError` and an unattributable fill: the passes whose reasoning an operator most needs, and the poll was recording only the ones where everything went fine. Now `applied \|\| closed \|\| refused`, and **`refused` is a list rather than a subtraction**: it was derivable as `skipped.length - unreadable`, true today only because every unreadable read happens to push exactly one line, which nothing states or enforces. A `refuse()` helper writes both lists and `#foldTerminalState` takes it instead of the raw array. **Unreadable passes stay excluded**, which is the whole reason the split must exist rather than gating on `skipped`: that would write ~2,880 rows/bot/day during an outage, and that condition already has the backoff and one standing `poll_blind`. **3. `POST /api/bots/:id/check-open-orders` + `CheckOpenOrdersAction`.** The control's gate deliberately differs from its neighbour's: the repair renders only on an open drift FINDING (it writes trades to correct a proven-wrong belief, §9's stance), this renders on CAPABILITY — not stopped, something resting — because it re-derives what is true now and is byte-for-byte the pass the alarm already runs unattended every 30s, and because **it is needed exactly when no finding exists to gate on** (a blind poll raises `poll_blind`, not drift; gating a recovery control on the broken machinery is 18.1's silent-gap failure in a new place). **The dialog says it can place orders, because it can:** on a RUNNING GRID bot a folded buy places its paired replacement sell, since 19 derives `placeReplacement` from `status === "running"` where the repair passes `false` unconditionally — correct behaviour, and a live order from a button labelled "check", which the operator is told before pressing. `deferred` gets its own banner: three empty arrays are indistinguishable from a clean pass. The three alert-type strings moved to `shared/alert-types.ts` as `POLL_HEALTH_ALERT_TYPES` with `POLL_STANDING_ALERT_TYPES` built from them, because the dashboard needs them and a hand-copied list there is precisely the silent-rename failure that module exists to end. **22 tests (1504 → 1526), 6 mutants all caught**; `FakeExchange` gained `orderStatusFailureFor` so the MIXED pass (one order unreadable, another refused) can be expressed at all. Three are worth recording: the **self-resolving loop** (raising without adding the key to `pass.standing` means each clean pass closes the row and the next re-raises it forever — step 18's failure mode through the back door, invisible in any single-pass test, 4 failures); the **cadence test that asserted nothing** — its first version delivered a tick before each pass so the measured age was zero every time and it would have passed at ANY threshold including one second, meaning the one number this step was asked to justify had no test behind it until it was rewritten to poll *between* ticks across the full ~130s gap; and the **guard that would not have caught its own case** — `standing.test.ts` listed the standing types by hand, so a fourth was covered only if whoever added it remembered to extend the guard, which is the exact thing the guard exists to catch. It now imports the real exported set. **Deliberately not done:** no escalation tier for `price_updates_stale` (one `warning`, no second flag or lifecycle while the first has never fired in anger); poll interval and 5-failure threshold still decisions not measurements (19.3, 20.1) — the staleness threshold is the first number in this arc derived from a real measurement; **not deployed**; `applyMissedFills`/halt-sweep still untouched per the operator's standing direction. **Is 19–22 complete? As scoped, yes** — every "deliberately not done" item 19–21 assigned to 22 is closed. Three things remain open by boundary rather than oversight: a poll-observed fill still waits for the next tick (now a *sound* wait rather than a hopeful one, which is what this step was for, but not the same as acting immediately); `bot-44400a`'s 0.00031777 BTC still has no in-system home (19.1); and nothing here has run against a live bot. Open questions: **a running bot that never receives its FIRST tick is still unmonitored** (null `lastPriceAt` raises nothing and the poll never runs on it, since it cannot place its base order without a price — closing it needs either a `startedAt` in runtime state or an alarm on every running bot regardless of open orders, a materially different timer budget); `price_updates_stale` can outlive its condition (20.2's rule); its resolution is coupled to the venue being readable because the resolve half is one call for every owned type; and **the threshold is derived from Gemini BTCUSD-shaped activity**, a liquidity-dependent measurement, so a genuinely thin pair could sit quiet past ten minutes without anything being wrong — first false positive is the signal, and the constant is one line. 21's open questions 1–4 all stand, including the suite's pre-existing uncounted test file (`60 passed (61)`) |
| [23.md](23.md) | 23 | **The manual halt control, and a missing log entry.** The dashboard half of `POST /api/bots/:id/halt`, which shipped 2026-08-03 reachable only by `curl` — and which left **no decision-log entry at all**, recorded here along with the endpoint's facts re-verified from source. `HaltAction` renders on `running` only, with a plain confirm dialog (type-to-confirm stays reserved for the systemwide pull) carrying the **required free-text reason** the endpoint demands — gated on the TRIMMED value, because `requireString` trims before testing and a button that can only 400 is one this codebase refuses to ship. `created` is excluded even though the backend ACCEPTS it: a deliberate scope choice (a bot that has placed nothing does not need halting, and halting it strands it in a status whose only exit is the heavier, latch-asserting `resume`), pinned by a test so the reasoning stays legible. `already_halted` is its own outcome rather than a success, because the operator's typed reason is silently **discarded** in that case and the bot keeps the first halt's reason. The success banner refuses to promise a clean exchange: the status flip is durable and comes FIRST, so an unconfirmable cancellation leaves its order open with a `cancel_failed` alert and the halt still returns 200. Six tests, mutation-checked with five mutants — one of which exposed a **vacuous assertion in this session's own test**: a flat DCA position serialises as `"0.00000000"`, so `not.toBe("0")` stayed green against a zeroed position. |
| [24.md](24.md) | 24 | **A severe-tier trip investigated, and the silence that preceded it.** A 2267-unit unexplained `0G` increase reported against `gemini-main` was **never on `gemini-main`** — all 1,359 `0G` snapshot rows are `main` (Binance), whose faucet holding it legitimately is; `0G` is a real token (0G Labs, CoinGecko rank ~653) holding fake testnet units, and `0G`-under-a-Gemini-label was step 16's bug, already fixed. It **did** trip the account-wide breaker (`main`, 19:15:38Z, run `12ec9351`, 75,397-char reason, 256 severe findings across 504 assets) and **halted nothing** — `"halted":[]`, because `main` has zero bots; all ten are on `gemini-main` and untouched. Cause: the Binance testnet faucet re-provisioned the account during a **7-hour HTTP 502 outage** (85 consecutive skipped passes, `reconciliation_blind` correctly raised at 12:05:41Z), so the first successful read was compared against a 7.5-hour-stale baseline. Every finding was arithmetically correct; `main` re-armed with the cause in the reset note. **The real defect is the four passes at 18:55–19:10**: `getAccountBalances` SUCCEEDED returning no holdings, and `if (assets.size === 0) return []` made "the exchange says you hold nothing" indistinguishable from "nothing to check" — no snapshot, no `skipped` entry, no alert, and worse, they counted as **observations** entitled to close live standing alerts on the strength of seeing nothing (section 5.6 reached through a branch that never touched it). Fixed with `auditEmptyBalanceSet`: distinct `reconciliation_empty_balance_set` standing alert (NOT the blindness type — "could not read" and "read, told nothing is there" need different operator responses) plus a `skipped` entry so the empty set is recorded as **unread**, preserving the baseline. **The obvious guard cannot work**: gating on `capital_ledger` expectations is dead code, since an empty union already proves there are no ledger rows — and `main` has none. The discriminator is **prior observation**. 6 tests replaying the real recorded sequence, 5 mutation-verified against two mutations; 1571 pass (1565 before). **A THIRD finding, from assessing that deferral rather than filing it**: `recordedActivity` opened its window at the account's **oldest** snapshot and handed every asset one lifetime sum, which the caller added to a five-minute-old baseline that already contained it — so the reported discrepancy WAS the account's lifetime trading, negated, on every pass. Its own docblock claimed the correct behaviour; the caller never implemented it. Confirmed against live data to 8 decimal places (`gemini-main`: BTC 0.00149818, USD 96.24) and climbing as a ratchet — **0.0965% against a 0.1% meaningful threshold, ~$3.53 from crossing**. Fixed with per-asset windowing (`activitySince`) plus a per-contribution `attributed` flag; safe to deploy directly because it drives discrepancies TOWARD zero, verified against live balances first. 4 tests, all mutation-verified — they need two snapshots with a trade between, which is why all 52 existing tests passed before and after. **Stale-baseline comparison after a long outage stays deferred, with its obvious remedy marked as a trap**: adopt-on-expiry converts a false positive into a false negative and would silently adopt a balance drained during the blind window. 1575 pass |
| [25.md](25.md) | 25 | **The account-level summary on the bot list.** The main page had seven count tiles and a table of per-bot money, and no answer to "what is this account doing" — totalling meant reading ten rows by hand, in a column whose bots need not share a currency. Built as a second row inside `StatusStrip` (`AccountSummary.tsx`) folded by a pure, React-free `accountTotals.ts` alongside `derive.ts`, exact `bigint` Money at SCALE 8 with **no float constructed anywhere**. **The brief said not to assume where fee data lives, and the assumption would have been wrong in the expensive direction:** fees are the one figure not derivable from a DO snapshot — they are in D1 `trades`, and of the two fee columns only `fee_reporting_amount` is summable (`fee_amount`+`fee_asset` is whatever the venue charged, commonly BNB; summing it adds BNB to USD and calls the result money). **That column is nullable on purpose and that nullability is the load-bearing fact**: `#mirrorTrade`'s rate lookup knows only the base asset's price, so an unpriceable fee leaves all three reporting columns NULL (step 2 decision 9, migration 0001's `fee_conversion_all_or_nothing` CHECK) — and **SQLite's SUM skips NULLs silently**, so the sum alone cannot tell "paid 4.12" from "paid 4.12 plus unknown BNB". `feesFor` is therefore **two queries, not one**; the `IS NULL` count is not diagnostics, it is what makes the omission representable. Three more fields added: **`position.cost` on the grid arm** (was DCA-only, so the grid arm published a held *quantity* with nothing to value it against and every "in position" total silently omitted grid bots — grid's `ladder.heldCost` renamed to match DCA's rather than the reverse), plus **`lastPrice`/`cycleCount`, which were already in the snapshot `botSummary` is handed and simply unpublished**, so marking to market previously cost one detail fetch per bot for a number the list had already read. Both are **null for an orphan, not 0** — an object with no state has not seen a price of zero nor completed zero cycles — pinned by a test. **Cost stated rather than buried:** 2 D1 reads/bot on a 5-second poll (20/poll at ten bots), issued inside the *same* `Promise.all` so latency is one round-trip; the cheaper single account-wide SUM was rejected on **correctness not cost**, since it blends amounts denominated in each bot's own capital asset into a total in no currency at all. **Three decisions:** grouped **per capital asset always** (one group today, but the failure it prevents is silent); **net withheld, not caveated**, when any of three gaps exists — orphan, held position with no price (§5.6), unpriced fee — which is **not a new rule** but `realizedPnl`'s existing `complete: false` arm ("an overstated profit is exactly the error a trading system must not make quietly"), with every gross figure staying visible; and **gross beside net, neither replacing the other**, matching 10.7's per-bot convention, with no bare "P&L" tile anywhere. **Two findings from reading the capital ledger, neither in the brief:** `releaseBotCapital` subtracts a closed bot's allocation from the ledger but **leaves `allocated_capital` on the row** as history, so summing every bot reports returned capital as still committed — an overstatement growing monotonically with every bot ever closed, correct-looking on day one; and `close` cancels orders and releases capital but **does not flatten the position**, so a stopped bot can still hold inventory, which makes **negative idle capital real** and therefore unclamped (amber, not red — red reads as "a loss" everywhere else here). **6 tests, each mutation-verified against the mutation it exists to catch**; the fee test drives real fills through `applyMissedFills` so §5.5's actual conversion runs, asserting `0.25` and **not** `0.325` — the sum that would have added two currencies; the precision test uses fees whose exact total is `1000000000.00000002` where a float gives `1000000000.00000000`, confirmed by running it. **1581 pass** (1575 before). **The frontend fold has NO tests** — the dashboard has no test runner, `vitest.config.ts` runs only the Worker suite, and standing one up is its own deferred step; behaviour was verified off-tree by bundling with esbuild across eight scenarios (all as designed, including negative-idle and asset-grouping), which is **a one-off run, not a regression guard**. Open: `unpricedCount` says how many fills, not how much — quantifying needs a rate the system deliberately refuses to invent |
| [26.md](26.md) | 26 | **The archive action, and what it deliberately is not.** A finished bot had nowhere to go, and the bot list is the page an operator reads to see what is happening *now*. `bot_instances.archived` (migration 0007, additive `ADD COLUMN`, last in the column list because `ALTER TABLE` appends and `schema.test.ts` compares order), two thin idempotent endpoints, an `ArchiveAction` control and a "Show archived" toggle. **Explicitly not a delete, and structurally so rather than by intention:** the storage layer has **no `delete` method at all** (`/src/db/table.ts`, for §8.7), `no-raw-d1.test.ts` fails the build on any file outside `/src/db` reaching for the raw binding, the migration is purely additive, the write is **column-disjoint in both directions** from `#mirrorStatus` and the capital ledger, and the Durable Object is never asked to mutate — only `snapshotIfCreated()` for the response. Held still by a **deep comparison of the whole `GET /api/bots/:id` payload across an archive**, two fields permitted to differ, with three pre-archive history assertions that stop it passing vacuously against a bot with no history. **Not in the DO**: archived is not trading state, nothing branches on it, and putting it there would mean a `schemaVersion` migration on every bot (§16) plus a mirror path for a flag only a D1-derived list view reads. **Three scope questions settled by the operator first** — `created` is *not* archivable (step 23's `HaltAction` exclusion again, pinned by a test); `start`/`resume` **refuse** an archived bot with `bot_archived` (409) rather than auto-unarchiving, preserving the invariant that **nothing hidden is capable of trading**; and archived bots **still count toward the account totals**, since an archived bot is halted-or-stopped, not closed — it holds its allocation and may hold inventory, and a total that changed when a view toggle flipped is step 25's silent omission in a new place. That last answer is what makes the filter **client-side with no server parameter**: the dashboard needs every bot anyway, so a parameter its only caller never sends would be an unexercised branch. The gate reads the **D1 row, not the snapshot** — a deviation named because an **orphaned** bot must still be archivable and there is no snapshot to ask. **Two mutants survived the first version and both were right to:** the idempotence guard was **unreachable** behind a redundant caller branch, so the "twice writes one audit entry" test was passing through a different path than the one it protects (removing the branch made the conditional `UPDATE` the single decision, ledger-style); and **"unarchive has no status gate" was not observable at all**, since every bot reachable through the archive endpoint is halted or stopped and a copied gate lets all of them through — the case needs a bot archived in a status archiving itself refuses, reachable only by writing the flag directly. **One mutant still survives deliberately** (the archive `WHERE`'s status condition, which defends only a race needing step 21's stub-harness concurrency), and `start`'s gate is **unreachable today** but tested by direct write, because it becomes reachable the moment `created` joins `ARCHIVABLE_STATUSES`. 13 tests, **1594 pass** (1581 before). **Open:** the frontend still has no test runner (typecheck + `vite build` only, and the filter is three inline lines rather than a pure module like `accountTotals.ts`); **an archived bot still costs a DO read and two D1 reads per 5-second poll**, so fleet polling cost grows monotonically with every bot ever created — the real argument for a server-side filter, which loses today to totals correctness and whose honest fix is server-computed totals; the toggle does not persist; one cosmetic race is open by design (an archive landing between the resume gate and the status flip); not deployed |
| [27.md](27.md) | 27 | **The halt alert that outlived the halt.** `#halt` inserts a `halt_{reason}` row and **nothing anywhere ever set `resolved` on one** — so a bot halted, fixed and resumed left a `critical` standing open forever, permanently inflating the one number step 18 did real work to make meaningful again ("how many unresolved criticals are there"). This is **step 16's bug 3 one layer out**: 16 fixed `halt_reason`, the current-state *column* a running bot kept advertising a fixed failure in, and left the alert *row* for the same halt behind. **Deliberately NOT `resolveClearedStandingAlerts`**, and the reuse question was the one thing worth checking first: it would produce exactly the right rows, but its `observed` flag guards section 5.6's nastiest failure (a pass that found nothing because it SAW nothing) and a resume observes nothing — it is a state transition, not a detection pass, so it would have to mean something different by the word, and a guard one caller may redefine is a guard for nobody. `src/alerts/halt.ts` therefore sits beside `standing.ts`, keeping its **ownership** rule (`source` scopes the resolve) without pretending a halt is a standing condition; `haltAlertType`/`isHaltAlertType` move to `shared/alert-types.ts` because a resolver makes a second place that must agree about the format. Closes **every** open `halt_*` row for that bot (the bot is `running`, so no halt is current; the episode-matching alternative needs `halt_{reason}` re-parsed out of `"{reason}: {detail}"` and strands every pre-existing row), and **nothing else** — `cancel_failed` may still mean a live order and is reconciliation's, the poll's standing alerts have their own owner, `price_feed_fanout_failed` is another writer's. Placed after the status writes so a resume refused by a latched kill switch keeps its alert open. 6 tests, 4 mutants, all caught; the per-bot scoping property needed a **second Durable Object on one database** to be observable at all. **1600 pass.** Open: `close()` has the identical gap, left unfixed on purpose |
| [28.md](28.md) | 28 | **The section 21.3 watchlist, in D1 and behind an API.** Section 21 stays PLANNED, NOT YET BUILT in every part but this: the first of 21.3's two candidate sources — the fixed, human-chosen watchlist — moved out of code into `watchlist` (migration 0008, additive, STRICT) with a write path enforcing what a config constant cannot, plus `GET`/`POST /api/watchlist` and `DELETE /api/watchlist/:id`. **The separation from the trending pull is the rule this step exists to hold**, and it is structural: no `syncFromTrending`, no upsert, no bulk import, both entry points refusing the automated actors `placeholder-balance.ts` already names. The merge is tempting (both produce pairs, both feed one pipeline per 21.2) and wrong twice — 21.5 requires every proposal to display *which watchlist entry, or which trending pull*, destroyed at the moment of writing; and 21.3's **hype hazard** means a human's stated `note` and a popularity number become indistinguishable to every later reader, which is how a proposal comes to read as confident because its input was loud. **Three shape decisions the sketch did not settle:** `account_label` is in the table, because "validate against `listTradablePairs`" is meaningless without one (that call resolves `accounts.exchange` to pick a venue, and the venues spell pairs differently) — giving `accounts` its first foreign key; removal is a **soft delete**, forced not chosen, since `Repository` offers no `delete` at all and `no-raw-d1.test.ts` fails the build on routing around it (step 26's `archived` shape, one table over); and the 10-cap counts live rows **globally**, since a pipeline run's cost is per candidate wherever it is registered. The cap is code-only and **deliberately not also a trigger** — this schema has no triggers, and 21.5 is explicit that "a second implementation of a risk check drifts from the first"; while raw SQL was the editing path the bypass was stated rather than hidden, and was the strongest argument for the endpoints. **Removal does not re-check tradability, now a settled decision rather than a flagged deviation:** refusing to remove a *delisted* pair traps exactly the entry most in need of removing and lets an outage freeze the list, so the gate belongs on the action that adds risk, not its reversal — the same stance `unarchive` takes ("a gate here could only ever strand a bot", step 26) and `halt` takes in asserting neither latch (steps 15, 23). Fail-closed runs **both** directions: a pair absent from the listing AND a listing that could not be read are both refusals, §5.6 one layer out. The endpoints wire `TradablePairSource` to the **real** cached `listAccountSymbols` — the same call and cache the symbols dropdown uses, so the two cannot disagree about what a venue lists — with a test asserting two adds cost ONE venue call; statuses reuse codes of the same *shape* (`cap_exceeded`/`already_watched` 409, `not_watched` 404, `pair_not_tradable` 400, `tradable_set_unreadable` **503 not 502**, since it is a refusal to write on unverifiable input rather than a relayed read failure). A body `actor` is **refused, not ignored** — a new convention, recorded as one — because the module refuses automated actors on purpose and a caller-supplied actor would sign someone else's name to the one table whose value is that a named human vouched for each row. `DELETE` by id adds the one guard the module cannot: the unique index is **partial**, so a removed and a live row may share a pair, and passing a dead row's pair through would make the module match the LIVE one. **Verified live by the operator 2026-08-08** — 0008 applied clean ("No migrations to apply!"), real Access JWT via `cloudflared`, real accounts, the **real 392-pair Gemini listing**, add/read/duplicate-409/untradable-400/actor-override-400/unknown-account-404/remove-200/second-remove-404 all as designed, and exactly two `audit_log` rows on remote D1 with correct actor, pair and order. **The most useful result was a refusal nobody predicted:** `"btcusd"` was rejected, because `listTradablePairs` does NOT report the venue's wire format — `parseSymbolList` upper-cases Gemini's lowercase `/v1/symbols` to this system's `Pair` convention, so the real listing is `BTCUSD`. Migration 0008's header and `assertTradable`'s docblock both claimed `btcusd`, written from memory of the Gemini API *in the very comment justifying an exact match*; both are corrected along with the fixtures, which had modelled the wrong catalogue. **This confirms the no-case-folding decision on evidence rather than argument** — a folded match would have stored a symbol every later exchange call had to re-spell, looking fine until the first `getCandles` — and re-earns the rule this project keeps re-learning (10.14, 14.10, 17): read a value's shape from the function that produces it, never from the vendor's docs. `parseSymbolList` was four lines and in the repo the whole time. Also recorded: **the first verification attempt failed entirely**, every request 302'ing to Access login because `cloudflared` was not installed or logged in — an environment gap, not a code defect, whose lesson is on the handover commands, which assumed a local login state that did not exist and failed as a page of HTML rather than an auth error. **Mutation testing did real work in both halves:** session 1's 13/13 includes a survivor that was right to survive (the removal's `removed_at IS NULL` guard, invisible to every sequential test, defending the interleave where both callers read the live row before either writes — closed with a `Promise.allSettled` test, and it is the guard the live second-removal 404 exercised against real D1); session 2's first run caught only **11 of 17**, and all five survivors were genuine gaps — the removal actor never tested against a competing value, "the pair validated is the pair stored" unpinned, the registry-derives-the-exchange claim asserted only in a docblock, and `not_watched`'s status-map row reachable by **no test at all** because the handler's own `ApiError(404)` shadows it, which is why `src/api/envelope.test.ts` now exists. One equivalent mutant survives deliberately (`body.actor` is provably `undefined` behind the refusal guard). 1600 → 1633 → **1667 pass**. **Deliberately not done:** no dashboard control (deferred again, on purpose), and no candidate selection, trending pull, `getCandles` extension, Workers AI call or proposal record — all of section 21 beyond this table, with 21.7's five open questions untouched. `readWatchlist` still has no non-test caller by design. Open: the schema guard's "if `bot_instances` is absent they all are" premise now has a counterexample (0008 can be missing while it is present, turning a pre-migration `POST` into a generic 500 rather than a `no_schema` 503) — not changed here, recorded so it is not a mystery; the cap's count-then-insert is not atomic; and a pair delisted after it was added stays on the list until someone removes it |
| [29.md](29.md) | 29 | **`getCandles` for any listed pair, and the endpoint that proved it.** Section 21 stays PLANNED, NOT YET BUILT in every part but 21.4 Stage 1's first bullet: candles for a coin that has no bot, which a candidate coin never does. Reading the source first found the spec's premise was understated — `getCandles` is a METHOD on `RestExchangeClient`, so reaching it means holding a client, and it has **no production caller at all** (the rate-limit pass-through, six tests, one script; not even the price feed, which 14.3 moved to the WS replay batch). The interface was therefore left **untouched** and wrapped: `fetchCandleWindow` returns a `CandleWindow` carrying the candles exactly as the client returned them plus `earliestOpenTime`/`truncated`/`missingHistoryMs`. **The reuse is the point in both directions** — `clientForAccount` is `envSymbolLister`'s own two steps lifted into `exchange-dispatch.ts` so the symbol listing, the candle fetch and the watchlist's gate resolve a client one way; `checkTradable` is step 28's `assertTradable` lifted into `tradability.ts`, RETURNING a refusal so each caller keeps its own error type while sharing the code and message. **Truncation is measured off the client's own `since` contract** (candles close AFTER `since`, so a fully-satisfied request's oldest candle OPENED at or before it), and `truncated` is DERIVED from the shortfall so a true flag with a null figure is unrepresentable. **Fail closed five ways**, including a successful-but-empty response (`no_candles_returned`) — a judgement call past the letter of the requirement, taken because an empty array survives every type check and reaches a human indistinguishable from a coin that did not trade. **`VERIFIED_INTERVALS = ["1m"]` enforces a step-14 docblock that had been advisory**, and it must be a hard refusal because the failure is silent: the venues spell the wider intervals differently (Gemini writes `1hr`/`6hr`/`1day`), so a wrong mapping returns correctly-shaped candles of the WRONG duration. **`GET /api/accounts/:label/candles`** publishes the whole window (money as decimal strings — `Candle` holds five bigints and `JSON.stringify` throws on them) and reuses `STATUS_BY_CODE` rather than inventing a scheme; `unknown_account: 404` is load-bearing because the same code is an `ApiError` elsewhere and would otherwise take the table's 400 default. **VERIFIED LIVE against real testnet Gemini by the operator on 2026-08-08, and the headline finding is a negative one: 21.7 open question 1 is NOT settled.** A targeted second run (three calls) found `earliestOpenTime` **identical** across `since=24h`/`48h`/`72h` (`1786156920000`), `count` identical (558) regardless of `since`, `missingHistoryMs` scaling linearly and correctly (20,000 / 86,429,000 / 172,828,000 ms) against a boundary that does not move, and `latestCloseTime` identical between two runs ~28 minutes apart while `count` fell 587 → 558 — the oldest bound advancing ~29 minutes while the newest did not move at all. **The "9.78 hour window" is an artifact, not a measurement**: it read 9.30 half an hour later. More consistent with the testnet sandbox serving a fixed, non-advancing block than with a live venue window; the mechanism is not determinable from outside Gemini. `arithmeticChecks: true` stands and matters — the truncation reporting is honest relative to whatever boundary the venue returns — but that is evidence about the CODE, not about the venue's depth. **New open question: sandbox-only or also production?**, which needs production Gemini credentials in a pointed environment and is deferred deliberately. `interval_not_verified` fired for `1h` and `invalid_filter` for `1w`, confirming the two-tier distinction over the wire; lowercase pair, fake pair, unknown account, missing pair, bad `since` and POST all refused as designed. A real candle showed `volume: "0.00000000"` — a low-liquidity testnet bar, noted so it is not mistaken for a parsing bug. **The first verification attempt 404'd `no API route` and it was not the code: the branch was green and not deployed** — the mirror of step 28's 302 (a CLIENT precondition) and just as invisible, since `/health` answered fine from the previous build; comparing its `version` to the build under test is the one-line check nobody has written yet. **30 mutants then 35, all caught**, but only after six real first-pass survivors: the gate moved AFTER the registry read (no code changed, only the I/O the ordering exists to avoid — closed with a counting `db`), a blank `?pair=` (also a 400, wrong code, wasted catalogue call), `closed` hardcoded true (every fixture was a closed candle), `since` forwarded as an explicit `undefined` key, and `clientForAccount`'s SUCCESS branch which **no test had ever reached**. Two session-1 mutants died to typecheck rather than a test and are named as such. 1667 → 1693 → **1728 pass**. **Unverified/deferred:** `envCandleLister`'s actual `getCandles` call is exercised by NO test — the live run is its only coverage; `5m`/`15m`/`30m`/`1h`/`6h`/`1d` remain unverified on any venue and `1m` is verified on Gemini only, not Binance; deep history is still unobtainable (a scoping limit was removed, not the depth limit); and candidate selection, the trending pull, the news fetch, the Workers AI stages and the proposal record are all untouched |
| [30.md](30.md) | 30 | **The news/sentiment vendor question, investigated and deliberately PAUSED — not resolved, not abandoned.** 21.4 Stage 1's second bullet is now blocked on a decision rather than on code. A full CoinDesk `fetchNewsSentiment` **was built and mutation-tested** (three-variant `NewsSentimentResult` — covered / `no_articles_in_window` / `not_covered` — fail-closed throughout, no caching, `fetchedAt`/`coverageCheckedAt`, `raw` carried by identity; 48 tests, **1728 → 1776**, typecheck clean) and is **deliberately left UNCOMMITTED in the working tree**, not discarded: it is the reference for whoever resumes this, and re-deriving it would cost the same effort twice. **40 mutants, 37 caught on the first pass, 40/40 after fixes** — the survivor that mattered was a blank-API-key test that installed no `fetch` stub and asserted only `ok: false`, so with the guard deleted it made a **real outbound call to CoinDesk** and still passed, because a blocked call also fails; closed by asserting **zero requests sent**. A second survivor changed no behaviour, only a diagnostic message (kept, because that message is what a first live run reads to learn which assumption was wrong); the third was an **equivalent mutant the session itself flagged as worthless** (a cast to its own type) and replaced. **Then the vendor stopped being free:** verified against CoinDesk's OWN announcement, not a third-party summary — "As of **May 21, 2026**, we will be retiring our free API tier", "accounts without a subscription will no longer have API access", no public pricing, no trial, sales only. So the first live run that would settle whether ANY of the module's assumed field names are right now costs a sales conversation. **A three-vendor survey was run before spending anything**, tagged by source reliability throughout because the vendors' marketing and their own legal terms disagree: **CoinGecko** is not a candidate (no sentiment field at all; news endpoint needs paid Analyst-tier-and-above) **[P]**; **NewsData.io** has sentiment on paid tiers only, and as a paid option competes with CoinDesk while putting its key in the **query string** **[V-blog]**; **APITube** is the only free tier carrying pre-scored sentiment (numeric score AND polarity in three scopes, a documented 0.4 threshold and a fourth "mixed" state the current three-label taxonomy cannot represent) plus **"Suggest" enumeration endpoints** that might support a coverage check at all — but **how its sentiment is produced is undisclosed anywhere in its docs**, which is *worse* than CoinDesk on 21.5 req 2, since CoinDesk's categorisation prompt is at least public. **APITube contradicts itself about commercial use, on its own domain, currently:** its ToS says the "Developer" plan is "development and testing in a development environment only" with the licence revoked in staging or production, while its marketing FAQ says "the free tier can be used for commercial applications"; the pricing page names the zero-cost plan **"Free"**, not "Developer", so whether the clause even applies **cannot be determined from outside APITube**. Read from primary sources and **independently re-confirmed by the operator** — live, not stale. Its ToS also appears to restrict **storing** returned data, which would collide with **21.5 requirement 5's permanent, full-input audit log**; recorded as an open question against the audit design generally, since **CoinDesk's terms were never checked for the same clause**. **The finding most likely to outlive the rest is vendor-independent: ~12-hour article delay** on APITube's free tier **[P]** and NewsData.io's **[V-blog]** — `fetchedAt` is honest about when the CODE fetched and says nothing about how old the data already was, so a truthfully-stamped proposal can still be wrong about the world, which is requirement 6's "degraded proposal indistinguishable from a good one" arriving through the one channel nothing measures. Same shape as step 29's truncation problem and the same answer: **make the delay a first-class field**, as `CandleWindow` carries `truncated`. **Does the module survive a vendor change? Two-thirds** — the result union, the non-empty tuple making "covered with zero articles" unrepresentable, fail-closed, no-caching, `raw` retention and the publication-time unit check are genuinely vendor-agnostic; the **two-call coverage protocol** (category list then article list) and the **three-label taxonomy** are NOT, being built on an assumption neither alternative shares (an enumerable asset index), so this is a **real redesign of the part the module is about, not a field-mapping swap**. **Paused because nothing else depends on it** — candidate selection, Assess, Derive, the proposal record and the audit table are all unbuilt and none takes news as an input — so deferring costs only the deferral, while committing now buys an unquoted subscription to validate guessed field names for consumers that do not exist. **To resume, any one of:** a written unambiguous commercial-use answer from APITube (plus sentiment provenance and the storage clause); a decision on whether ~12-hour-old sentiment is acceptable at all; or a decision to simply pay for CoinDesk or a paid NewsData.io tier. **This session: research and this entry only** — no fetch code written or modified, no vendor contacted with an API key (only public docs, pricing, ToS and CoinDesk's announcement), no test run, nothing deployed |
| [31.md](31.md) | 31 | **Candidate selection for 21.2's two entry points, and the trending vendor researched before a line was written.** `selectNamedCandidate` / `selectGeneralCandidates` in `src/research/candidates.ts` — Stage 1 (Gather) plumbing only: no LLM, no Assess, no Derive, no proposal assembly, nothing persisted. **38 tests, 24 mutants, 24 caught, every one by a test and none by typecheck. Suite 1776 -> 1814**, both figures measured this session (the baseline by re-running with the new file moved aside, not by subtraction); typecheck clean; nothing deployed and **no trending API ever called from this repository**. **The vendor research came first and did NOT repeat step 30: CoinGecko's `GET /api/v3/search/trending` is genuinely on the free Demo tier** **[P]** — top 15 coins by *searches on CoinGecko* in the last 24 hours, refreshed every 10 minutes, `x-cg-demo-api-key` header, key required, 10,000 monthly credits, 1 credit per 200 — and **its terms contain no development-environment-only clause**, which was APITube's disqualifier; **ToS 4.1.6 grants the right to charge for products incorporating the API without reference to plan**, while the pricing page markets "Commercial license" as a paid feature and the words "Demo", "free plan", "non-commercial" and "personal use" **appear nowhere in the API Terms** — a tension, not a contradiction, and moot for a private bot that sells and redistributes nothing. **The finding that does matter is the storage clause, and it is now the SECOND vendor to have one:** 6.2 forbids storing or deriving from Data "except as expressly permitted", 6.1.1 says a cache should refresh at least every 24 hours, and **6.1.4 requires permanent deletion of all Data on termination** — flatly incompatible with **21.5 requirement 5's permanent, full-input, indefinitely-retained audit log**. Raised against APITube at step 30 and established here against the vendor **the spec itself names**, which makes it look less like a fact about one vendor and more like **a constraint on the audit design**: what may a permanent proposal record contain from a third-party feed at all. Unblocking for this step only because **this module persists nothing** — now a stated reason rather than a coincidence. **4.4's "Powered by CoinGecko" attribution (>= font size 10) is an unmet obligation the moment trending data reaches a UI**, and 21.5 req 2 guarantees it will. **The Demo rate limit is unresolved between two of CoinGecko's own pages** (pricing says 100/min, the support KB is reported as 30/min and 403'd to an unauthenticated read) — irrelevant at one call per run, recorded because two primary sources disagreeing is a fact, not a discrepancy to average. **CoinMarketCap set aside**: trending first appears on the **$79/mo Startup tier**, closing the question regardless — with the caveat that its docs render client-side (step 30's CoinDesk obstacle) so the per-endpoint gate came from the pricing matrix, and a second claim from that same lossy read was **discarded rather than recorded**. **What the endpoint measures is narrower than the spec's prose suggests — searches on one website**, which makes 21.3's hype hazard sharper: nothing may read `rank` as evidence in a coin's favour. **The shape:** one `CandidateSet` for both entry points (21.2: they "differ only in how the candidates are chosen"), kept from converging **by their ports** — `NamedCandidatePorts` has no trending source in it, so the named path cannot reach a vendor even by mistake. `Candidate.sources` is a **non-empty tuple** (step 30 called that discipline "the best thing in" `news.ts`), and **dedup MERGES provenance rather than dropping it**: a coin on both sources is one candidate carrying two, keeping `entryId` and `pullId` for 21.5 req 2's "WHICH watchlist entry, or WHICH trending pull" — the convenient alternatives (a boolean, a set of names, first-one-wins) all destroy that at the moment of merging with nothing able to recover it. `watchlist`/`trending` are **null** on a named run rather than zeroed, since a zero would claim a read that never happened. **A failed trending pull is FATAL, not a fall back to the watchlist** (21.5 req 6 names it explicitly), and the error says how many watchlist entries were discarded; the cost is real and was chosen, because a watchlist-only set is not a smaller general run but "a re-read of a list you wrote yourself" (21.3) wearing the name of a general one — an explicit watchlist-only entry point can be added later **under its own name**, and tightening this later would be breaking while loosening it is an addition. **An unreadable catalogue aborts too and is specifically NOT recorded as a rejection** — a rejection list calling fifteen coins untradable when nobody could tell is a false record (section 5.6) — and it aborts on the first one. **An empty result is a FACT and is reported as one.** **The assumption, stated plainly:** nothing maps a vendor ticker to a venue pair (`SymbolListing` has no base/quote split; `getSymbolFilters` costs one request per pair), so `${BASE}${QUOTE}` is **constructed as a hypothesis and judged by the real catalogue** through the shared `checkTradable` — safe because a hypothesis that passes IS the catalogue's own spelling character for character, so step 28's exact, case-sensitive comparison survives intact; the ticker is upper-cased, which translates a vendor's vocabulary rather than case-folding a comparison. It assumes base-then-quote with no separator (true for Gemini and Binance, **not universal**), and **`TrendingRejection.triedPairs` exists so that failure is legible as fifteen rejections naming fifteen tried pairs rather than as a quiet day** — step 29's `truncated` posture: make the limit observable, do not work around it. `quoteAssets` is **required with no default**. **Watchlist entries are NOT re-validated at selection time** — step 28's recorded gap, left as a deviation because closing it changes what watchlist membership means; a test pins the current behaviour. **Mutation survivor 1 of 2: every fixture in the file happened to be alphabetical**, so the two tests whose whole purpose was asserting first-appearance order asserted a list that sorting produces identically, and the documented ordering guarantee was checked by nothing; closed with deliberately non-alphabetical fixtures. **No test here has spoken to any venue or vendor**: every `TrendingSource` in the suite is a stub written by the session that wrote the code, and the perp catalogue entries are real strings copied into a stub. **VERIFIED LIVE by the operator against a real CoinGecko Demo key and real Gemini** (the coding session still has never called either): **15 coins returned; symbol casing UPPERCASE as assumed; `${BASE}${QUOTE}` real and exact -- 3 of 15 matched Gemini's catalogue** (`IOTX`, `SOL`, `ZEC`), 12 did not. **The trending source is not theatre on Gemini, and it is thin** -- twelve rejections is the normal shape of a run. **Recorded as a dated observation in the step 29 mould, NOT a rate**: one snapshot, one venue, one 10-minute refresh window, at **approximately 2026-08-09 between 02:25 and 02:53 operator-local time -- an APPROXIMATION, a ~30-minute window reconstructed from recollection and conversation order rather than a logged timestamp** (the terminal session was closed before the exact time could be captured; no UTC offset recorded). **The imprecision is the same size as the thing measured** -- the endpoint refreshes every 10 minutes, so the data could have turned over ~3 times inside the window, and step 29's candle figure decayed from 9.78 to 9.30 hours in half an hour, which is exactly why the instant matters. A later run comparing against these figures is comparing against a snapshot whose own position is known to +/-15 minutes. **No code changed for the match rate** -- low-but-nonzero is the case the module was written for. **The sharpest finding was a near-miss that was correctly NOT a near-miss:** `HYPE` trended, Gemini lists no spot `HYPEUSD` but DOES list `HYPEGUSDPERP` and `HYPEUSDCPERP`; a perpetual is a different instrument (margin, funding, liquidation) and every path here is spot, so it is rejected plainly with `triedPairs: ["HYPEUSD"]` and the perps are never mentioned -- correct, and **correct by accident**, since `checkTradable`'s exact case-insensitive near-match was written for step 28's casing lesson and `grep -rn "PERP" src/` returns **nothing**. **The pinning test written for it did not work, and that is worth more than the test:** aimed at `selectGeneralCandidates`, which **discards the refusal message**, it survived a `.startsWith` mutant unchanged -- it was asserting on the one path where the bug is structurally unreachable. Re-aimed at `selectNamedCandidate`, which propagates the message verbatim and under the mutant answers *"that venue spells it HYPEUSDCPERP"* -- **recommending a perpetual to a human by name, in a message written to be helpful.** Both `.startsWith` and `.includes` now caught. **Fourth and fifth times a test has been found passing for the wrong reason** (26, 28, 29, 30): *a fixture chosen for readability tends to be sorted, and a sorted fixture cannot test order*; and, new here, *a test aimed at the path where the failure is structurally unreachable will pass forever and reads exactly like one that works.* **FLAGGED, PRIORITIZED GAP -- spot versus perpetual, live in production today and reachable by an ordinary human mistake, not an edge case:** `parseSymbolList` passes every catalogue string through unfiltered, so **`addToWatchlist`, `fetchCandleWindow` and `selectNamedCandidate` would each accept a perp with zero resistance** and nothing at any later stage would flag it, up to a bot proposal a human is asked to approve; the trending path is safe only incidentally. **Closing it needs its own decision** -- a spot-only filter at the shared `checkTradable` level (one place, per 21.5's rule against second implementations of a risk check; needs a per-venue answer to what marks an instrument as not-spot, since `PERP` is one observed Gemini convention and Binance's is unexamined) **or** an explicit recorded accepted-risk. **Deliberately NOT fixed in this step**, per the operator's scoping direction -- a decision about *when*, explicitly not a judgement that the gap is small. **Still unverified:** the **merge path has never fired against real data** (nothing on the watchlist overlapped the snapshot); argued safe structurally -- both sides are pinned to the same catalogue, so two equal members of one string list are identical strings and the merge cannot silently fail on a spelling mismatch -- but argued, not observed. Cheapest verification: put `SOLUSD`, `IOTXUSD` or `ZECUSD` on the watchlist and re-run; **blocked on a candidate-selection endpoint, which does not exist**. **To verify the rest:** the Demo key as a Worker **secret** (never a query param), a decision on the storage clause before any raw payload is persisted, an `envTrendingFetcher` and wire-format parse (deliberately unwritten -- step 30's "designing an abstraction to fit the only vendor it has ever seen is how the wrong seam gets set in concrete"), and a verification run that captures its own timestamp at the time -- a process gap in the same class as steps 28's and 29's unasserted-precondition lessons: this run produced good data and lost the one field that dates it |
| [32.md](32.md) | 32 | **A real tradability gate on `POST /api/bots`, and the spot-versus-perpetual check that closes step 31's flagged gap at the one place it ends in an order.** Research found something broader than the flagged gap alongside it: **bot creation performed no tradability check of ANY kind** — `pair` was a free-typed string stored verbatim and `bot-instance.ts` has never called `listTradablePairs`, so a typo, a delisted symbol, a wrong-venue pair and a perpetual all reached creation identically. Both are now closed at that endpoint by composing **two** checks from the existing shared `tradability.ts`: `checkTradable` (catalogue, KV-cached, usually free) then `checkSpotInstrument` (per-symbol details, uncached). **Deliberately NOT folded into one function** — they ask different questions at different costs, and folding would force `selectGeneralCandidates` to pay a per-symbol request for up to fifteen trending coins times each quote asset per run, turning one cached read into dozens of live ones. The instrument answer is a **REAL structured field, not a suffix rule**: Gemini's `product_type` (`spot`/`swap`, documented as "Instrument type") decides and `contract_type` (`vanilla`/`linear`/`inverse`) corroborates, with a contradiction mapping to `unknown` and refusing. A `PERP` suffix is documented as a rule **nowhere** in Gemini's reference and guesses in both directions — a perp named without it passes, and a real spot pair whose base asset is literally `PERP` (Perpetual Protocol is a real token) would be refused. Fails closed **three** ways — `instrument_not_spot` 400, `instrument_type_unknown` 502 (**including when the field is ABSENT**: Gemini lists spot and perps in one catalogue, so "no instrument type" and "this might be a perpetual" are the same sentence — §5.6's rule one level up), `instrument_unreadable` 503. `VENUE_PUBLISHES_INSTRUMENT_TYPE` is an exhaustive `Record<ExchangeId, …>` so a third venue **fails to compile** until answered; `binance: false` is structural (its `/api/v3/exchangeInfo` IS the spot API, perps live on `fapi.binance.com` behind `contractType: PERPETUAL`, a host this system cannot reach), and Binance is asked no instrument question at all. **The gate runs in the HANDLER, before `botStub` is even asked for**, because the property is not "creation is refused" but **nothing happened** — a check inside `create` would already have instantiated the DO, and one after `createBotInstanceWithCapital` would leave capital reserved for a refused bot. **Two corrections landed before a line was written, both from checking rather than assuming:** the research predicted the testnet watchlist AND bot list were empty — the watchlist was (1 row, 0 live), but **`bot_instances` held 11 real bots with real allocated capital** (9 `BTCUSD`, 2 `DOGEUSD`), a prediction derived from reading this log, which does not record current state (10.14's rule, repeating); and the hoped-for "creation already fetches symbol details so this is free" is **false** — `create`/`createGrid` set `filters: null` and only the ORDER path calls `getSymbolFilters`, so this genuinely costs one extra uncached request per creation. **Verified live by the operator, 2026-08-09**, on real deployed testnet with the build confirmed via authenticated `/health` (closing step 29's lesson): the flagged unverified assumption was settled FIRST by unauthenticated calls to `api.sandbox.gemini.com` — `BTCUSD` → `spot`/`vanilla`, `HYPEUSDCPERP` → `swap`/`linear`, **the exact branch pair mutation testing found masking itself**, now confirmed on real data; then `HYPEUSDCPERP` → **400 `instrument_not_spot`**, `FAKEUSD` → **400 `pair_not_tradable`**, `BTCUSD` → **201**; and the load-bearing part — after both refusals D1 showed **only the one created bot**, `total_allocated` carrying nothing from either rejected attempt, and **exactly one** `capital.allocated` row. **The sharpest proof: re-using the SAME `botInstanceId` rejected on a perp returned 201, not 409** — a live DO answers `409 already_created` on id reuse, so refusal precedes **any side effect of any kind**, not merely a database write. **15 mutants, 15 caught — but TWO survived the first pass and were the same bug, and the lesson generalises beyond this file.** `parseInstrumentKind` answers `derivative` from two independent lines, and every fixture supplied BOTH fields, so `contract_type` answered first and the `product_type: "swap"` line was never reached; reciprocally, dropping `inverse` survived because `swap` caught it next. Each line masked the other and the test aimed at one passed on the strength of the other. Closed with fixtures that **isolate one field at a time**. Recorded as a **STANDING CONVENTION**: *when a check reads multiple fields that can each independently signal the same conclusion, a fixture supplying all of them lets any one field silently backstop the others — there must be a fixture per SIGNALLING FIELD, not per conclusion.* Invisible to inspection (the fixture looks more realistic, the assertion is correct, the suite is green) and worth applying to every future multi-field check here. Seventh instance in this project of a test passing for the wrong reason, second in two consecutive steps. 11 files, +963/-12; **1814 → 1834**, typecheck clean; the pre-existing `68 passed (69)` + worker-teardown error verified unchanged on clean `HEAD` by stashing. **Deliberately deferred:** the **suffix-heuristic follow-up for the cheaper research paths** (watchlist/candles/candidates still have no spot check and would accept a perpetual today) is a separate, smaller step; the **Binance `isSpotTradingAllowed`/`permissions` gap** stays deferred, recorded in the venue table's own docblock; and a real **`v-spot-1` bot with $50 allocated now exists on testnet** from the verification run, left in place, **disposition undecided** — named so it is a decision rather than a bot nobody remembers creating |
| [33.md](33.md) | 33 | **The other half of the spot-versus-perpetual answer: a naming heuristic at the four research paths, and the masking it was nearly built into.** Step 32 closed the gap only at the order path; `addToWatchlist`, `fetchCandleWindow`, `selectNamedCandidate` and `selectGeneralCandidates` still accepted a perpetual with zero resistance, because `checkTradable` only asked "does this venue list this string" — and it does. Closed with a **free, per-venue suffix check** (`DERIVATIVE_NAME_SUFFIXES: Readonly<Record<ExchangeId, readonly string[]>> = { gemini: ["perp"], binance: [] }` — the empty array an *answer* meaning "this system reaches only Binance's spot host, so there is no derivative catalogue to name-match against", and the `Record` forcing a third venue to fail to compile rather than silently defaulting to "no perps here"), a new `pair_not_spot_by_name` 400, and `endsWith` **never** `includes` so Perpetual Protocol's real `PERPUSD` ticker is not wrongly refused. **THE FINDING THAT CHANGED THE DESIGN BEFORE ANY CODE WAS WRITTEN:** `checkTradable` has FIVE callers, and the fifth is step 32's bot gate, where it runs *first* — so an unconditional heuristic would have refused `HYPEUSDCPERP` at `POST /api/bots` by inference instead of by real `product_type`, **and masked `checkSpotInstrument` so completely it could have been deleted with every test still green.** That is step 32's own multi-signal-masking convention arriving one layer up, across two FUNCTIONS rather than two fields. Resolved by a **required** `DerivativeNamePolicy` argument with no default: the four research paths opt in, the bot gate passes `"structural-check-elsewhere"`, and a regression test pins that the perp there is still refused by `instrument_not_spot`. Documented throughout as **an inference, not a structural guarantee** — Gemini publishes no naming rule — with residual risk stated in both directions (a perp not following the convention passes silently; a future pair ending in those letters is wrongly refused) and the **false reject chosen as the safe direction because it is loud, fixable in one table entry, and cannot block a legitimate bot**. 14 files, +476/-28; **1834 → 1853**, typecheck clean; **14 mutants, 14 caught** (12 runtime + 2 by typecheck), **one survivor**: the `pair_not_spot_by_name: 400` envelope row was asserted by nothing, because the table's own default is also 400 — closed with `statusForCode(code, 599)`, and the same gap noted as still open on two pre-existing rows. **Verified live 2026-08-10T03:35:44Z** (timestamp captured *before* the run — step 31's lesson closed) at rev `0edd5a9`: 52 `PERP`-suffixed pairs live, **0 `PERP*`-prefixed — so the `PERPUSD` false-reject case remains a constructed fixture, never observed**; the regression check run FIRST and confirming `instrument_not_spot` unchanged; the watchlist refusal returning `400 pair_not_spot_by_name` **quoting the "THIS IS AN INFERENCE" caveat verbatim from source** (after a first attempt that 409'd on a stale row — **an operator sequencing error, recorded as a process artefact, not a finding**); and **both candle calls INCONCLUSIVE** — perp *and* known-good `BTCUSD` alike returned `502 NoDataFound`, a dated sandbox condition consistent with step 29's stale-window drift, so **the candles path stays UNVERIFIED LIVE** and must be re-attempted against a real spot pair first. **Deliberately deferred:** the Binance `isSpotTradingAllowed`/`permissions` gap; **neither gate ever re-validates existing bots** (11, or 12 counting step 32's still-undisposed `v-spot-1`) |
| [34.md](34.md) | 34 | **The over-concentration flag for 21.4 Stage 1's third read, two thresholds that are policy and say so, and a mutation test that reversed a design decision.** `readAccountExposure` / `assessConcentration` / `checkCandidateConcentration` / `assessCandidateSetConcentration` in `src/research/concentration.ts` — given a candidate, what the account is ALREADY holding on that pair and that asset. **A FLAG, structurally not a filter** (21.4: "a flag on the proposal for the human to weigh … not a silent filter"): nothing refuses, drops, reorders or scores a candidate, `assessCandidateSetConcentration` returns exactly one result per candidate in the candidate's own order, and the only two error codes are a blank field and a failed read — never "this candidate is bad", which is a deliberately different shape from every other refusal in the folder because those answer *can this system honestly do this* while this answers *should you want to*, and 21.1 puts that on the human's side. **The brief's premise was wrong and correcting it was the most valuable research output:** there is no `src/research/accountTotals.ts` — the file is `dashboard/src/accountTotals.ts`, it **never touches D1** (it folds a `Bot[]` already fetched over HTTP), it imports `./api/types` and `./derive` so calling it from `src/` would be a Worker-importing-dashboard inversion, and it has no tests; **nothing was reusable and the honest answer was to say so** rather than manufacture a shared abstraction across the layer boundary. What *was* reused: step 31's `Candidate`/`CandidateSet` by import (no pair/account/exchange type declared here, and **the account is not re-resolved** — the candidate already carries what `resolveAccount` read from the registry), `db.botInstances` as the only sanctioned D1 path, and **two RULES from step 25 taken as rules since the code was unusable** — `stopped` bots excluded from committed capital because `releaseBotCapital` leaves `allocated_capital` on the row as history (counted and reported *beside* the figures, since `close` does not flatten a position), and **grouped per capital asset always**, a share over a USD+USDT total being a percentage of nothing that looks completely normal. Archived bots count (step 26). **The two thresholds are POLICY, verified against NOTHING**, in one named constant for `VERIFIED_INTERVALS`'/`DERIVATIVE_NAME_SUFFIXES`' reason: **2+ same-pair committed bots** (21.4's own example read as a boundary rather than an illustration — the looser of two readings of one sentence) and **40%+ of committed capital in the candidate's base asset** (an argument, not evidence; 25% or 60% equally defensible), stated as policy in the docblock, the README **and every flag message a human reads**; and recorded in code, **the live data cannot discriminate between candidate values** — with 11 of 13 bots on `BTCUSD`, any threshold below ~80% fires identically. Both comparisons are "at or above", and the money one is **cross-multiplied bigint** so a value a hair under 40% that *renders* as `40.00000000` does not flag, tested one satoshi out both ways. **Three states, never two:** read failed → throws `bot_list_unreadable`; no rows at all → clean with `rowsRead: 0`; rows but all stopped → clean with `committedBots: 0` — the first because a risk check reporting "no concentration" *because it could not look* hides the exact risk it exists to catch inside a proposal that then reads as reassuring, and the last two being both-clean-and-not-the-same-sentence is step 24's `audit_empty_balance_set` and step 30's coverage variants again. Clean is a **variant**, not an empty array (the flagged variant's `flags` is a non-empty tuple). **A MUTATION TEST REVERSED A DESIGN DECISION:** the first version compared pairs exactly and case-sensitively with a confident comment arguing it matched `checkTradable` — a mutant making it case-insensitive **left the whole suite green**, and asking why produced the real answer: `checkTradable`'s exactness is load-bearing because **that** pair is about to be sent to a venue, while this one is a grouping key that never leaves the process, so copying the rule was reasoning by resemblance. The failure directions are asymmetric — folding needlessly over-counts a flag a human dismisses, **not** folding reports "0 bots on BTCUSD" with nine `btcusd` rows in the table, a **false clean result** — so it now folds, with `samePairSpellings` making the fold never silent. **The mutant found a wrong argument in a comment, not a bug in the code**, in the direction that produces silence. **The base-asset split is a narrow heuristic with a narrow safety argument:** `bot_instances` has **no base-asset column**, and neither available grouping is the asset 21.4 means (by pair, `BTCUSD` and `BTCUSDT` are the same BTC exposure twice; by capital asset every bot is `USD`, so the share is 100% forever and **a flag that always fires is not a flag**). Step 30's refusal to split a pair **stands**; this splits only on **observed** quote assets (the account's own `capital_asset` values plus what an operator states), **longest first** so `BTCUSDT` is `BTC` and never `BTCT`, and a pair matching none is **counted as undetermined** making every base-asset figure a stated **floor**. Safe here and not in `news.ts` for one specific reason: **this module cannot block anything**, so a wrong split yields a sentence a human can see is wrong — the report names the suffix stripped — never a refused pair or an order, where `news.ts` would produce a false claim about vendor coverage that reads like a finding. When no split is possible the signal **degrades to the exact pair and says so**, never to silence. **VERIFIED LIVE 2026-08-10T20:35:36Z by the operator's own read-only `SELECT`s: 13 real bots** — **not 11** per log 32, **not 12** assuming only `v-spot-1`; `v-perp-1` too, both created by steps 32/33's own verification runs — **11 on `BTCUSD`, 2 on `DOGEUSD`, ZERO stopped, one quote asset (`USD`), 1750.00 USD committed** (1650 BTC / 100 DOGE). Exact computed output recorded in full for both candidates and **reconciled against by-hand arithmetic computed before any output was seen**: `BTCUSD` → both flags, `samePairBots: 11`, **94.28571429%** (1650/1750, half-even at SCALE 8); `DOGEUSD` → **5.71428571%**. **`v-perp-1` IS A SPOT BOT** — pair `BTCUSD`, name leftover from a rejected-then-reused botId in step 32's regression test — asserted by name, because nothing here infers an instrument from a bot id and a future reader grepping "perp" will find that row. **THE CORRECTED FINDING, recorded as more than a typo: `DOGEUSD` IS flagged**, by the same-pair count signal **at exactly the policy boundary** (two existing bots, threshold two), despite its 5.7% share — the first summary said DOGE "does not flag", which is **true of one signal and false about the result**, and that is precisely the class of error this project's discipline exists to catch (step 25's NULL-skipping `SUM`, step 30's coverage distinction, this module's own refusal to report clean on a failed read), **caught here in the summary of the work rather than in the code**, which was right all along. Pinned by a test whose comment says so. **The bot count keeps moving and this is the THIRD correction from stale log-reading** (28, 32, now 34): verification runs create real bots, so a count is stale the moment it is written down — 10.14's rule, and three occurrences is a pattern rather than bad luck; written into `src/research/README.md` too, where the next session actually reads. **Correctly built, never observed** — same category as step 31's merge/dedup path and step 32's Binance permission gap, not new kinds of problem and not fixed: **the stopped-bot exclusion rule** (no bot in this account has ever been stopped, so `expect(exposure.stopped).toEqual([])` is explicitly commented as **not a pass for the rule**) and **the multi-quote-asset path** (this account has only ever used `USD`). Two fixture facts named rather than implied: the **per-bot split** of each pair total is invented (the read gave pair totals), licensed by a test asserting the report is **invariant** to the split, and the **bot ids** apart from the two real ones. A schema fact came from a failing test rather than an assumption: migration 0001's `allocated_capital > 0` CHECK makes a zero-capital asset group **unreachable through the real table**, so `sharePct`'s null branch is pure-fold-only and stays as a guard against a future migration. **+51 tests, 1853 → 1904. 19 mutants, 19 killed**, re-run in full after the test file was rewritten around real data — including all four threshold mutants and the rounded-share comparison. **One flake recorded as INCONCLUSIVE:** `src/api/api.test.ts`'s live-price-in-the-LIST test failed once, in a file this work never touched — **1 failure in 6 runs with these changes, 0 in 3 clean-tree runs**, co-occurring with the `vitest-pool` teardown segfault present as `Errors 1` in **every** run including all clean baselines; isolated storage makes cross-file contamination implausible, but one-in-six versus zero-in-three settles nothing, so it is **neither attributed to this work nor ruled out**. **Deferred:** no endpoint (**note: `policy.assetCapitalShareFlagAtPct` is a `bigint` and needs `api/serialize.ts` treatment before it can cross HTTP**, like every money field — it serialised cleanly in verification only via the harness's JSON replacer); no dashboard surface, though 21.4 requires the flag be "presented prominently"; **not wired into a combined Gather-stage bundle** — this consumes step 31's types but nothing calls selection and this in sequence, and neither has a non-test caller. Nothing deployed; no `wrangler` command, no deploy and no exchange call by the sessions that wrote the code |
| [35.md](35.md) | 35 | **Stage 1 assembly: an honest partial bundle, the isolation that makes it honest, and the paused input given a place in the type.** `gatherCandidateData` / `gatherCandidateSetData` in `src/research/gather.ts` — the wiring step 34's own entry named as missing ("not wired into a combined Gather-stage bundle"). **It adds NO read**: no query, no wire format, no endpoint, no persistence, no model call; it composes `candles.ts`, `candidates.ts` and `concentration.ts` and carries step 30's paused news state in the fourth slot. **An HONEST PARTIAL bundle — collection, not judgement.** Whether a bundle is good enough to show a human is **Stage 4's** job (21.4, "Explain and assemble"), which does not exist; putting it here would hide a policy decision in the collection layer. This does **not** weaken 21.5 requirement 6, and the distinction runs in two directions: **WITHIN** an input fail-closed is untouched (`fetchCandleWindow` still throws six ways, `readAccountExposure` still throws rather than reporting "no concentration" for a read it could not do — nothing here catches those and substitutes a value); **BETWEEN** inputs a failure is **recorded, not propagated**. The failure a whole-bundle throw would cause is specific: **one unreachable venue would erase a concentration flag read successfully from D1** — exactly what 21.4 wants "presented prominently" — and losing a real risk signal because an unrelated fetch failed is not failing closed, it is failing closed on the wrong thing. Value reachable **only** through the `ok` arm (section 5.6's `ExchangeOutcome` discipline, one layer up). **`GatheredInput<T, E>` composes and invents no error vocabulary:** the `failed` arm carries the producing module's **own** error object, so `error.code` reads as it would for a direct caller (`candles_unavailable`, `interval_not_verified`, `bot_list_unreadable`) — a second vocabulary restating those as some flatter `fetch_failed` would drift, and 21.5's duplicated-risk-check rule applies unchanged to duplicated failure descriptions. **Three design notes.** (1) **`Candidate` gets NO envelope**: it exists only because selection succeeded, so selection's failures are refusals that produce no candidate to bundle, and a wrapper would model a state unreachable by construction — made unrepresentable rather than checked for, like `Candidate.sources`; it is asserted to arrive **verbatim by identity** (21.5 req 2). (2) **`threw_unexpectedly` is a distinct state because it is REACHABLE**, backed by a real gap: `fetchCandleWindow` **does not wrap its ports**, so a `Database` or `CandleSource` that throws raw yields something that is not a `CandleWindowError`, and folding it into `failed` would type `error` as `CandleWindowError` while holding a D1 exception; `error` is `unknown`, not `Error`, since a thrown non-`Error` is legal JavaScript. (3) **Concentration's `threw_unexpectedly` is currently UNREACHABLE** — `readAccountExposure`'s catch is total and normalises even a thrown string — and **a test pins it** by driving a `findMany` that throws a bare string, so removing that catch surfaces there rather than in a proposal. **The isolation trace is control flow, not an assertion:** `gatherAgainstExposure` has **no `throw` and no early return**; `foldConcentration`'s parameters **do not include the candle result**, so the coupling is absent by signature and a later edit would have to add an argument to introduce it; `runInput`'s catch is total and returns a value; **no shared mutable state** — no accumulator, no first-error variable; and `gatherCandidateSetData` does one read then a `for` loop that pushes **unconditionally** — no `break`, no `continue`, no predicate. Independent fields with **no shared status and no "first error"**, so candles-failed / concentration-failed / all-succeeded are three distinguishable states. **The news slot is a STATE, not a TODO:** `NEWS_NOT_YET_AVAILABLE`, frozen and shared, in the same discriminant position as every other input's state, with **no `error`, no `failedAt` and no `fetchedAt`** — no request was made and a timestamp would claim one was. "News could not be fetched" invites a retry; the truth is a deliberate pause with three unmet conditions, and **those two sentences must never come from the same branch** — here there is no branch that produces the first. `NewsInput` is a **one-armed union on purpose** (the `ok`/`failed` arms are not pre-declared because nothing can produce them); a vendor landing makes every exhaustive switch stop compiling, which is why the pause lives in the type. **`GatherPorts` holds no news source**, so assembly cannot reach a vendor even by mistake — `candidates.ts`' technique reused. **Every fetch keeps its own timestamp** (21.5 req 4): `candles.value.fetchedAt`, `concentration.value.readAt`, `candidate.sources[]`, and a failed slot's **`failedAt`** (when the failure was *observed*, since no fetch supplied one); `assembledAt` sits beside them, documented as **not one of them**. Tested with a clock that **moves on every read**, and a stub venue answering far from that clock, so a collapse fails there rather than in a proposal. **ONE `bot_instances` read for N candidates** (step 34's design reused), reported on `exposure`; a failed read lands on every candidate's slot as **the same error object by identity** (`toBe`) — accurate because it genuinely was one read, not a duplicated guess — while candles are still fetched per candidate and a **fold** refusal isolates one candidate from its neighbours. **26 tests, 1904 → 1930**, both measured this session by running the suite, not by subtraction; typecheck clean. **10 mutants, 10 caught**, aimed at isolation: rethrow-instead-of-record, candles short-circuiting the fold, a failed read throwing before candles, a failed read reported as **clean** (the one whose survival would be invisible — a clean concentration result is well-formed and hides the exact risk the check exists to surface; the test asserts the **absence of the value**), every throw dressed as the module's own refusal, the news slot rebuilt per bundle, the set dropping or stopping at a failed candidate, a per-candidate re-read, and the quote-asset merge dropped. **M5 and M8 were each caught by a SINGLE test, recorded as thin rather than glossed** — thin in the way that matters, but in both cases the catcher is the test **written for that exact property**, not a bystander that happened to trip. **Six assumptions stated.** Chiefly: **`interval` is REQUIRED with no default** — `VERIFIED_INTERVALS` holds only `"1m"` so a default would be a *correct guess today* and silently wrong the moment it widens, and **the failure mode is why this is not style: each venue spells the wider intervals differently (Gemini writes `"1hr"`/`"6hr"`/`"1day"`), so a wrong interval does not error — it returns correctly-shaped candles of a DIFFERENT duration, which passes every type check and no downstream reader can distinguish**; and **the quote-asset merge is asymmetric between the two functions** — the set-level one reads the operator's own assets off `CandidateSet.trending.quoteAssets` and **merges** (not replaces; `normaliseQuotes` keeps longest-first), while the single-candidate one gets **none**, because a `Candidate` carries no quote assets and inventing some is the silent mis-split `AssessConcentrationOptions.quoteAssets` documents against. Plus: sequential not concurrent (rate budget); `fetchCandleWindow`'s duplicate registry read and `checkTradable` accepted rather than worked around (step 11's rule beats saving a cached call); an exchange/registry disagreement **not** made a new error state since both values are in the bundle. **NAMED OPEN QUESTION — the rate-budget cost of an N-candidate gather.** **No run in this system has ever issued N sequential candle requests as one logical operation** — every fetch to date is one request per operation (a strategy tick, or the single-pair candles endpoint). A general run is exactly that shape: 21.3's 5–10 watchlist coins **plus** a trending pull, so a dozen-plus venue requests in sequence with no pacing of this module's own. Whether that trips **section 5.4's rate budget** — and therefore whether **`candles_unavailable` becomes the NORMAL outcome for the tail of a candidate list rather than an exception** — is **unobserved and unanswerable by stubs** (every test drives an injected `CandleSource`; the tests prove the outcome is *recorded* correctly, not how often it occurs). "Honestly reported and routinely empty" is a design problem, not a success. **To be checked once `gather.ts` has a real caller — an endpoint or the Assess stage — and NOT before:** nothing invokes it in production, so answering it now means standing up a temporary caller and inventing the request shape, candidate count and interval the real caller will decide, **re-deriving exactly the conditions this step correctly deferred**; the number would have to be redone and would be worse than none because it would look like an answer. **No live check for this step, judged unnecessary and agreed independently:** no new read/write/wire-format boundary exists, the three inputs' live behaviour is already proven (29, 33, 31, 34's 2026-08-10T20:35:36Z real-data run), and isolation is a structural claim about control flow that a live run tests no better than the mutants do. **Both suite runs report `Errors 1 error`** — the pre-existing `vitest-pool` teardown "Worker exited unexpectedly", present in the clean-tree baseline, attributable to no test file, **no test failed in either run**; the same one step 34 recorded. **Deferred:** no endpoint; **no judgement of any kind** (no ranking, scoring, filtering or refusing); no "what failed" summary helper, since shaping it before Stage 4 exists shapes it to a caller nobody has written; no concurrency; no news vendor; **`gather.ts` has no non-test caller**, because the thing that would call it is the proposal pipeline. **No `wrangler` command, no deploy, no exchange call and no news vendor contacted this session** |
| [36.md](36.md) | 36 | **Stage 1 assembly gets a caller, and step 35's open question is answered at N=8 and no further.** `GET /api/accounts/:label/gather` over `gatherCandidateData`/`gatherCandidateSetData` — Access-gated, read-only, reusing the same cached `listAccountSymbols` and uncached candle lister every other research path uses. **A third entry point, `selectWatchlistCandidates`, recorded as a DEVIATION from 21.2's two** and justified by 21.2's own rule (the doors "differ **only** in how the candidates are chosen"); the prohibition it does not touch is a *general* run that quietly degrades, and `selectGeneralCandidates` still throws `trending_unavailable` rather than fall back. `?entryPoint=general` is **503 `no_trending_vendor` before any work** — not a stub that fails, which would report a pull that was never attempted (`NEWS_NOT_YET_AVAILABLE`'s distinction at the wire). **The serialization boundary was the real risk, and two of its three hazards are invisible from inside the module:** `Error`'s `name`/`message` are **non-enumerable**, so an unserialized error is `{}` — coded and mute, at 200; and two raw `bigint`s throw `JSON.stringify` outright, `policy.assetCapitalShareFlagAtPct` (a *policy* field, the last place anyone auditing money looks) and `ExposureBot.allocatedCapital`. Third: `String(Object.create(null))` **throws**, which would turn a 200-with-one-failed-slot into a blanket 500 and lose the concentration result that succeeded — `gather.ts`'s isolation defeated one layer up. `jsonSafe` and `candleWindowView` reused verbatim; only the error views are new. **No top-level success flag, deliberately** — a 200 means assembly ran, and a 200 with every slot failed is correct. **Live (operator, real Gemini testnet): 8 watchlist entries added clean, N=8 run twice back-to-back, 16/16 candle fetches `ok`, zero failures, no tail clustering, ~2.1 s and ~1.5 s spans under a second apart.** Control run returned **1440** 1m candles. **Conclusion stated at the width of the evidence: N=8 confirmed safe; larger N untested** — 9/10 (the watchlist cap) and a dozen-plus general run are near and unmeasured, so this is NOT "no rate limit exists". **And the question itself was wrong:** 5.4 is scoped to *order-execution* requests, so "does this trip the rate budget" needed no live run — `clientForAccount` never applies `withRateLimit` and `resolveGeminiExchange` binds no `RateLimiter` at all, so the research candle path is **ungated and unobserved**. The clean run does not change that a future limit hit would surface only as failed slots. **Provenance recorded honestly:** the reported "commit `59c5da5`" is **step 35's** commit and contains no endpoint, and `/health` returns a static `package.json` version — it confirms reachability, not revision; the deploy bundled an uncommitted working tree. **This system cannot confirm which revision is deployed.** **Item 8 reassessed:** the never-revalidated pre-existing bots are **empirically clean** — all 13 on `BTCUSD`/`DOGEUSD` per step 34's live read, neither a perpetual — so the gap is real but **currently empty**; re-scope the flag rather than repeat it, trigger revalidation on *the pipeline gaining the ability to create bots*, and build it inside **section 9 reconciliation**, not a new cron. **Halt/archive UI correction:** the dashboard gates tighter than the API — `HaltAction` renders only for `running` and `ArchiveAction` only for `halted`/`stopped`, so a `created` bot shows **neither button**; halt needs curl, archive then works in the UI. **Neither releases capital** — only `stopped` does, via `releaseBotCapital`, and `BotInstance.close()` **has no route**, so log 32's "archived and released" disposition is unreachable and "archived" means hidden, not cleaned up. **1930 → 1964 (+34), both measured; typecheck clean; 15 serialization mutants, 15 caught** (8 by a single test, recorded as thin). **Two self-caught errors:** an assertion reading `not.toBe("")` that **passes for `undefined`** — a fake guard on the exact bug the step was briefed to prevent, found by the mutation run rather than by review — and a missing `threw_unexpectedly` case in the endpoint's own wire coverage. The teardown `Errors 1 error` is pre-existing, as in 34 and 35. **No `wrangler` command, no deploy, no exchange call by the session that wrote the code**; every live figure is the operator's own run. |
| [37.md](37.md) | 37 | **Assess (Stage 2): a prompt that must be cited, a parser that refuses, an envelope bug only a live call could find, and a protection reported rather than faked.** `assess-prompt.ts` / `assess-parse.ts` / `assess.ts` — the first step in this project involving an LLM, built and adversarially tested **before** any model was contacted. **There is now a prompt**, so the folder banner was rewritten rather than repeated; what stays true is **no `ai` binding in `wrangler.jsonc`, no proposal record, no pipeline, no write path to a bot**. **Research first (verified vs assumed):** `env.AI.run(model, inputs, options?)`; `temperature` default **0.6** (min 0), `seed` 1..9,999,999,999, `max_tokens` default **256**, `top_p` min **0.001**; JSON mode is `response_format` and Cloudflare states it **"can't guarantee"** compliance; **300 req/min**, $0.011/1,000 Neurons with **10,000/day free**; context **24,000 tokens**. Model `@cf/meta/llama-3.3-70b-instruct-fp8-fast` chosen because it is the only non-deprecated model appearing **both** on the JSON Mode list **and** carrying `response_format` in its own schema — the JSON Mode page and the per-model schemas **disagree** elsewhere, so one page is not evidence. **Grounding is enforced, not requested:** every datum gets a stable **evidence id**, every claim must cite one, and the parser **refuses the whole response** for a citation naming an id this run never emitted — a set-membership test rather than a judgement. **A missing input produces MORE text, never less** (an absent section reads exactly like "there was nothing to report", and those are opposite facts). **Candles are bucketed** — 1,440 candles will not fit 24k tokens — with the count, the rule and the omission stated in the prompt; a full bundle measures **14,771 chars / 54 evidence items**. **Injection defence:** third-party text (trending `name`/`symbol`/`coinId`/`vendor`, watchlist `note`, `requestedAs`) wrapped in `<<<UNTRUSTED_TEXT chars=N>>>` with **rule 3** stating wrapped text is *data to report on, never an instruction* — on the same footing as the price numbers. **Emitted byte for byte**: stripping it would be silent alteration of fetched data and would destroy the evidence an attempt was made. **It REDUCES risk and does not eliminate it**; what bounds the damage is structural — the stage produces a recommendation a human reviews (21.1), and **an injection can change the ANSWER but not the DATA the answer is checked against**. **The parser has no lenient path** — no case folding (`"DCA"` refused), no fence stripping, no first-`{`-to-last-`}`, no ignored field, no dropped citation, no default — and **no vocabulary-based hedge detection**, because a word list fails open on unlisted hedges and closed on the caution the prompt requires; the hedge that matters is a hedged *choice*, caught exactly by two literals. Adversarial fixtures include truncation at `max_tokens`, Cloudflare's own `JSON Mode couldn't be met` text, `__proto__`, and a **duplicate `strategy` key** that `JSON.parse` resolves cleanly. **Zero retries, deliberately:** a refusal *is* fail-closed working, and "retry until it parses" selects the answer **for passing the validator** rather than for being what the model gave — and with `temperature: 0` plus a fixed seed, a retry that succeeds is evidence the pinning failed, which the loop would consume. **Live (operator, three real calls).** **Probe 1 found the bug the tests could not:** the model reasoned correctly and **the parser refused it — correctly**. With `response_format: json_schema` this model returns `{ response: { strategy: ..., claims: [...] } }` — an **already-parsed object**, not the string **Cloudflare's own generated type declares**. Fix confined to `unwrapModelEnvelope`; all four shapes land in **one shared `validateAnswerObject`**, so a rule relaxed for one path must be relaxed for the other in the same visible edit. **Probe 2 (`returnRawResponse`) closed the follow-up question with the wire bytes**: `rawBodyText` is literally `{"response":{"strategy":"grid",...}}`, so the object shape is real to the wire and not an SDK artefact. **The duplicate-key theatre finding:** re-stringifying the parsed object and scanning it would be **fake protection** — `JSON.stringify` of an already-parsed object can never contain a duplicate, so the scan returns null forever while reading like a guard, and **a check that cannot fail is worse than an absent one because only the absent one is visible**. Reported instead, per response, as **`duplicateKeyCheck: "performed" | "unavailable_transport_parsed"`** on both result types, so an audit record says which guarantees actually held. Two mutants pin it, including the theatre version itself. **Stated at the width of the evidence:** both real calls produced clean, well-grounded, correctly-cited `"grid"` answers that **correctly reported missing news as a limitation** — a positive early signal and **two data points, not a compliance rate**. **NOT a determinism test:** the two runs saw different `candles.earliest_close`/`latest_close` (new candles arrived between them), so inputs were not identical; **`temperature: 0` + fixed seed on truly identical input is untested**, and needs the same bundle fed twice. **Latency is a RANGE, not a figure: 13,540 / 20,326 / 10,614 ms** (~2× spread). The earlier one-point "~2.5 min for 12 candidates" is widened to **two to four minutes in Assess calls alone**, and Stage 3 is a **second** call per candidate — so **whether Assess can run synchronously in an HTTP response or must be queued is an OPEN QUESTION, to settle before Stage 3, not after**. **OPEN PROBLEM, structural:** declaring the `ai` binding makes `vitest-pool-workers` open a **remote proxy session at config-parse time**, and the suite reports **0 tests / 75 errors**; `vitest.config.ts` pins `environment: "testnet"`, so **the binding cannot be declared for deploys without also declaring it for tests**. Worked around for the probe by a three-line pre-deploy edit; **not a workable answer for production code**, and it must be solved before `envAssessModel` exists. **1964 → 2121 (+157)**, measured; typecheck clean. **30 mutants, 30 caught** (22 parser incl. 6 envelope, 8 injection); every pre-existing mutant still caught by **equal or more** tests after the refactor. **Two of this session's own mutants were worthless** — one equivalent by construction (like log 30's third survivor), and its corrected replacement then **survived because a two-element fixture was too thin**, fixed by strengthening the test: **the mutation run found the weak test, review did not** — the fourth time in this project a test has been found passing for the wrong reason. **Working tree recorded precisely:** the probe is **not yet removed**, and **`wrangler.jsonc` is NOT byte-identical to HEAD** — the revert left an inert commented-out `// "ai": {"binding": "AI"},`, which is why it was easy to miss. **Deferred:** the real `env.AI` implementation (blocked on the open problem above), Stage 3, Stage 4, the proposal record and audit table, any endpoint, any UI, and an identical-input determinism replay. |
| [38.md](38.md) | 38 | **The `ai` binding / test-suite conflict, resolved by one option — and entry 37's "structural" framing corrected.** Step 37 called this **"structural and unsolved"**; **it is not, and this entry supersedes that framing** (the log is append-only, so the correction lives here and `vitest.config.ts` points at it). **Real root cause, read from the installed `@cloudflare/vitest-pool-workers@0.18.6` rather than from docs:** `remoteBindings: z.boolean().default(true)` (`dist/pool/index.mjs:2514`), and the session starts only `if (options.remoteBindings)` (`:2603`). **The default is `true`**, so pointing the pool at a wrangler config holding a binding with no local simulation opens a remote proxy session **at config-parse time, before any test runs** — refused here by Cloudflare Access, giving **0 tests and one error per file**. Workers AI is such a binding because miniflare's `AI_PLUGIN` has **no local branch at all**: `getBindings` and `getServices` both unconditionally build a `remoteProxyClientWorker`. So the failure was **the pool's default**, not an incompatibility this project created. **Fix:** the real `ai` binding back in `wrangler.jsonc`'s **testnet** env (+3 lines), and `remoteBindings: false` in `vitest.config.ts` (+36, all comment plus one line) citing this entry and the installed line numbers. `environment: "testnet"` is unchanged, so step 4.1's rule stands. **Measured, with the failure REPRODUCED FIRST so the fix is known to address the real thing:** before — `Failed to start the remote proxy session … behind Cloudflare Access`, **0 tests**; after — **2121 passed (2121), 75/76 files, 1 error**, typecheck clean. **2121 → 2121 is not "no change"**: the suite was 2119 after step 37's probe removal and this step adds a **2-test pinning file**, which is the entire difference — the binding itself moved no count, which is the point. The single error is the **pre-existing** teardown `Worker cloudflare-pool emitted error`, checked rather than assumed, same as in 34–37. **`wrangler deploy --dry-run --env testnet` lists `env.AI  AI` in the upload manifest — a dry run; NO DEPLOY WAS RUN.** **All three candidate options verified by real runs, not by reading Cloudflare's docs** (the discipline this arc learned when the generated output type turned out wrong in 37): **(1) declaring `ai` only in vitest's `miniflare` key WORKS — tested literally, no crash, 2 tests passed — proving the pool does NOT special-case `ai` regardless of location (the trigger is the wrangler config path plus `remoteBindings`) — but it solves nothing, since deploys need the binding in wrangler.** **(2) A fake/mock `ai` binding is IMPOSSIBLE, tested directly:** miniflare has no hook (no local branch in `AI_PLUGIN`), and the documented `serviceBindings: { AI(request) {...} }` canned-response pattern **does not shadow an `ai`-type binding** — added alongside the real one, `env.AI` **still** threw `Binding AI needs to be run remotely`; experiment reverted. Even standalone it exposes `.fetch()`, not `.run()`. **The consequence is a design confirmation: a mock must live at this project's own port, which is what `AssessModel` already is.** **(3) `remoteBindings: false` achieves "exclude from tests" with no second config file, no env split, no duplication.** **New pinning test `src/workers/ai-binding.test.ts` (2 tests), because a configuration fact with no test is one edit away from silently reverting.** It defends two load-bearing facts: **the binding EXISTS in tests** (its absence would mean a lost wrangler declaration, and a deploy shipping an undefined `env.AI` that nothing else would notice), **and calling it FAILS locally and loudly** — `Binding AI needs to be run remotely`, thrown by `makeFetch`'s `remoteProxyConnectionString || throwRemoteRequired(...)` **before any request is constructed**, confirmed both by reading miniflare's `remote-proxy-client.worker.js` and by running the test. **The second is the guarantee that no test here can reach a real model or spend money** — a limitation by design, not one being tolerated. **Assumptions flagged:** `remoteBindings: false` is safe **only because no other binding here is remote-only** (verified by reading `wrangler.jsonc`: D1, KV, R2, DOs all locally simulated, none `experimental_remote`) — **add a genuinely remote-only binding later and this flag silently disables it in tests too**, with the same symptom `env.AI` now has by design; **production `wrangler.jsonc` is untouched**, a deliberate separate decision; the dry run proves **manifest inclusion, not a successful deploy**; and the pool's gate was read from **0.18.6 specifically**, with version and line numbers recorded in the config comment so an upgrade can re-check rather than re-derive. **Still NOT done, explicitly: `worker-configuration.d.ts` has NOT been regenerated**, so `Env` has **no `AI` field** and **no application code can reference `env.AI` and typecheck** — the pinning test casts locally for exactly this reason, and **`npm run cf-typegen` is a real prerequisite for `envAssessModel`** (it rewrites a 551KB tracked file), deliberately out of a step scoped to the binding conflict. **`envAssessModel` itself is still unbuilt**; `AssessModel` remains an abstract port with nothing behind it. **No deploy was run, so the deployed Worker still has no `ai` binding.** Entry 37's latency open question (10.6–20.3 s per call) is untouched and still open. **Zero deleted lines** in either tracked file. |
| [39.md](39.md) | 39 | **`envAssessModel`: the real transport, built as a composition — and the first live vendor call this project made without the operator running it.** Step 38 removed the blocker and said plainly it had not used it; this step builds the transport and **nothing else — there is still NO ENDPOINT**. **`src/workers/assess.ts`: 139 lines, of which 38 are code.** It defines **no settings, no parsing, no validation, no retry and no catch** — `ASSESS_MODEL_SETTINGS` is **imported and read off the request, never restated** (a second `temperature: 0` would be a second thing to keep in step), the prompt comes from `buildAssessPrompt`, the answer is read by `parseAssessResponse`. The only transformation is a rename to Workers AI's parameter names. **It performs NO narrowing, which is the point:** `return raw.response` is exactly step 37's live-found bug, so `text` and `raw` are the **same transport value by identity** and `unwrapModelEnvelope` owns the shape. A second temptation is documented because it looks harmless: **`JSON.stringify(raw)` would be actively dangerous** — it makes the answer arrive as text, which makes `findDuplicateKey` run, which reports `duplicateKeyCheck: "performed"` for bytes **this system generated itself**, i.e. step 37's theatre failure reintroduced one layer below where the parser's tests can see it. **Call path traced by reading** (`assess.ts:376` → `:382` → `unwrapModelEnvelope` → `validateAnswerObject`) **and corroborated at runtime**: the result carries `envelope` and `duplicateKeyCheck`, fields set nowhere but `parseAssessResponse`. **`cf-typegen` on a 551KB generated file: 11 insertions / 4 deletions there (12/5 with `package.json`), runtime types byte-identical, every line audited.** `AI?: Ai` is the intended change; **`SYMBOL_CACHE` and `ACCESS_TEAM_DOMAIN` are PRE-EXISTING STALENESS surfaced but not caused here** — added to `wrangler.jsonc` at step 11 and earlier and never regenerated since, so the generated `Env` had been quietly wrong for many steps. **The first regeneration BROKE the typecheck** (`TS2430`): plain `wrangler types` narrows `vars` to **literal** types, colliding with this project's own `declare global { interface Env { ACCESS_TEAM_DOMAIN?: string } }` in three files. Fixed with the documented `--strict-vars=false`, chosen for blast radius (touches only the generated file, versus hardcoding the team domain into typed source), and **`package.json`'s `cf-typegen` script updated to match — the script is the only durable record of a flag like this**. **Accepted cost, verified not assumed:** `ENVIRONMENT` loses its literal union, harmless because every consumer already validates at runtime and was written to (`geminiBaseUrlForEnvironment` takes `string | undefined` and throws in `default:` — *"Total on purpose"*). `ProductionEnv` correctly gained neither `AI` nor `SYMBOL_CACHE`; **production lacking `SYMBOL_CACHE` is a real pre-existing config gap, noticed here, out of scope here.** **The live call — real, specific, through the production path** (`assessCandidate(envAssessModel(env), bundle)`): `strategy: "grid"`, **`envelope: "envelope_object"`, `duplicateKeyCheck: "unavailable_transport_parsed"`**, 10,546 ms, 16,001 prompt chars, 54 evidence items, 4 claims, `rawKeys: ["response"]`, every citation a real emitted id, and the missing news input again reported as a limitation. **Two claims quote system-computed values exactly and one was checked ARITHMETICALLY:** the model wrote *"total volume … 1200.00000000"* and the fixture is 240 candles of `3 + (i % 5)`, which sums to exactly 1200 — **grounding confirmed by arithmetic, not merely by structure**. **How:** vitest remote-bindings with `remoteBindings: true`; the attempt against `wrangler.jsonc` failed exactly as step 38 predicted (Access blocks the shared workers.dev host), so it used a **throwaway wrangler config with a different worker name**, four temp files, all deleted. **No deploy, checked not asserted:** wrangler's log shows `POST .../edge-preview` then `teardown complete`, and `wrangler deployments list --name trading-bot-ai-probe` returns *"This Worker does not exist on your account"*. **FIRST live real-vendor call made by a coding session rather than the operator, and explicitly NOT A NEW DEFAULT.** Every prior one (Gemini steps 32–36, both Workers AI probes in 37) was operator-run. This happened because **the step's prompt pre-authorized exactly this action**, and was possible only because step 38 removed the Access constraint. **Per-step authorization, not standing permission** — the default remains *the session writes the code, the operator runs anything live*. Operator verification was agreed unnecessary **for this step specifically** (result cross-checks against both prior probes) and deferred to when an endpoint exercises **gather → assess → response together against real market data**. **10 composition mutants, 9 caught first pass, 1 survived.** **C1/C2 answer why this layer needs its own tests, as a measurement:** transport-narrows-`.response` and transport-stringifies were each run against `assess-parse.test.ts` alone and **NEITHER was caught** — those tests hand the parser a value directly, so a transport that mangles it beforehand is structurally outside what they can observe — while both are caught through the real port (3 and 7 tests). **C9 SURVIVED and is the FIFTH instance in this project of a test passing for the wrong reason** (26, 28, 29, 30; 37 held a thin fixture of the same family). A transport that **retried a thrown call** satisfied the propagation test identically, because the stub always threw and the test observed the error's identity but **never the call count** — so zero-retry was enforced for parse refusals and had a hole for transport failures, the harder one to see precisely because the observable outcome is unchanged. **Same symptom, new mechanism:** not a weak matcher (36) and not an absent network guard (30), but **an assertion about the wrong dimension of the same event**. Closed by counting calls; C9 then caught. **The mutation run found it; review did not, again.** **Suite 2121 → 2135 (+14), 76/77 files**, both measured; typecheck clean; the one error is the same pre-existing teardown failure as 34–39. **Assumptions:** the live call used a **SYNTHETIC bundle** (240 fabricated candles) so it proves transport/envelope/parse/grounding mechanics and **says nothing about answer quality on real market data**; **four live calls is four data points, not a compliance rate**; `--strict-vars=false` leaves a future exhaustive switch on `ENVIRONMENT` unchecked; the teardown check **covered one name** and did not enumerate the account. **Still NOT done: `envAssessModel` HAS NO CALLER — no endpoint exists.** No deploy was run, so the deployed Worker still lacks the `ai` binding; Stage 3, Stage 4, the proposal record and the audit table are untouched; **step 37's latency question is now better informed and still open** — 10.5–20.3 s across four samples, roughly 2× spread, and **the first thing the endpoint step must decide**; and no identical-input determinism replay has been run. |
| [40.md](40.md) | 40 | **The assess endpoint — gather → assess → response for ONE named candidate, verified end to end on real market data, on a sandbox whose data is not a real market.** `GET /api/accounts/:label/assess?pair=&interval=`, Access-gated, `envelope.ts` status mapping reused, plus `evidenceItemView`/`citedClaimView`/`assessResultView` and an injectable `assessModel` port on `ApiContext` **so no test can fall through to a paid vendor**. **6 files modified, 0 new, +687/−0.** **ONE candidate deliberately** — `entryPoint` fixed to `named` with no batch parameter to grow one by accident, because at 10.5–20.3 s per assessment a watchlist run is a **different design problem**, not a parameter on this one. **Duration settled BEFORE building, from Cloudflare's own docs:** HTTP-triggered Workers have **no wall-clock duration limit** (*"no hard limit … as long as the client remains connected"*), and the limit that does exist — CPU time, 30 s default on Paid — **excludes waiting on network requests** (*"Waiting on network requests … does not count toward CPU time"*). A model call is almost entirely that wait; the endpoint's own CPU work is milliseconds and it issues ~6–8 of 10,000 allowed subrequests. **One real caveat, quoted:** a runtime update gives in-flight requests a **30-second grace period**, which a 20 s request could collide with — rare, non-deterministic, presenting as a dropped connection. **ASSUMED, not verified: Workers Paid**, inferred from Durable Object usage; on Free the 10 ms CPU limit would likely break the prompt builder. **The failure rule: the model is asked only when the question can be grounded; every other gather failure is REPORTED, not propagated.** No candle window → the request fails via `assessCandidate`'s `no_price_history` precondition, **unbypassed** (mutant caught). **Failed concentration or the paused news slot → the assessment RUNS**, with the gap stated to the model as missing and returned verbatim in the bundle — refusing there would be the over-propagation `gather.ts` exists to avoid, losing a grounded assessment because an unrelated D1 read failed (mutant caught). A bad model answer is a **502 carrying the parser's own code**, mapped by class in one place rather than twenty near-identical `STATUS_BY_CODE` rows. **ATTRIBUTION CORRECTED HERE:** the *"no assessment was attempted"* improvement was reported as step 39's and **is this step's** — `git` shows `handlers.ts` at +178/−0, uncommitted, with HEAD at 39's `envAssessModel` commit which touched no endpoint. `assessCandidate` knows only `no_price_history` because at that layer the reason genuinely is not known; at the endpoint it **is** — the candle slot carries the producing module's own error — so the underlying code and status are surfaced (`interval_not_verified`→400, `candles_unavailable`→502) **with the precondition unchanged and still the thing that refused**. Prompted by a unit test expecting 400 that got 503, and now confirmed under a real call. **Live, operator-run, whole chain (the FIRST real end-to-end operator verification of Assess — every prior live check exercised one stage in isolation):** strategy **grid**, **4 claims**, every citation a real emitted id, **`range_pct` = 1.14868489% reconciles EXACTLY by hand against the real high/low in the same response**, news `not_yet_available` **and named as a real limitation in the model's own claim** (rule 5 working on real inputs, unprompted by any fixture), `envelope_object` + `unavailable_transport_parsed` **matching all four prior probes exactly** (five consistent observations of a shape Cloudflare's generated type still declares differently), latency **11,470 ms**. Five samples now: **10,546 / 10,614 / 11,470 / 13,540 / 20,326 ms**, ~2× spread, **not widened by a real end-to-end call**. Both failure checks fired live: `interval_not_verified` with the corrected message, `pair_not_tradable` unchanged. **⚠ THE DATA IT RAN ON IS NOT A REAL MARKET, and this outranks the success.** The real candles show **83 minutes at a single price and ~5 hours at another**, consistent with step 29's live finding that *"the sandbox's candle window does not slide"*. **Two things are true at once:** the grid recommendation is **correctly grounded in the data received** (a barely-moving series *is* range-bound, and grid is this system's strategy for that — the pipeline did its job), **and** that low liquidity is a **testnet artifact, not BTC's market character**. **So this run verifies the PIPELINE and says NOTHING about the QUALITY OF THE JUDGEMENT** — "the model recommended grid for BTC" is a sentence this entry deliberately refuses to endorse, and every strategy conclusion from sandbox data should be read as **mechanically correct and financially meaningless**. Sharper edge: **frozen data is exactly the input most likely to produce a confident-looking grid recommendation**, because flatness reads as a tight stable range — the shape of a proposal that looks good and is built on nothing, which is 21.3's characteristic failure arriving by a different route. **Answer quality cannot be assessed until this is pointed at a market with real price movement.** **11 mutants, 8 caught first pass, 3 resolved.** **S1/S2 are the least flattering finding and the most useful:** the tests asserted `expect(cited.value).not.toBe("")`, and **a dropped field is `undefined`, so that assertion PASSES** — precisely the weak-assertion bug **decision log 36 already recorded and named**, reintroduced verbatim by the session that wrote that entry. **Sixth instance of a test passing for the wrong reason** (26, 28, 29, 30, 36/39) and **the first that REPEATS a documented mechanism rather than finding a new one** — the earlier five each taught something; this one only shows that documenting a failure mode does not prevent it. Closed by asserting type **and** content plus cross-checking each resolved citation against the evidence table. **S7 is an equivalent mutant, recorded as worthless:** `jsonSafe(settings)` is a no-op since the settings hold no bigint; kept as defensive insurance and **no test manufactured to make a no-op look guarded**. **Suite 2135 → 2147 (+12)**, measured; typecheck clean. **Still NOT done: NOTHING IS PERSISTED** — no proposal record, no audit row, no D1 write; **21.5 requirement 5's audit logging is NOT satisfied, NOT partially satisfied, NOT begun** (Stage 4 territory), and `AssessResult` merely already carries what such a row would need. No batch/general/watchlist door; Stage 3 (Derive) untouched and it doubles the per-candidate model cost; Stage 4, the proposal record and the audit table untouched; **no dashboard control**, consistent with `/candles`, `/gather` and the watchlist, all still curl-only; **answer quality unverified and unverifiable from this environment**; no identical-input determinism replay (five calls, five different prompts); and the synchronous-vs-queued question is **resolved for one candidate** and now belongs entirely to the multi-candidate step. |
| [41.md](41.md) | 41 | **Derive (Stage 3) — two conditional prompts, the real create-bot validators reused rather than re-implemented, a capital read that is deliberately not a check, and a live run that widened the latency band and invented a use of the citation mechanism nobody designed.** Built on step 37's terms: research, both prompts, the parser, three validation layers and the port all adversarially tested **before** any model was contacted; first contact was a temporary probe. **Suite 2147 → 2302 (+155) for the pure modules, → 2329 (+27) with the probe**, measured; typecheck clean; **+1366/−79 across 10 tracked files with all 79 deletions audited** (58+14 are rewrites of the shared-code extraction, no rule or check removed). **Research re-verified, not carried forward:** context window **24,000 tokens** unchanged, and the real measured prompts are **Assess 15,800 chars / 54 items** vs **Derive 23,383 (grid) / 23,812 (dca) chars / 75 items** — **~1.5× Assess, not 2×**, about a quarter of the window. **Cost roughly TRIPLES per candidate rather than doubling** (~490 neurons typical, up to ~1,270), so the **10,000-neuron daily free tier covers ~20 full two-stage candidates** and a 12-candidate run spends over half of it. **TWO prompts and two schemas, never one with optional fields** — an optional field is one the model MAY fill, so a DCA proposal could carry an `upperBound`, and `requireExactFields` would have nothing exact to require; a test pins the field lists literally (they ARE the spec quotation) and asserts they intersect in **exactly `["stopLossPct"]`**, a growing intersection being the signature of the design collapsing into a universal schema. **Sharing is by CODE, not convention:** `collectBundleEvidence`, `SHARED_GROUNDING_RULES` and `readModelAnswer`/`requireExactFields`/`resolveCitations` were extracted from Stage 2 with an injected `ParseRefusal`, so the grounding LOGIC has one implementation while each stage keeps its own error class — and **a test proves every Assess evidence id renders byte-identical in the Derive table**, without which the same id could mean two different numbers across stages. **The extraction changed no byte of the Assess prompt, proven by a new test pinning the whole eight-rule block literally** rather than trusting `toContain`. **Stage 2's choice is an input, never a question:** a disagreement is `strategy_disagreement`, a total refusal, because 21.2 says a divergence between stages is a bug and weighing it would put a strategy choice before a human that no stage is accountable for. **THREE validation layers in the real path's order — and the third is deliberately SMALLER than requirement 3's wording:** an inverted range and a zero percentage are **already refused by the REAL `validateGridParams`/`validateDcaParams`**, so a second copy would be the duplicated risk check requirement 3 itself forbids **and**, running last, a check that could never fire — step 37's duplicate-key "theatre" reintroduced. **So no copy exists, the cases are tested, and the tests assert WHICH LAYER refused** (`DeriveValidationError.layer`), so deleting a real validator fails the suite instead of being silently absorbed. **Recorded as a deviation from this step's literal instruction rather than absorbed.** **The min-notional check would have been a permanent no-op:** Gemini publishes **no** notional bounds (`parseSymbolDetails` sets `minNotional: DISABLED`), so a `minNotional`-only floor passes every proposal forever on the only venue this system runs against; it publishes `min_order_size` → `minQuantity` instead, so the check runs in **whichever dimension the venue publishes**, at the highest price an order would rest at (where quantity is smallest), using the strategy's own flooring arithmetic — and `MinimumOrderCheck` **reports which floor held**, `DuplicateKeyCheck`'s instinct one stage on. **The capital read is a PREFILL and the word is load-bearing:** `readAccountCapital` writes nothing and holds no lock; **`ledger.ts` exposes no read-only headroom function** (all three exports write, the arithmetic living inside the compare-and-swap closure), so the **subtraction is restated and the CHECK is not copied** — the binding refusal stays `createBotInstanceWithCapital`'s at creation, and the headroom is **stale by construction from the instant it is read**. Negative `available` preserved, not clamped (a losing bot legitimately over-allocates); zero rows is a reported state, not an error. **`gatherDeriveContext` is separate from Stage 1** because a symbol-details read is one uncached per-symbol request that `selectGeneralCandidates` would pay fifteen times (tripwire test proves Stage 1 never touches it). **Zero retries, four call-counting tests** — the reason is stronger than Assess's: nine numbers against validators makes a retry loop **a search whose objective is "parameters that squeeze through the checks"**. **Four preconditions refuse before any inference**, and **four fail-open mutants WOULD NOT COMPILE** — the `GatheredInput` union means you cannot fail open by deleting a guard, only by actively fabricating a value. **LIVE, OPERATOR-RUN, TWO FULL CHAINS on real market data** against real headroom **97,943.08787000** (balance 99,693.08787000 − allocated 1,750.00000000, **matching step 34's thirteen real bots exactly**): both runs produced a **fully-validated grid proposal**, every field cited, every arithmetic constraint satisfied, `envelope_object` + `unavailable_transport_parsed` **matching all five prior probes** (seven consistent observations), and **`minimumOrderCheck: "quantity"` because Gemini's real `minNotional` is confirmed `0.00000000` — exactly as predicted from source before any call**. **⚠ LATENCY SUPERSEDES ENTRIES 37/39/40:** run 1 **13,080 ms assess / 28,632 ms derive**, run 2 **41,511 / 46,969** — **~88 seconds of model time for ONE candidate**. Assess alone is now seven samples spanning **10,546–41,511 ms (~3.9× spread, was ~2×)**; entry 40's claim that a real end-to-end call had not widened the band **no longer holds**. Entry 40's core reasoning is unchanged and still correct (no hard wall-clock limit; network waiting excluded from CPU time), but the margin around its one caveat is not: it judged a 20 s request's collision with the **30-second runtime-update grace period** *"very unlikely"*, and **an 88 s request is not in that category**. Multi-candidate is now firmly a queued or streamed design, and a real Derive endpoint should be designed for a p100 near ~50 s rather than ~20 s. **⚠ A NEW, UNANTICIPATED USE OF THE CITATION MECHANISM — a finding, not a bug, left UNRESOLVED:** the model cited **Stage 2's own claims (`assessment.claim.N`) as justification for Stage 3 parameters**, and cited **`news.status` — an ABSENCE marker — as justification for setting `takeProfitAmount` to `null`. Both are **technically valid**: real ids, real values, and the set-membership check is exactly the guarantee step 37 argued for. But "no news was collected" offered as evidence **FOR** a parameter choice sits in an unexamined gap between rule 5's required reporting of a limitation and reasoning **from** a non-observation, and a parameter grounded in another model's claim rather than in fetched data is a real question about 21.5 requirement 1 that "the id was real" does not answer. **OPEN QUESTION flagged for its own decision** (a distinct namespace or flag for absence markers; forbidding them as a SOLE citation; or concluding it works as intended) — deliberately not settled here, because changing the citation rules changes the safety mechanism itself. **37 mutants, 35 caught, 1 survivor closed, 2 attempts recorded worthless.** **M6 survived** — widening the schema's `strategy` enum to accept both strategies broke no test; blast radius bounded and stated (the schema is a request Cloudflare does not guarantee; the parser's refusal is the enforcement), closed anyway with four schema tests because the schema travels on the audit record. **D3 justifies `src/workers/derive.test.ts` existing:** a transport that re-derives the per-strategy schema instead of forwarding it **compiles, reads identically in review, and passes every test that only checks which schema was sent** — caught by one test that deliberately builds an inconsistent request. **⚠ A TEST-SUITE FLAKINESS THIS STEP MAY HAVE INTRODUCED:** 2 of ~20 full runs reported failures whose identity was never captured; **HEAD was run six times in an isolated worktree and was clean 6/6**, so **this step cannot claim the failures are pre-existing** — weak evidence in both directions, recorded rather than averaged away. **⚠ DERIVE CANNOT DETECT A BAD UPSTREAM JUDGEMENT**, carried forward from entry 40 and compounded here: frozen sandbox data reads as a tight stable range, and Derive faithfully turns that into plausible, fully-cited bounds that pass every check — **both live runs are exactly that: mechanically correct and financially meaningless as market advice**. Every check asks *"is this internally consistent and grounded?"*; **none asks *"was the input worth reasoning about?"***. **Still NOT done: nothing is persisted** (21.5 requirement 5 not begun); **the probe is still in the tree** and removing it is the immediate next action; `envDeriveModel` has no production caller and **there is no real Derive endpoint**; **both live runs produced GRID, so the DCA half of the conditional design is verified only against fixtures**; Stage 4, the proposal record, the audit table and anything human-facing untouched; no identical-input determinism replay; no news or trending vendor, so `entryPoint=general` still 503s. |
| [42.md](42.md) | 42 | **The derive endpoint — a Stage 2 result resubmitted from an earlier, SEPARATE call, re-verified against evidence gathered now; the probe deleted; and the staleness case firing live, unprompted.** `GET /api/accounts/:label/derive?pair=&interval=&assessment=`, Access-gated, wired through `handlers.ts` like every other real endpoint (`deriveContextPortsFor` = `gatherPorts` + `symbolDetailsFor`, the REAL adapters — the probe's locally-rebuilt ports were correct FOR A PROBE, so deleting it left nothing behind). **Suite 2329 → 2377**: **−12** probe tests, **+40** unit (`assess-resubmit.test.ts`), **+20** endpoint; typecheck clean. **Requirement 1 checked from source, not assumed: `/assess` already carried everything needed** — `assessResultView` publishes all four `ParsedAssessment` fields, so **nothing changed in `/assess`** and the client's entire job is one projection, `citations: [.citations[].id]`. **THE CENTRAL QUESTION, and both halves are true:** `resolveCitations` was ALREADY a set-membership test over an **injected** map with no opinion about its provenance, so verifying against an unfamiliar evidence set needs **zero new logic** — **but no existing call site ever passed an INBOUND assessment through it.** `collectAssessment` merely *renders* the ids into prompt text and its docblock states the same-request provenance assumption out loud, true for the probe's chain and **false for a resubmission**. So: new **call site**, not new **logic**; saying only "resolveCitations already handles it" would be wrong, because it handles it *when someone calls it* and nobody did. **The map is the ASSESS evidence set (`collectBundleEvidence`), not Derive's** — Derive's contains `assessment.claim.N` built FROM the assessment being checked, so verifying against it is **circular and would pass by construction**, step 37's duplicate-key "theatre" again; a mutant widening the map is caught. **The drift is reachable through ORDINARY operation, not only fabrication:** `candles.bucket.NN` exists for `min(24, n)` buckets so a shallower window removes high-numbered ones, a failed candle fetch collapses NINETEEN ids into one `candles.status`, `concentration.flag.N` vanishes when a bot stops. **Citations are BARE IDS and the omission IS the contract** — a submitted `EvidenceItem`'s `label`/`value`/`source` are a rendering of data as it stood at the ORIGINAL call and must be ignored entirely, and *accepting a field and ignoring it is how a caller concludes their data was used*; refused as `citation_not_a_string` rather than helpfully extracted. **`envelope`/`duplicateKeyCheck` are REQUIRED and NOT verified** — they describe a call this system never witnessed, so they are union-checked, taken on the caller's word, and nested under `unverifiedOriginalCall` beside `source: "client_resubmitted"` and `citationsReverified: true`; **defaulting them would manufacture an audit fact, which is worse than an unverified one**. **The duplicate-key scan genuinely RUNS here for the first time** — the only path holding the caller's own bytes, unlike both model paths. **NO PERSISTENCE BRIDGES THE TWO CALLS** — no D1, no KV, no cache: trust is answered by **re-verification against reality**, not by storage, and 21.5 requirement 5's real proposal record belongs to Stage 4, so a throwaway bridge would be storage built twice; reasoning recorded in the endpoint's own docblock. **Three new status rows** (`capital_unreadable` 503, `symbol_filters_unreadable` 503, **`no_capital_headroom` 409 — the read SUCCEEDED**, a conflict with current state, never an outage), **and `citation_unknown` deliberately ABSENT from `STATUS_BY_CODE`**: it is carried by THREE classes needing 502/502/**409**, so a row would silently make one wrong — whichever handler someone later simplified into a bare `throw`. **LIVE, OPERATOR-RUN, TWO GENUINELY SEPARATE HTTP CALLS WITH A REAL 600-SECOND GAP; all nine checks passed; four model inferences (arc total now 13).** `/assess`: **grid, 17,622 ms, 5 cited claims** — one citing `candles.bucket.12`/`.13`. `/derive`: **`citationsReverified: true`, `source: "client_resubmitted"`**, real grid proposal **64036.14 / 62660.91** inside real headroom **$97,955.11544000**. **⚠ THE DECISIVE CHECK: the same evidence id `candles.last_close` resolved to two genuinely different real values ten minutes apart — 63775.31 at assess-time vs 63757.71 at derive-time** — direct, undeniable proof the second call performed an **independent fresh gather**, not a replay or a cache, and that the human reads the claim beside the price that exists NOW rather than the one it was written about. **No test in this repository can produce that observation**, because every test drives a stub. **⚠ THE STALENESS CASE FIRED FOR REAL, UNPROMPTED:** a shallower window aged out a **real original citation** (`candles.bucket.12`/`.13`, cited by the model of its own accord in check 1) and it was correctly refused `citation_unknown` — **the scenario this step's own module header argues is "reachable through ordinary operation", then observed in ordinary operation on live data without being staged.** That answers the strongest criticism of the whole check — that it is a spelling check dressed as a freshness check, tripped only by an invented id: **check 6 is that case; check 7 is a caller behaving perfectly correctly and refused because the world moved.** Fabricated citation → `citation_unknown`; bad strategy → `strategy_not_recognised`; **ledger and accounts unchanged, nothing written**. Refusals cost **zero Derive inferences** by construction (re-verification runs after the gather, before the model), and a mutant moving it after the model call is caught by 6 tests. **Deploy: `env.AI` confirmed a PERMANENT binding in the deployed Worker's bindings table on every deploy since step 38's fix** — first deploy-time proof, retiring the risk that it depended on a hand-edit; ⚠ **precision recorded: `wrangler.jsonc` declares `ai` under the `testnet` env ONLY** (line 65), production deliberately does not, so any "in production" phrasing should be corrected before it is quoted forward. **Latency, both stages: Assess 17,622 ms sits INSIDE entry 41's band and does not widen it** (eight samples, 10,546–41,511 ms); **Derive 27,473 ms**, independently re-confirmed from the saved body against the operator's report, is the **third real Derive sample and the first taken through the REAL endpoint rather than the probe** — **27,473 / 28,632 / 46,969 ms, range 27,473–46,969 (~1.7×)**, and the lowest of the three, though three points isolate nothing from ordinary vendor variance. **Entry 41's anticipated design consequence is now MEASURED:** this run spent **45,095 ms of model time in total** (inside entry 41's 41,712–88,480 ms span) but **no single HTTP request carried more than 27,473 ms**, because the two stages are two endpoints with a human decision between them — splitting them does not shrink the total, it **caps per-request exposure at one stage's latency**, so the worst per-request figure ever observed drops from **88,480 ms to 46,969 ms** against the 30-second runtime-update grace period entry 41 flagged. ⚠ **A CORRECTION TO ENTRY 41, made here rather than inherited:** its claim that Derive's samples were *"both above every Assess sample ever taken"* **was already false when written** — its own run 2 recorded Assess at **41,511 ms**, above run 1's 28,632 ms Derive — and with 27,473 ms added, **two of three Derive samples now sit below the slowest Assess sample**. What survives is the weaker true statement (Derive clusters higher than Assess's typical range, consistent with a ~1.5× prompt and a larger answer); **what does not survive is any claim of strict separation between the two stages' latencies**. **17 mutants, 17 caught, 1 genuine survivor closed, 2 attempts recorded worthless.** **M14 SURVIVED** — gathering twice and verifying against a different bundle than it derives from returns *identical data under deterministic stubs*, so **no value assertion could ever catch it**; closed by asserting the **property** instead (a test requiring the endpoint to fetch candles **exactly once**). ⚠ **The first-pass M14/M17 were `void x;` no-ops that mutated nothing** — the same class of harness error entries 41 and 10.14 recorded, and **only after rewriting them did M14's real survival appear**; ⚠ **M2 and M13 were caught by the TYPECHECK, not a test**, a real but weaker catch. **Probe deleted entirely** — file, route, import, twelve tests and every ⚠ TEMPORARY marker; the `deriveModel` injection seams stay, re-documented, under the standing rule that no test may fall through to a paid vendor. ⚠ **DERIVE STILL CANNOT DETECT A BAD UPSTREAM JUDGEMENT**, carried forward from 40 and 41 undiluted: **the re-verification makes the grounding FRESHER, not the judgement BETTER** — an assessment that was financially meaningless when made is still meaningless once every citation resolves against a newer window, and is now meaningless *and current*, which is the more convincing failure. **Still NOT done: nothing is persisted** (21.5 requirement 5 not begun, and the bridge deliberately not built either); not deployed by this step; **no live DCA derivation yet** across three live Derive runs, so that half of the conditional design remains fixture-only; Stage 4, the proposal record, the audit table and anything human-facing untouched; no dashboard control; no batch/general/watchlist door; no news or trending vendor; entry 41's two citation-semantics questions **OPEN by intent**; answer quality still unverifiable from a sandbox. |
| [43.md](43.md) | 43 | **The citation mechanism stays exactly as it is — a decision NOT to build, and the requirement moved onto Stage 4 instead.** **A decision-recording step: no source file changed, nothing built, measured or deployed.** Entry 41 flagged two live-observed citation uses nobody designed for and **deliberately refused to settle them in the same session that found them**; entry 42 carried them forward untouched. This is that decision, **made by the operator after discussion: `resolveCitations` stays as-is — no new parser logic, no new prompt instructions, no new validation.** **The two uses:** an **ABSENCE MARKER** (`news.status`, rendered `NOT COLLECTED …`) cited as grounds for `takeProfitAmount: null`; and a **PRIOR STAGE'S OWN CLAIM** (`assessment.claim.N`) cited as grounds for a Derive parameter rather than the datum underneath it — used as a **premise** where the design intended **context**. **What is NOT in question, stated so a decision to change nothing is not misread as tolerating something:** both ids were real ids those runs really emitted, both are **checkable by a human** (the item travels with its `id`/`label`/`value`/`source`), both produced **sound reasoning on inspection**, and **neither has produced an unsound or hard-to-verify result** — an observation about two runs, not a guarantee. **Three options.** **A — do nothing further at the parser level: CHOSEN.** **B — a parser-level marker (id namespace or `EvidenceItem` flag): REJECTED on a fact about the code, not taste** — **absence is a per-run STATE of an id, not a property of the id** (`candles.status`/`concentration.status`/`capital.status`/`filters.status` each emit `"PRESENT"` or `MISSING …` depending on the run), so a namespace would have to **rename ids per run, destroying the stable vocabulary step 42's resubmission re-verification depends on**; a flag re-runs a decision already made (*"There is deliberately no `derived: boolean` flag"*, `assess-prompt.ts`); and **`news.status` is the only id with no success arm ONLY because no news vendor has been chosen** (entry 30) — **designing a mechanism around a placeholder**. **C — split citations into two formal kinds: REJECTED because it changes what the parser IS** — today it answers one mechanical question (*is this id in the set this run emitted?*); C makes it judge **which kind a citation is and whether that kind is permitted here**, manufacturing a new *"right id, wrong kind"* refusal class **with no observed failure behind it**, and failing its own first case since a parameter may legitimately rest on a datum **and** the assessment at once. **The core reason for A is OVER-FITTING: two data points, from two live runs.** A rule written now fits *what has been seen* rather than *what needs solving*, and its likely failure is not strictness but a third shape arriving that it neither catches nor is obviously wrong for. Supporting: **the mechanism did not fail** (a change would alter a working safety mechanism in response to a non-defect); **its value is that it is mechanical** (every semantic rule erodes the determinism step 37 chose set-membership for); and **the distinction is genuinely useful, just not HERE** — nobody argued the three kinds are equivalent, only about which layer should know. **⚠ SCOPED TO THE PARSER/VALIDATION LAYER AND NOTHING ELSE.** Read as *"all citations are interchangeable"* this entry would be **actively harmful — the opposite of what was concluded**. **⚠ A REQUIREMENT PLACED ON STAGE 4 (Explain, not yet built), not a suggestion and not something it may rediscover independently: Stage 4's presentation layer MUST be designed knowing a citation may point to (a) real fetched data, (b) an absence marker, or (c) a prior stage's own claim, and MUST describe these differently rather than rendering all three as interchangeable "here is the source" citations.** (b) and (c) are **not weaker citations and must not be hidden or demoted — the requirement is to stop them reading as (a)**, because a reviewer who cannot tell that a parameter rests on a non-observation or on another model's judgement is shown a proposal **that looks better grounded than it is**, and 21.1 puts that reviewer there as the only part of the loop that can catch a bad upstream judgement. **The classification needs NO parser change — which is what makes the decision coherent rather than merely deferred:** `EvidenceItem.source` already carries it (`assessment.*` → (c); `news` and `*.error` → (b); everything else → (a)), so **option B would have added a field to say what `source` already says**. One observation recorded for whoever builds Stage 4: **the Derive prompt's own worked example cites `capital.status` for `allocatedCapital`** — a status id rather than the `capital.row.NN.available` figure — not a defect (placeholder values, deliberately real ids) but **the prompt itself demonstrates citing a status id**, relevant to why pattern (b) appeared. **Reopens on stated conditions rather than unease:** a **THIRD** kind of citation use appears; either kind produces an **unsound** result; or either kind proves **hard to verify in practice** — and condition 3 is why the Stage 4 requirement is a requirement, since **a presentation layer rendering all three identically would itself CREATE it**. **What this entry does NOT claim:** not that two data points prove the patterns safe — only that they do not justify building enforcement, a deliberately weaker and asymmetric claim (a wrong *do nothing* costs a revisit; a wrong parser rule costs the safety mechanism plus everything later built on it); **it does not settle whether citing `assessment.claim.N` satisfies 21.5 requirement 1** (entry 41 was right that *"the id was real"* does not answer it — this says a parser rule does not either, and routes it to the human via Stage 4); and it produced **no live run, no measurement and no new evidence**. |
| [44.md](44.md) | 44 | **Stage 4 (Explain and assemble) — the proposal rendered for a human, entry 43's citation requirement discharged in the layer it was placed on, and the (c)-only pattern seen live in the rendering itself.** The first thing in this arc that is **not a backend endpoint**: no third endpoint, no model call, **no backend code at all** — `src/` untouched, verified by diff. **13 new files, 3 modified, +3,244/−1, all under `dashboard/`**: `citations.ts` (the three-way classification), `proposal.ts` (`freshnessOf`, `dataLimits`), `api/research-types.ts`, eight `Proposal*` components and the `/proposal` page. **Entry 43's requirement is DISCHARGED HERE, and the entry predicted exactly how it would fail** (*"will otherwise meet a uniform list of `EvidenceItem`s and have every reason to render them uniformly"*) — so **no citation is rendered as a bare "source"**: every one carries a class badge with **words as well as colour**, amber/violet not red because **(b) and (c) are not errors**. **Rule traced from source and entry 43's table confirmed accurate — with one correction to how it had been paraphrased: the news test is EXACT EQUALITY, not a prefix.** `"news"` is the literal source today; the moment a vendor is chosen that slot emits `news.value.*` for **genuine fetched headlines**, and a prefix test would label real data an absence marker. `.error` is a **suffix** so a fifth slot needs no edit. **⚠ Two real branches read as absence but classify as (a)** — `collectCandles`' empty-window (`candles.value.candles`, documented unreachable, **arguably a genuine gap**) and `collectCapital`' zero-rows (`capital.value.assets`, **probably correctly (a)**: the read succeeded and found nothing). **Implemented as entry 43 specified rather than deviated from — deviating would be Stage 4 rediscovering the decision, which that entry forbids** — and both pinned by tests; the full `value` prints verbatim beside every badge, which is what makes it survivable. **THE LIVE VERIFICATION — real browser, real data, screenshots reviewed by the operator; the session that wrote the code never saw the page.** Concentration banner **first and prominent**, both real findings in full (**11 bots, 94.29% capital share**); freshness **4 real timestamps correctly ranked** (price fastest-moving, bot list slowest, matching the flag's own ageing rate); all three badges **visually distinct**; evidence **31/75 cited (Derive), 9/56 (Assess)** so both stages left most of what they were given untouched; drift table showing real differing values (**`candles.volume_total` 0.60404208 → 0.60421708**) — **a second, independent confirmation of entry 42's check 5, now visible to a human rather than to `jq`**. **No code changes resulted. The completing live-verification step for Stage 4, at the bar Stages 2 and 3 were held to.** **⚠ THE FINDING: the (c)-only pattern rendered live and unscripted.** On the first real proposal this layer rendered, a real model cited **`assessment.strategy` and nothing else** for **three** parameters at once (`takeProfitAmount`, `breakoutTakeProfit`, `breakoutThresholdPct`), each correctly carrying *"Rests on no fetched data — every citation is an earlier stage's own claim"*. **Not a fault** (real ids; the prompt tells the model the strategy is decided and its reasons are there to build on) and **meets NO reopening condition**: not a THIRD kind (still (c)); not unsound *on this run* (weaker than not met); and **condition 3 was contradicted** — the reviewer spotted all three at a glance from the field-level warning, the first evidence that a layer which does not render the kinds identically does not create "hard to verify". **One frequency observation recorded and NOT acted on:** entry 43 reasoned from one (c) citation on one parameter; this is three in one response — **a change in frequency, not in kind**, and the right response is to keep looking, which is entry 43's own argument applied to its successor. **Requirement 4 (21.3) is a distinct panel, not a relayed claim** — a judgement made and recorded: a prompt instruction is a thing a model may or may not follow, and a limitation rendered as claim four of six is one a confident proposal absorbs. **4 real items found live**, settling the "decorative and empty on real data" objection: no depth requested, 1 open candle, no news, and **NO LIQUIDITY TEST EXISTS** — named as **a decision this system has never made**, pointing at `candles.volume_total` for the reader's own judgement rather than inventing a threshold. **The operator's review calls this the clearest real-data confirmation yet of "state the limitation, don't invent a verdict"**, applied identically to the staleness threshold and to `minimumOrderCheck: none_published`. **⚠ 21.5 req 4 is HALF satisfied**: timestamps yes, staleness flagging **no** — nothing in this system defines the delay, so none is invented and the page says so; the one freshness rule that IS defined stays in the backend (`citation_unknown`, entry 42 check 7). **⚠ 21.5 req 5 NOT satisfied, NOT partially, NOT begun** — no D1 row, no `audit_log`, no KV, no `localStorage`; **stated in four places including the rendered footer** (*"If you close this tab, this proposal is gone"*) so a working Stage 4 is not mistaken for req 5 landing. **What it deliberately does NOT do**, each refused for a stated reason: **no re-verification of citations** (duplicates step 42), **no re-validation of parameters** (*a second implementation of a risk check drifts, and the copy that drifts is the one nobody is watching*), **no model call**, and **no approve button or prefilled create-bot link** — the one-click bridge is the affordance that turns "the human approves" into a rubber stamp. **Transport is pasted responses, not a trigger**, argued as a **21.6** question: every research endpoint is curl-only, a `/derive` costs a real inference and 41,712–88,480 ms of model time has been measured across the three call-chains so far, and the resubmission projection is new client BEHAVIOUR not presentation — **recorded as a judgement call, not an obvious one**. **Suite 2,377 → 2,395** (82/83 → 83/84 files, 0 failures): `citations.test.ts` (+18) **drives the backend's REAL prompt builders over every outcome state**, because a rule about `EvidenceItem.source` tested against hand-typed strings **pins the dashboard's belief about the backend rather than the backend**. **Mutation: 4 mutants, 4 caught**, each grepped for after application before its result was trusted (the rule from 10.14, 41, 42). **⚠ NONE of the eight components is tested** — no jsdom, no testing-library; **prominence, ordering, colour and legibility are verified by the operator's eyes and nothing else**, and `proposal.ts` has **no unit tests** either. The pre-existing `[vitest-pool]` teardown error is present and **not new, not fixed**. **The toolchain seam, hit again:** the test's backend imports break the dashboard's own `tsc -b` (TS 5.7/7 split, `noUnusedLocals`, no Workers types) — **the identical failure `derive.ts` already records** — fixed with a test-file `exclude` in `dashboard/tsconfig.app.json`, **the one modified file outside `dashboard/src`, called out rather than folded in**; **⚠ consequence: the test file is EXECUTED but TYPECHECKED BY NO `tsc` PROJECT**, since root `tsconfig.json` excludes `dashboard`. **⚠ STAGE 4 STILL CANNOT DETECT A BAD UPSTREAM JUDGEMENT**, carried forward from 40/41/42 undiluted and most at risk here: **a page that presents a proposal beautifully has made the proposal no better** — classifying a citation says what a value rests on, never whether resting on it was wise. What it does change is narrower and is the point: until this step the human 21.1 depends on was reading `jq` output. **Limits:** **never rendered against a live DCA proposal** (every live derivation this project has produced has been grid — entry 41's two, entry 42's one, and the one behind this render — so the DCA field list is exercised by no real response); the trending branch of the limits panel is **written and untested against a real trending source** (no vendor, entries 30/31); 31/75 and 9/56 are **two observations, not a rate**; **nothing was deployed by this step**. |
| [45.md](45.md) | 45 | **The permanent proposal record — a dedicated table argued against `audit_log` from source, four staleness thresholds that are policy and say so, the outcome link that makes "approved" recordable at all, a mutation harness that lied, and a real blank-screen crash found live and fixed.** **21.5 requirement 5 is SATISFIED**, after every entry from 30 to 44 closed with "NOT begun" and entries 42/44 deliberately refused to build a throwaway store. **Why NOT `audit_log` rows**, traced through all seven of its real writers: (1) a proposal has a LIFECYCLE and an audit entry is an EVENT — **not one writer has ever UPDATEd a row**, and recording an outcome means updating; (2) `Repository` **refuses to filter a JSON column** by design, so *"every proposal nobody acted on"* — the requirement's own measurement — would be unaskable; (3) `findMany` has **no projection**, so the **measured 290,459-byte** worst-case payload would ride on every unrelated `audit_log` read (`listReconciliationRuns`); (4) an FK needs a **constrained referent** or `proposalId` is a claim. So: dedicated table for the RECORD, `audit_log` pointer for the EVENT, **one `Database.batch`** — migration 0008's watchlist shape — measured live at **273 bytes**. **Two rows per run** (`stage`), each independently complete, because the two calls gather independently (entry 42's check 5); the derive→assess id link is **deliberately absent** rather than taken on the caller's word. **`ignored` is NOT a stored value: `outcome IS NULL` IS it**, needing no deadline and no sweep. **Automatic on every real call** (an opt-in denominator biases the rate it exists to measure); **a failed write FAILS the request** and discards a paid inference, on requirement 6's terms. **`/gather` writes no row** — a stated deviation: it could never be acted on, so it would inflate the numerator forever. **THE OUTCOME LINK:** optional `proposalId` on `POST /api/bots`, all checks run **before** anything irreversible, and the one write that cannot move earlier **soft-fails into the response body** rather than misreporting a completed creation (`undoReservation`'s precedent). `POST /api/proposals/:id/reject` added, curl-only, because a CHECK value with no writer would be the check-that-cannot-fire steps 37 and 41 each refused. **Four CHECKs enforced at the DATABASE**, tested by writing past the repository from `/src/db` (where `no-raw-d1` allows it). **8.7 holds structurally**: no delete method, no soft-delete column, no summarization. **REQUIREMENT 4's THRESHOLDS** (grid price **15 min** — absolute bounds; dca price **60 min** — every behavioural param is relative; capital **1 h** — a prefill whose binding check is the ledger's; bot list **24 h**; venue rules **7 days**) are **POLICY WITH NO BACKTEST**, same category as `DEFAULT_CONCENTRATION_POLICY`; only the **ordering and ratio** are argued. **Four thresholds, not one, because `oldest` gets two ordinary cases backwards.** Three states — `unknown` is **not** fresh. **`staleness.ts` has ZERO imports** so the dashboard imports it directly (fourth file across that seam, after `alert-types.ts`, `money.ts`, `proposal-shape.ts`); a mirror would silently show "fresh" about a stale proposal. **⚠ THE MUTATION HARNESS WAS LYING:** `grep -oE "Tests  [0-9]+ failed"` broke on vitest's ANSI codes **on the failing path only**, so two genuinely-caught mutants read as SURVIVED; found by manually reproducing one, fixed by stripping ANSI and cross-checking the real exit code, and every mutant re-run. **Extends entries 10.14/41/42/44's rule: the harness's REPORTING must be verified too, not just that the mutation landed.** **⚠ A REAL CRASH, FOUND LIVE:** a hand-built file with `strategy: "dca"` over grid params reached `formatMoney(undefined)` and threw `TypeError: undefined is not an object` out of `roundDecimal`, **blanking the whole page with no error** — worse than any error, being indistinguishable from a failed load. **A real response cannot produce it** (per-strategy JSON schema, `requireExactFields`, a discriminated view) **and that is not a reason to skip the check**, because the page's input is PASTED. Fixed by `checkParamsShape` over the **real `GRID_DERIVE_FIELDS`/`DCA_DERIVE_FIELDS`**, imported not mirrored, rendering **the same red `role="alert"` banner as the Stage 2/3 disagreement** — same category: pasted input whose parts disagree. **`null` is present, `undefined` is missing.** Plus nested `ErrorBoundary` (parameters / evidence / whole proposal), whose pure half **must never throw** (13 throwable shapes, incl. the null-prototype object this project hit once). **TWO GAPS THE FIX ITSELF FOUND:** (a) "unexpected = belongs to neither strategy" let an object carrying **BOTH** field sets pass as grid while silently dropping a whole DCA description — closed against the **claimed** strategy's exact list (`requireExactFields`' rule); (b) a mutant **survived** because the guard's call site lived in a `.tsx` and **React does not resolve in the Workers pool**, so a test importing it collects ZERO tests — moved to a plain `.ts` returning check-and-fields **together**, making "refused but rendered" **structurally unrepresentable**. **LIVE (all the operator's, over a real multi-hour session):** full inputs stored not summarised (`write_lag_ms > 0`; prompt stored but never on the wire); **⚠ the window was 122–125 candles (~2 h), not the ~1,440 of steps 37–44**, confirmed genuine (`truncated: false`) — **a second confirmation of step 29's unstable sandbox depth**; both Stage 3 context reads on the row; `client_resubmitted` + `citationsReverified` — **entry 42's mechanism confirmed inside the logging path**; two independent rows; **all four staleness states**, including **the control file going stale by real elapsed time, unstaged**, re-flagging price and capital while the 24 h/7 d rows correctly did not; the crash re-tested after the fix and confirmed graceful against the earlier blank screenshot; **THE OUTCOME LINK END TO END** — real bot `prop-live-1`, `recorded: true`, DB showing `approved` + bot id + actor, **`pendingMs: 28013070` (~7.8 REAL HOURS)**, an honest unstaged measurement of exactly the gap requirement 4 exists to expose, plus a **400** on approving an assess row; **THE REJECTION PATH** — real note, `outcomeBotInstanceId` null, `pendingMs: 28303660`, and a second reject **409**: a decision, once made, is locked. **Tests 2,395 → 2,551** (+156, +6 files); **47 mutants, 47 caught, 5 genuine survivors closed** — four of which exposed a missing TEST (the fail-closed rule, the verified-actor rule and the soft failure had **none**) and one a structural problem. **⚠ M9/C8 caught by typecheck only.** **⚠ Four pre-existing guard tests failed and each was a convention to follow, not loosen** (`.batch([`/`.prepare(` outside `/src/db`, two `schema.test.ts` lists). **⚠ Entry 42's "writes NOTHING" test is now deliberately false and was restated MORE specifically, not loosened.** **STILL NOT DONE: DCA has never been exercised by a real model call** (the synthetic fixture pins the shape check only and is explicitly not DCA verification); **the refusal rate is not measurable from this table** (a refused run writes no row — a different table, deferred); **no component/JSX tests** (no jsdom; `react-dom/server` fails in the pool, probed); trending and news still paused — and **`TrendingCoin.raw` landing permanently in `inputs_json` makes every vendor's data-storage clause a hard precondition now**. **A permanently logged proposal is not a good one** — carried forward undiluted. |
| [46.md](46.md) | 46 | **The trigger — client-side orchestration of two real endpoints, the first browser-native Cloudflare Access session this project has ever exercised, a live Derive that took 77 seconds and broke the range the UI was quoting, and a feature that is deliberately not finished.** Entries 42 and 44 each **declined** to build this, on the stated grounds that it was a 21.6 TRIGGER decision and that a button which spends money and blocks for a minute is a design problem of its own. It is built now, **entirely in the client**: `/assess`, then entry 42's projection (`citations: [.citations[].id]`, four fields exactly), then `/derive` — **no new endpoint, no new backend code, no new safety mechanism, `src/` untouched**. Every check stays server-side and unweakened. **⚠ THE ARCHITECTURAL FIRST: every authenticated call this project has ever verified used a manually obtained Access token in `curl`; this is the first time a real interactive browser Access session authenticated a real API call** — and therefore the first permanent proposal record whose actor came from a real login. The mechanism, corrected from source before it gets quoted forward: **`credentials: "same-origin"`, not `include`**, and sufficient because `wrangler.jsonc` serves the dashboard as assets from the SAME Worker (`run_worker_first: ["/api/*","/health"]`) — one origin, one session, no login UI to build. **Named entry point only** (trending still unchosen, entries 30/31/45). **The paste page is kept unchanged**, and not as legacy: it renders with no backend and no spend, and it is the only route to the adversarial input that found entry 45's blank screen. **Two real progress states, no bar** — the projection gets no phase because a 0 ms step is a spinner wearing a label, and the second phase names the strategy Assess **really** answered. **Real refusal codes, with the stage as the headline**: the module is shaped around five codes meaning **two different things at two statuses** (4xx = bad request, 502 = bad model answer), and a Derive refusal **carries the paid assessment through** rather than discarding it. **⚠ LIVE (operator, real browser): grid on `BTCUSD`/`gemini-main`, all citations resolved, and the concentration figures MOVED — 11→12 bots, 94.29%→96.36% — which is the proof the data was live rather than replayed.** **⚠ Stage 3 took 77,344 ms — a NEW HIGH, 1.65× the previous worst — while the page was telling the operator "27–47s"**; the honest-range copy under-reported on its first contact with reality. **Not a performance problem (nothing here controls inference time) but an HONESTY one, and FIXED IN THIS SAME COMMIT** (PART 4a): the full real history re-derived from the log rather than assumed — Assess **9 samples, 10.5–41.5s**, Derive **4 samples, 27.5–77.3s**, whole chains **4, 41.7–91.8s** — and, mattering more than the numbers, **the framing changed from a bare range to an explicit not-a-bound report**: `expected…` renamed **`observed…`** (a function called `expected` invites the caller to render an expectation, which is what happened), every range now publishes its **sample count** as a required field of the return type (*"Derive takes 27–77s"* is a claim about the future; *"Derive has run 27–77s across 4 measured runs"* is not), and the **elapsed counter is promoted to the primary readout**. The page now says outright that *"the longest Derive on record (77s) happened on a run this page was calling a 47-second job at the time"* — this step's own mistake on screen, on entry 44's principle that **an unstated gap is the failure mode and a stated one is a limitation**. Suite **2,551 → 2,632** (+81). **23 mutants: 22 caught, 1 equivalent, 2 genuine survivors closed** — the `citation_unknown` 409 guard was unreachable yet load-bearing (it is the only branch whose advice is "this is normal, run it again"), and an assess-stage `strategy_not_recognised` would have named a resubmission that does not exist on that call. The harness was verified in **four** directions first; its own first version was wrong (`grep -F` on a multi-line pattern) and **failed by refusing to report**, unlike entry 45's, which reported the opposite of the truth. **Two real bugs found by reading, catchable by no test here**: a failed `/symbols` load locked the run button while the copy said otherwise, and an unexpected throw would have left the spinner running forever — unreadable in the same way as entry 45's blank screen, from the opposite direction. **⚠ EXPLICITLY NOT DONE**: no create-bot prefill (next step; four safety constraints already agreed — the real form, no outcome change on navigation, every field editable, a visible AI-sourced banner); **no proposal history view, which is a missing READ and not a new gap** — entry 45's table already holds everything; no summary-card/collapsed-evidence view (raised within minutes of first use, and in **tension** with entry 44's deliberate show-everything decision); and a **genuinely open** polling investigation (kill-switch ~2.6 s, bots/alerts ~5.25 s; HAR evidence **rules out** progressive server slowdown and client leak; baseline latency and frequency unresolved, **not** attributed to this feature nor ruled unrelated — with a source-read *hypothesis*, not a finding, that two independent kill-switch pollers are mounted at once). **A working trigger makes no proposal better** — carried forward undiluted, and it matters more now: obtaining one used to cost two `curl`s and a paste, and now costs one button. |
| [47.md](47.md) | 47 | **One kill-switch poll instead of two — the duplicate subscription entry 46 deferred, confirmed from source, fixed, and LIVE-VERIFIED by the operator; a reported "wrong account shown" bug whose premise did not exist; and a real safety-communication gap deliberately left for its own step.** Closes the polling investigation entry 46 named as still-open. **The coding session made no live call of any kind** — both HAR captures, the timestamp arithmetic and the rate confirmation are the operator's. The 609 s idle HAR measured `GET /api/kill-switch` at **232 requests (~2.625 s)** against alerts' **116 (~5.25 s)** — exactly double, from **two** independent 5 s `setInterval` timers on the same singleton row (`global_kill_switch`, migration 0005, `CHECK (id = 1)`); flat server wait times (1881 ms → 1363 ms) had already **ruled out** a progressive slowdown or a timer leak. Three call sites existed (`KillSwitchBanner` outside the router, `Dashboard`, `KillSwitchPage`) but **two, never three, are ever mounted at once** — recorded explicitly because it has been miscounted repeatedly; three would have predicted ~1.72 s and ~354 requests, and a 3× rather than 2× change after the fix. **`git blame` puts all four lines in `db52912c0fce9fd22b1dbe0521187dc6385b0400` (2026-07-24), 56 commits before the section 21 trigger (`cadeb84`, 2026-08-13) — a three-week-old bug in the kill-switch feature itself.** Fixed by one shared `KillSwitchStatusProvider`/`useKillSwitchStatus` context carrying the **whole `PollState`** (not just `data`) so the freshness indicator, first-load state and `refetch` consumers survive; **the banner did not move and there is no `AppShell` in this codebase.** **A staleness bug, not only a cost one:** `KillSwitchPage`'s header had claimed since 2026-07-24 that `refetch` updates "the card, the strip and the banner" — false, since `refetch` is per-instance, leaving the banner stale for up to 5 s after a real pull. **The reported account-scoping bug is disproven at five levels plus the router** — the switch is a global singleton with no `account_label` anywhere, **zero of nine routes carry an `accountLabel`** (the only `useParams` in the dashboard extracts a bot id), and `"main"` exists only in test fixtures; scoping the banner would have **understated the blast radius** of section 7.4's every-bot-every-account control. **Corrected premise recorded plainly so a future reader does not inherit the wrong framing.** The genuinely per-account sibling — **circuit breakers, which have no dashboard surface at all** — is named as the real gap and deferred. **No component-test infrastructure exists in `dashboard/`, stated plainly rather than faked**; verified by a source-scanning guard in the repository's existing `no-raw-d1.test.ts` pattern (+6 tests), **proven to fail** by temporarily reintroducing the duplicate (3 of 6 assertions failed, then restored). **2632 → 2638 tests**, same 1 pre-existing miniflare teardown error. **Live result: 266 requests over 1373.7 s = ~5.164 s, ratio 1.967 ≈ 2× the pre-fix 2.625 s** — the halving the two-poller diagnosis predicted, with +3.29 % drift, tighter than the same session's pre-fix alerts (+5.00 %) and bots (+7.80 %). **Found and deliberately NOT fixed:** `KillSwitchBanner.tsx:35` (`db52912`, same commit) returns `null` when `status === null`, so if the very first poll fails **while the switch is actually tripped**, the banner is silent — it conflates "armed" with "unknown". Neither introduced nor worsened here, and scoped out as **a visible safety-UX decision deserving its own design pass**. **No interval changed, `/api/bots`' ~38-round-trip N+1 untouched, ~1.4 s baseline latency still open.** |
| [48.md](48.md) | 48 | **Create a bot from a proposal — the prefill 21.1 explicitly allows, built to entry 46's four agreed constraints; a staleness verdict RECOMPUTED at render rather than carried; a reported fixture bug whose premise did not exist; and the navigation-does-not-approve guarantee proved three ways.** Closes the "no create-bot flow" gap entry 46 named as the next step. **The coding session made no live call of any kind** — both database queries, the browser session and the real bot creation are the operator's. **Prefill mechanism: URL search params, not router state** — the only cross-route data pattern this dashboard already has (`Alerts.tsx`), and the only *visible* one, which matters for a feature whose design principle is "no hidden bridge"; `readProposalPrefill` returns null unless a real `proposalId` AND a recognised `strategy` are both present, so **prefilled numbers can never appear without the banner that explains them.** **It is the same component**: `/bots/new` mounts the same `CreateBot.tsx`, with no `mode` prop, no variant, no second route and no branch on provenance — what arrives is different `useState` defaults, read once at mount, and every field stays ordinarily editable. **⚠ Entry 45's "a field that PREFILLED the form would be the one-click bridge 21.1 forbids" remains TRUE and is not contradicted** — that forbids `POST /api/bots` READING the row; this carries values the human is already looking at, browser-side, and the test submitting a body that DISAGREES with the proposal still passes. **The staleness verdict is a function of the clock at RENDER time**: the URL carries raw `{key, at, thresholdMs}` triples and **no verdict word exists anywhere to go stale**; the banner ticks `Date.now()` every second into the backend's own `stalenessOf`. Carrying a verdict would satisfy the brief literally and be strictly worse — entry 45 measured a real **7h 46m 53s** (`pendingMs: 28013070`) gap between proposal and decision, and a grid price threshold is **15 minutes**, so a frozen "fresh" would mislead at exactly the moment it matters; a stripped `freshness` parameter reads **`unknown`, never fresh.** **Outcome linking reuses step 45's `proposalId` unchanged** — one function, `withProposalId`, **absent not null** when there is no proposal, called only from the submit handler. **A branch this step made reachable for the first time:** `proposalLink.recorded: false` (entry 45's deliberate soft failure) is now handled — the form does not navigate away, names the bot, prints the server's error and removes the submit control. **⚠ The reported DCA fixture bug does not exist** — checked at four levels: this step's fixture always had `takeProfitPct: "2.00000000"`, no fixture anywhere sets it null, and both `DcaParams` and `decodeDcaParams` are already non-nullable; **no test was passing for the wrong reason and nothing was fixed**, recorded plainly so the framing is not inherited. **The investigation did find something real:** `checkParamsShape` accepts `takeProfitPct: null` for DCA (its null-is-present rule is right for grid's optional fields, more permissive than the DCA decoder) — **verified by running a probe, fails closed at three layers, now pinned by a test.** **Navigation-does-not-approve proved three ways:** a real end-to-end test against real D1 (all four outcome columns null, zero bots, `total_allocated` `0.00000000`, no `proposal.approved`/`bot.created` audit rows); an 11-assertion source guard (no file on the navigation path may import `api/client` or name any of the twelve write endpoints); and **the guard PROVEN TO FAIL** via four real violations firing 3/3/1/1 assertions. **2638 → 2692 tests** (+54, +2 files), both typechecks clean, Vite build 109 modules / 495.03 kB, same 1 pre-existing miniflare teardown error. **Mutation: 21 mutants, 20 caught, 1 equivalent (F10, reachability checked not asserted)**, harness verified in four directions before any result was trusted; **S4 — the verdict computed at GENERATION time, i.e. the frozen-snapshot bug — is caught by 5 tests**, which is the mechanical proof the recomputation is load-bearing. **Live, BOTH DIRECTIONS verified by the operator against the real database: abandonment — real proposal, form opened, navigated away, real D1 query confirmed `outcome: null` at 1m59s; submission — real proposal, form opened and actually submitted, real D1 query confirmed `outcome: "approved"` with `outcome_bot_instance_id: "bot-9wzfci"` matching the bot actually created, Access-verified `outcome_actor`, `created_at` 1786683829752, `outcome_at` 1786683868421, `pending_ms` 38669 (~38.7 s; `outcome_at − created_at` reproduces it exactly).** ⚠ **The ~7h46m figure this entry reasons from is ENTRY 45's, and it was an APPROVAL, not an abandonment** — no run in this step reproduced a long gap, so the long-gap staleness path is covered by tests driving an injected clock, not by a live run here. **Deferred, not forgotten:** the button sits LAST, after the full evidence dump — **a summary card at the top with the button relocated into it is its own step**, and it is in tension with entry 44's deliberate no-collapsing decision rather than merely additional to it; **DCA has still never been exercised by a real live model call** (the fixture is synthetic and labelled as such); **entry 47's kill-switch "unknown state" banner gap remains its own separate deferred step.** |
| [49.md](49.md) | 49 | **The summary card — a fast path over a page that is long on purpose, the create-bot button relocated into it, the raw evidence collapsed by default, a three-state concentration badge found in SOURCE rather than assumed, a reported badge-precedence bug whose premise did not exist, and a live price observation that is the best argument for 21.5's staleness discipline this project has produced.** Reconciles entry 44's deliberate 30+ page length with the cost entry 48 recorded after real use: **the card ADDS a fast path and REMOVES nothing** — every panel still renders, in order, below it. **The coding session made no live call of any kind and never saw the rendered page.** **Three concentration states, not two:** `flagged` / `clean` / **`unknown`** (the D1 read FAILED, which `/assess` and `/derive` deliberately let through), because folding `unknown` into `clean` would be *a false negative, in green, at the top of the page, in the only case where the badge had nothing behind it* — and it would contradict the panel 40 lines below. **No verdict is computed in the card:** it reads the backend's own `ConcentrationResult.assessment` and composes `stalenessOf`/`worstVerdict`; a source guard forbids `samePairBots`/`capitalShare`/`flags` from the logic module entirely. **`<details>` is a DESIGN choice, not a harness workaround** — "expands on click" becomes the browser's behaviour this project cannot regress, and "defaults to collapsed" becomes the ABSENCE of an attribute a source scan can check. **Two things deliberately never collapse:** the `rests on no fetched data` warning (entry 44's live check 4) and the offered/cited counts (the DIFFERENCE is entry 44's argument). **A reported badge-precedence bug was checked at four levels and did not exist** — both badges always rendered; M3/M4/M5 were concentration mutants, not precedence ones. **⚠ THE LIVE FINDING:** a proposal whose upper bound was **62,912.34** while the real price was **62,944.83** — *the market was already above the top of the proposed grid* — **and the staleness badge said `fresh`, correctly.** *"Fresh" is a claim about WHEN the data was fetched; it is not a claim that the data still describes the market* — **no threshold at any value can certify the second.** **Tests 2,697 → 2,749; 15 mutants, 14 caught, 1 equivalent (recorded as equivalent); 7 guard-failure demonstrations.** Standing open item created: `docs/open-items/component-test-harness.md`. |
| [50.md](50.md) | 50 | **The proposal history view — a missing READ finally taken, a reconstruction that turned out to be lossless for a mechanical reason, TWO briefed findings whose premises did not exist (one of them a whole pagination design), a projection added to the storage layer and argued for, and a 126-hour-old proposal correctly re-flagged STALE live.** Closes the item entries 46, 48 and 49 each carried forward — entry 46 framed it correctly as **a missing READ, not a missing feature**. **All live verification is the operator's; the coding session's work was the build, the unit tests and the mutation tests only** — it ran no `wrangler` command, made no HTTP call of any kind, and never saw the rendered page. **⚠ THE FIDELITY FINDING, AND THE BRIEF HAD IT BACKWARDS:** `deriveProposalReasoningView` is `{ ...deriveResultView(...), promptText, response }` — the response's own object **SPREAD** — so `envelope`, `duplicateKeyCheck`, `settings`, `latencyMs`, `promptChars` and `promptVersion` are all in `reasoning_json`. **Nothing is lost; the record is a strict SUPERSET of the response**, because *the handler stores the very object it puts in the response*. History therefore hands the **real** response to the **unchanged `ProposalView`** rather than rendering an approximation. **⚠ TWO BRIEFED FINDINGS DID NOT EXIST** (the third and fourth consecutive instance, after entries 48 and 49): there is no `not_recorded` mechanism and the two transport fields were never lost — `grep` returns zero hits; and **"the keyset pagination choice" was never built** — the list is LIMIT/OFFSET with a **total order** (`created_at DESC, id DESC`, because two rows can share a millisecond and an unstable sort under OFFSET shows a row twice while hiding another). **Keyset is recorded as this step's named OPEN ITEM with its argument**, since offset paging really does re-show rows when the live trigger writes during a browse; it is not built because the seek method cannot answer `total`, and 21.5's signal is a RATE. **A projection was added to /src/db** (`findManyProjected`, `Pick<Row,K>`, no write and no delete) because a 25-row page through `findMany` is up to ~7 MB of candle windows — *migration 0009's own third argument, applied to the table it created*. **Read-only, three ways:** both routes GET, a real-D1 test asserting all four outcome columns still NULL and the `audit_log` count unchanged, and `offerCreateBot={false}` pinned by a guard to exactly one file. **⚠ The guard caught this step's own page copy and the COPY was changed, not the guard.** **⚠ LIVE:** all **11** real proposals listed, with `prop-live-1`'s **~7h 46m**, `bot-9wzfci`'s **~38s** and a real rejection **matching entries 45 and 48 to the figure** — faithful, not merely self-consistent; `bot-5ub501`'s **~11m 25s** is a new first record. A **126-hour-old** proposal reconstructed in full (11 bots / **94.29%** — entry 44's figures, a snapshot, while the account now holds 13) and **correctly re-flagged `DATA: STALE`, recomputed live with no injected clock** — closing the long-gap case entry 48 explicitly stated it had NOT exercised. **Tests 2,749 → 2,878; 26 mutants, 24 caught, 2 genuine survivors closed, 1 ineffective mutant recorded as such.** A `1e3` parsing hole found by a test, whose sister test had been **passing for the wrong reason** (the cap refused it, not the parse). ⚠ One run of eight failed 4 tests and **the names were not captured**; six consecutive runs since are clean. |
| [51.md](51.md) | 51 | **The kill-switch banner's third state — the conflation entry 47 named and DEFERRED to its own step, now closed; an EVENT-DRIVEN trigger rather than a timed one; a visual escalation the operator called correctly against this session's first answer; a mutation run that found nothing because there was nothing to find; and two real improvements deferred into a planned dashboard redesign.** `KillSwitchBanner.tsx:35` rendered **nothing** for THREE different facts — armed, first-poll-in-flight, and *no poll has ever succeeded* — so **"we do not know" was pixel-identical to "we checked, and it is safe"** on the loudest safety element in the system. **The verdict moved OUT of the `.tsx`** into React-free `killSwitchBannerState.ts`, because a test importing a `.tsx` **collects ZERO TESTS and reports a green suite** — the `proposalFields.ts` lesson: *a decision that cannot be tested is a decision nobody is watching*. **Four states, three renderings:** `tripped` (unchanged red), `armed` and `pending` (nothing), `unknown` (**the new AMBER bar**). ⚠ **`pending` IS A STATE, NOT A SCREEN** — there is no visible "checking" UI and this step did not build one. **An unrecognised `state` string resolves to `unknown`, NOT `armed`**, because `requestJson` casts the response with **no runtime validation** — reading "not tripped" as "armed" is *how the original bug worked*. **⚠ THE BAR IS AMBER, NOT RED, AND THAT IS THE DELIBERATE CHOICE** — a later framing describing it as "the same alert-red hue as tripped" is **not what the commit contains and not what was verified**; the commit has **no `red-` utility in the unknown branch**, now mechanically guarded. A different HUE is what stops habituation to one bar from dulling the other: **hue says CATEGORY, weight says URGENCY** — red-700 `oklch(50.5% 0.213)` against amber-900 `oklch(41.4% 0.112)`, **9.1 lightness points and 1.9× the chroma** apart. **The operator overruled this session's v1** (`amber-950` band, hairline rules, OUTLINED button — *under-communicating what the copy actually says*) into v2's solid bar with a filled call-to-action; the bottom-only rule is **not aesthetic** — a top rule would abut testnet's `bg-amber-400` environment banner and read as it being 2px thicker. **Trigger is `data === null && !loading`** — event-driven, **no threshold, no poll counter**. ⚠ **Its one real hole, recorded not hidden:** a request that is accepted and never answered leaves `loading` true forever (each tick aborts the last, and the `AbortError` is swallowed), so **the bar never appears**. **Tests 2,878 → 2,929 (+51 = 45 exhaustive + 6 source-guard); 14 mutants, 13 KILLED, M13 SURVIVED and PROVEN EQUIVALENT by brute force.** ⚠ **NO BUG WAS FOUND OR FIXED BY MUTATION TESTING** — all 13 kills are injected faults against already-correct code; a "mutation-testing catch" proposed after the fact **did not occur**, and there was never a poll-count-based threshold to fix. **Guards proven to fail three ways.** **⚠ LIVE, THE OPERATOR'S:** request blocking with **real failed requests in the capture** rendered the real amber banner with the exact reviewed copy; a normal load showed **no bar at any point**, then armed — and that second result is the confirmation of an **ABSENCE** (the no-false-alarm property), **not** the sighting of a calm "checking" UI, since none exists. **DEFERRED, NOT FORGOTTEN, into a planned whole-dashboard redesign:** (1) a real "checking" rendering — *the logic already exists and is tested; only the appearance is missing*; (2) an **elapsed-time threshold (15 s = 3 intervals, deliberately not the 5 s poll interval, since entry 47 measured a legitimate 4,957 ms response)**, which is **the fix for the hanging-request hole** and therefore a correctness item, not a cosmetic one. **Step 47's last-good-through-a-later-failure behaviour is byte-identical and untouched** — `error` is an input the verdict deliberately IGNORES, pinned by tests so the tempting over-fix (mutant M5) fails loudly. |
| [52.md](52.md) | 52 | **DCA verified by a real live model call at last — a research error corrected (production symbol data used to reason about a sandbox-only system), a 243-pair sweep proving no directional pair exists at 1m, a deliberately TEMPORARY widening of `VERIFIED_INTERVALS` to include `1h` with its limits stated rather than glossed, and a revert with zero trace.** **No permanent source change — this entry and its index row are the only files left behind.** Closes the gap open since entry 44: **11 real proposals, all `grid`**, DCA never once exercised by a real model call. **The first candidate analysis was WRONG** — it read Gemini's **production** `/v1/symbols` (347) for a system that has always run against the **sandbox** (392); the operator hit `pair_not_tradable`. WLDUSD and SUIUSD are **not listed on the sandbox at all**; ATOMUSD is, and returns `NoDataFound`. **A sweep of all 243 USD-quoted sandbox symbols found 9 moving, 112 flat (SOLUSD is a constant line at `72.88`), 122 `NoDataFound`** — and the 9 are **three assets × three quote variants** with identical metrics. **All three are range-bound at 1m**, so the 11/11 grid result was the model reading the data correctly: `VERIFIED_INTERVALS` is `["1m"]` and Gemini's `/v2/candles` takes no time-range parameter, so `since` can only NARROW — every judgement this project ever made spanned **24 hours of 1-minute candles**. **Interval width, not pair choice, was the only lever.** Spacing verified against the real sandbox before editing: **`1hr` 60 min × 1462 gaps, `6hr` 360 min × 364, `1day` 1440 min × 109**, all matching `GEMINI_TIMEFRAMES`. **⚠ THE BAR WAS MET FOR ONE VENUE AND NOT TWO: Gemini sandbox yes; Gemini PRODUCTION no; BINANCE no** — the constant is GLOBAL while the docblock it quotes demands per-exchange verification. **Permanence explicitly NOT decided.** **⚠ The brief said `"1hr"` — that is Gemini's WIRE spelling; the literal is `"1h"`.** The pin fired as designed; **3 further tests that hardcode `"1h"` as "unverified" were deliberately left red rather than patched**, and went green on revert untouched. **⚠ The brief's pair would have wasted the run** — BTCUSD at 1h is a round trip (**−0.36%, path efficiency 0.01**); **XRPUSD** (−15.44%, 0.29, no synthetic cliff) was chosen instead, and this step's own earlier 6h figure of −23.19% is **corrected as substantially a data seam** (`85500 → 60675`). **REAL RESULT: `strategy: "dca"`** on `@cf/meta/llama-3.3-70b-instruct-fp8-fast`, 38,836 / 43,195 ms, evidence **54/8** and **73/7**. **All 9 `DCA_PROPOSAL_FIELDS` present, ZERO grid-field leakage, `sellOnStopLoss` correctly `false`, worst-case spend 28.2 vs 1000** — verified independently by the coding session from the operator's saved files, and by the operator on the rendered page (items 1–5; **no blank-screen crash**). Model-cited **−15.35898264% / 19.40102721%** corroborates the sweep's independent −15.44% / 19.40%. **Two citation observations, neither a bug:** `autoRestart` is the **first-ever (c)-only field on a DCA proposal** (no entry 43 reopening condition met), and `sellOnStopLoss` cites a **timestamp that does not substantively justify a boolean** — a citation-QUALITY note, the first real-data instance of the limit entry 44 named. **REVERT CLEAN: 2929/0 → 2926/3 → 2929/0**, identical before and after, same known `[vitest-pool]` teardown throughout; `git diff` empty, `git status` clean, redeployed `d00e16ed`. **The live `interval_not_verified` confirmation is the OPERATOR's** — the coding session is Access-gated (302) and said so rather than offering 2 passing tests as closure. **Checklist items 6–14 CLOSED in a follow-up session (see the addendum), on the SAME proposal — no new probe, no new interval widening, no new model call**: the paste-based `/proposal` page replayed the saved responses; the link carried `strategy=dca` + all 9 values + the real `proposalId`; **`sellOnStopLoss` reached the created bot as "Disabled" despite having no form field**; `decodeDcaParams` accepted every prefilled value with **no hand-editing**; real bot **`bot-f170nr`** created; real D1 confirms `outcome` **`approved`**, `outcome_bot_instance_id` **`bot-f170nr`**, `outcome_actor` correct. **Entry 48's DCA-fixture path has now met real data.** ⚠ **Item 13 (body-disagrees-with-proposal) rests on the standing `api.test.ts` end-to-end test, which is driven by a GRID proposal — exactly the same standing entry 48 gave it**, so DCA now matches grid's bar item for item. **⚠ STILL OPEN: `takeProfitPct: null`** — the real proposal carried a non-null value, so that edge case is **unchanged and still theoretical**; **one `dca` answer is not a rate**; and `1h` is refused again, so **the proposal cannot be regenerated without another probe cycle**. |
| [53.md](53.md) | 53 | **A circular test assertion closed, false comments corrected, and deploy verification changed by exactly ZERO — stated at that size on purpose.** `/health` returns the same `version` literal it has returned since the first commit, before this step and after. **§16 is NOT satisfied by this step**; only the *environment* half of §16.1 step 6 works, as it already did. **DECISION: mechanism F** — build identity comes from **Cloudflare's per-deploy version id**, which already existed, required no code, and is **the only signal in the enumeration not self-reported by the artifact under examination** (every field `/health` could return is a literal captured from the deploying machine and recited back by the bundle). Recording it per deploy is the **operator's process, not code**; no tooling was built. **Six things explicitly NOT adopted and recorded as OPEN, not oversights:** bumping `package.json` (§16), git tags (§16), injecting a git SHA at build time, a tree-cleanliness deploy gate, an `audit_log` deploy writer (§16, **no writer exists anywhere**), and CI (§15/§16.1 steps 3–5, **no `.github/` exists**). **From SOURCE:** `/health` returns `status` (hardcoded), `version` (**static `package.json` import, `0.1.0`, one commit in 90 — the scaffold commit — never bumped**) and `environment` (`env.ENVIRONMENT`, **the one field produced by the deployment rather than copied into it**); **zero git tags**. **Entry 36 was RIGHT.** **Entry 29 named a real gap but prescribed a check that only discriminates if §16's version-bump discipline is followed, and it never has been** — with a constant on both sides the comparison passes for every build. **Entries 32 and 33 each contain ONE unsupported sentence about build confirmation**; 33 attributed git rev `0edd5a9` to an endpoint that **has never returned a git rev** (it was real, and came from a terminal). **⚠ SUPERSEDED, NOT INVALIDATED:** entry 32's live verification **stands entirely on its own evidence** — 400 `instrument_not_spot`, 400 `pair_not_tradable`, 201, D1 showing only the one created bot, and the `botInstanceId`-reuse **201-not-409** proof that refusal precedes any side effect; correcting more than the one sentence in each **would be the mirror of the error being corrected**. **Two files:** `api.test.ts`'s circular import → an independent literal `EXPECTED_VERSION`; `api.ts` **comments only**, verified **mechanically by `git diff` filtering rather than by eye** (every `+`/`-` line is a `*` or `//` line) — false "so a deploy can be confirmed without a session" clause removed, a **CERTIFIES / DOES NOT CERTIFY** split added, and the §16.1-step-6 implication dropped from the unauthenticated comment **with the unauthenticated claim preserved verbatim**. **⚠ THE SESSION CORRECTED ITSELF:** the investigation had written that the old test "can never detect staleness" and **a control run disproved it** — the old test also caught a hardcoded wrong value, so **M1 is NOT discriminating**. The blind spot is **exactly one case, `package.json`'s version changing** (expected and actual read the same file and move together), and that is **the entire gain** — and since mechanism F freezes that value by decision, what was built is **a freeze alarm on a constant, not a safety improvement**. **Mutations, every number scope-labelled** after the prior report conflated a single-file count with a suite count: M1 `version`→`"9.9.9"` **caught (1 file)**; **control** M1 + old circular test **also caught (1 file) ⇒ not discriminating**; **M2 drop the field entirely — caught, FULL SUITE, `1 failed \| 2928 passed (2929)`, and only ONE test in 2,929 caught it: the one this step wrote.** **Suite measured, not quoted: 2929 passed (2929), 104 of 105 files, 1 error** — the known pre-existing miniflare/`[vitest-pool]` teardown failure unchanged since entry 34. **Revert verified 2929 → 2928/1 → 2929**, `tsc --noEmit` clean. **This step adds no tests**, which is why the total does not move. **⚠ NOT DONE / NOT VERIFIED:** the one detection this change adds is **ARGUED FROM CODE AND NEVER RUN** — the `package.json`-bump mutation was forbidden and not attempted; **`/health`'s auth status is UNKNOWN and stays unknown** — the operator's Access-gated assertion had no evidence behind it and was recorded as their error, **no live `curl` was ever run**, and nothing describing auth status was changed; the **`/api/health` alias test (`api.test.ts:363-367`) uses `toMatchObject` and asserts nothing about `version`** — it passed with the field deleted, found this session and deliberately not fixed; and **a five-entry-stale test count (2,632, entry 46's total, superseded by 47 and 51) reached a session report as if current** and was caught by the operator cross-checking the log, **not by anyone noticing when it was written** — 10.14's rule again (*"anything which can change after an entry is written must be read from the system, never quoted from this log"*), which entry 34 called a pattern at its third occurrence. **`planning/spec.md` is gitignored and untracked** — sha256 **`510b273d0ae9fcfd13394f6ccf6321e7bd7648a11ee7f582cc9101c1c268507f`**, verified this session and matching the operator's independent check. **LIVE FACTS ARE THE OPERATOR'S, none measured by these sessions:** testnet version id **`d00e16ed-a89f-4f68-b941-1e44be9ad1a3`** created `2026-08-18T08:16:22.638Z`, probed `2026-08-18T21:44:39Z`; **ten deployments, all with empty Tag and Message and Source `Unknown (deployment)`** — consistent with §16's tagging clause never having been exercised. **⚠ `d00e16ed…` is a CLOUDFLARE VERSION UUID, NOT a git SHA** — entry 52 records it in a format that reads exactly like the git SHAs elsewhere in this log, they resolve against different systems, and conflating them is the same error entry 36 recorded. **Environment event: an iCloud eviction under `~/Documents` made `.git/HEAD` and `vitest` dataless mid-session** (`git` reported "not a repository" with every file intact; 16 files under `.git`, 96 under `node_modules`, **0 under `src`**), recovered by the operator with `brctl download` — **this is why M2 was measured in a later session than M1 and the control.** |
| [54.md](54.md) | 54 | **The deferred elapsed-time threshold investigated and DELIBERATELY NOT BUILT — zero files changed, no commit; a root cause LARGER than the deferred item; a live reproduction that confirmed the defect but NOT its reported symptom; and a commit message in permanent git history claiming three things that do not exist.** **NOTHING WAS FIXED OR IMPROVED** — the only change is what is *known*. **THE ROOT CAUSE, bigger than entry 51's item (2):** each tick calls `controller?.abort()` (`usePolling.ts:61`) on the previous still-pending request, the abort is rethrown unwrapped at `client.ts:83` and swallowed at **`usePolling.ts:69`** — so **if the API responds consistently SLOWER THAN THE 5 s POLL INTERVAL the poll can NEVER settle.** No hang, no dropped connection, **only latency above 5 s against a healthy server.** Entry 51's table lists only *"connection accepted, no response ever"*, which reads as exotic; **the real entry condition is not** — entry 47 measured a legitimate response at **4,957 ms**, i.e. **under 43 ms of headroom** (that figure is *server wait*, so the true margin was smaller). **Warm pages are the dangerous case: `error` stays `null` and `lastUpdated` freezes — stale data shown with nothing marking it stale.** **LIVE REPRODUCTION, THE OPERATOR'S — the coding sessions ran nothing:** `window.fetch` patched in Firefox against testnet; **6000 ms delay → repeated `FAIL AbortError` on `/api/bots`, `/api/alerts`, `/api/kill-switch`, ZERO successes**; 20000 ms identical. **CONFIRMED, observed not inferred.** ⚠ **BUT THE REPORTED SYMPTOM WAS NOT REPRODUCED** — the page loaded fully *before* the patch, so last-good data kept displaying; the silent banner needs a COLD load. **That setup error was the operator's.** So: **defect confirmed, entry 51's described symptom NOT confirmed.** ⚠ **COMMIT `7809245` CONTAINS FALSE CLAIMS, VERIFIED AGAINST ITS OWN DIFF: "Real elapsed-time threshold" — NO `setTimeout`/`Date.now`/`elapsed` anywhere in +711 lines; "Calm 'checking' state" — `pending` returns `null`; "red…'unconfirmed' state" — THE BAR IS `bg-amber-900`, and the commit ships a test mechanically forbidding `red-` in that branch.** Plus a fabricated *"per a mutation-testing catch"* and an unfilled **`docs/decision-log/[TBD].md`**. **Entry 51 says that framing arose AFTER the commit — it was IN the commit message itself**, and this **false history is permanent and can only be SUPERSEDED, never edited.** **CORRECTIONS:** entry 51 cites `usePolling.ts:71`, the swallow is at **`:69`** (`:71` is the last-good `setState`; the file has not moved since `1b8ddd4`); and the other pollers show a **static `"Loading…"` panel, NOT a spinner** — the banner alone (`KillSwitchBanner.tsx:96`) renders **literally nothing**. **`usePolling` HAS NO TESTS ANYWHERE** — no `usePolling.test.*`, no test imports it, `single-kill-switch-poll.test.ts` only regex-scans its source text, no `@testing-library` installed; **the defect site is uncovered.** **One implementation, exactly FIVE call sites, all at the default 5000 ms**, all sharing the defect. **Weak assertions FOUND, NOT FIXED in `killSwitchBannerState.test.ts`:** an unreachable-guard test (`:341-346`, its own comment concedes it), a meta-count test against hardcoded numbers in the same file (`:186-192`), a **tautological** `CASES.length * ERRORS.length === 32` (`:191`, cannot fail if `:189`/`:190` pass), and an expectation **derived from the function under test** (`:306-317`) — contra entry 51's *"not derived from the function under test"*. **DECISION: NOT BUILDING** — testnet only; requires a latency condition never observed arising on its own (entry 47: **266/266 succeeded**); and the fix lands in `usePolling` — **five call sites, zero tests** — so **the repair carries more risk today than the defect does.** ⚠ That third reason **names its own expiry: cover `usePolling` and it evaporates.** **RE-SCOPED, NOT UI POLISH: *the dashboard displays stale data confidently when the network is slow*; root cause in `usePolling`; affects ALL FIVE pollers**, and fixing only the banner would leave four surfaces silently stale. **Four options recorded, NONE CHOSEN** (A threshold in the banner · B in the shared provider · C fix `usePolling` for all pollers · D stop aborting a still-pending first request) — a deliberate non-decision. **NOT VERIFIED:** cold-load silent banner not reproduced; **the `"Live · updated"` indicator's behaviour during starvation NOT checked** (source-level inference only); test 3 control **inconclusive**; `usePolling` still untested; item (a), the cosmetic checking state, **untouched and still deferred**. |
| [55.md](55.md) | 55 | **A grid CONFIG panel that said "Not set" about protection that was ACTIVE — built and verified; and an archived-bot capital trap FOUND, NOT FIXED, whose real fix turns out to depend on a question nobody has asked yet.** **Two tasks kept apart: Task A BUILT, Task B INVESTIGATION ONLY with NO CODE WRITTEN. NOT COMMITTED** — `HEAD` is still `e977e9b` (step 54) and Part 1's four files sit unstaged on disk. **PART 1.** Live bot `bot-ri6iml` (grid, `BTCUSD`, `gemini-main`) halted on an upside breakout while its CONFIGURATION panel read **BREAKOUT THRESHOLD: Not set** and every halt alert named a concrete trigger — *"breakout 6337800000000, above the highest grid line 6325200000000"*, raw `Money` at SCALE 8, i.e. **63378.00** and **63252.00**. **NOT A BUG AND THE VALUE WAS NEVER LOST:** `breakoutThresholdPct` is an optional percentage OVERRIDE, correctly null, and the dashboard reads the very field the halt-check reads (`snapshot.config` IS the `GridConfig` handed to `gridDecide`). **A GAP, not a binding failure** — null means `breakoutPrice`'s default branch, **one grid step above the highest line** (`top + topRungGap`), per spec 6.2 step 5 as resolved by **entry 09's decision 4**; `breakoutPrice` has **exactly one non-test call site** in the repository (`grid.ts:727`) and reaches no API response and no component. **The LABEL was the real defect:** "Not set" reads as *no breakout protection is in force* on a bot that had just halted on breakout protection, while the create form's own help says *"leave blank to use the default"* — two screens disagreeing, with the alert text the only voice naming a number. **Three display changes:** label regains the **`%`** the create form always carried (`Breakout threshold %`); null renders **`Default`** with the hint **`one grid step above the top line`**; and a new **`Grid step`** row shows the ladder's own top-rung gap. **`gridStepOf` MIRRORS `topRungGap` rather than importing it, and the import was TRIED FIRST and failed to compile:** `../src/strategies/grid.ts(66,26): error TS6196: 'Timestamp' is declared but never used` — the dashboard sets `noUnusedLocals` where the Worker does not, **the identical seam `derive.ts` already records for `dca.ts`'s dead `mulInt`**, and fixing it would make a display-only change a backend change. Pinned the house way, by a parity test running BOTH implementations. **DECLINED AND PINNED BY A TEST so the decline survives: no derived "Breakout at $X" price row** — `breakoutPrice` carries an `applyPercent`-with-`ceil` branch and a client-side copy is free to disagree by a rounding step on the one screen read to decide whether to intervene (`feesFor`'s *"one query path is how that stays true"*, **entry 44**'s *"the copy that drifts is the one nobody is watching"*); a test asserts neither `GridLadderView.tsx` nor `derive.ts` contains `breakoutPrice` or a `Breakout at` row, so adding one later **deletes a test** rather than being an incremental edit. Also NOT done: **no backend change**, **no alert-text scaling fix** (halt details interpolate `bigint` directly — same pattern in grid's and DCA's stop-loss), **no touch to DCA's parallel stop-loss-price gap**. **+27 tests across 2 new files.** `grid-dashboard-parity.test.ts` (15) runs both implementations over **11 deliberately awkward ladders**, geometric included **because its interior levels are FLOORED while the endpoints stay pinned**, so the undershoot lands in the top rung and a naive `(upper − lower) / (lines − 1)` mirror gets it wrong. `grid-ladder-config.test.ts` (12) is a **source scan** — ⚠ **a real gap it does not close**: no jsdom, no testing-library, a test importing a `.tsx` **collects ZERO tests rather than failing**, so `Default`, the `%` and the new row are asserted in SOURCE and **have never been seen on a screen**. Carries a **scope guard** (the take-profit row must STILL say "Not set" — a null AMOUNT means no target exists, a null THRESHOLD means a default computes one, and wording them alike is wrong about one of them) and a **guard on the guard** (a phrase living only in comment prose, asserted present in raw text and absent from code, so the wording assertions cannot pass on a reverted file). **Two things this session got wrong FIRST:** a test assertion that **contradicted a sibling test in the same file** (banning `"not set"` file-wide while another test required it on the take-profit row — two failures, both the test's fault, neither the component's), and an **invalid fixture** whose geometric ladder threw `degenerate_ladder` because the bounds really are too narrow at SCALE 8. **6 mutants, 6 caught** — label loses `%` (4 tests), `Default`→`Not set` (1), hint reverts (1), `Grid step` row deleted (3), **`gridStepOf` measures the BOTTOM rung (6 — the wrong mirror a reasonable person would write, disagreeing only on geometric ladders)**, zero-instead-of-null (1). **Suite measured both ways: baseline (changes stashed `-u`) 104 passed (105) / 2929 tests → after 106 passed (107) / 2956 tests, exactly +27 / +2, ZERO failures either run.** The `Errors 1` `vitest-pool` teardown is **pre-existing and unchanged**, the same one **entry 36** recorded. Both typechecks clean; all four files diffed byte-identical after the stash round-trip. **A judgment call recorded as a call:** the grid step renders **with** its unit (`126.00 USD`) under an instruction reading *"as a plain number"* — every sibling money row carries one — **reversible in one line**. **PART 2 — NO CODE WRITTEN. THREE CORRECTIONS to the briefed assumptions:** **`BotInstance.close()` ALREADY EXISTS, is tested, and releases capital** (`bot-instance.ts:2588-2615`) — **only a ROUTE is missing**, so "build a stopped route" is wiring, not design; the bots' last known state was **STALE** (entry 34's read had both `created`); and **`halt_manual` does NOT imply a human** — the kill-switch and reconciliation make the same call, and `audit_log.actor` is what distinguishes them. **THE FINDING: archiving FORECLOSES the only path that resolves the alerts.** Exactly **four writers** set `alerts.resolved = true` and **none is human-reachable** (`/api/alerts` is GET only); only `alerts/halt.ts` owns `halt_*`, and it runs **only on resume** — which `handlers.ts:202-207` refuses for an archived bot with `bot_archived` (409). So: resolve ⇒ resume ⇒ unarchive first ⇒ the bot is RUNNING again ⇒ halting it writes a **NEW** alert. **Two independently-correct pieces of code (entry 27's fix, entry 26's gate) compose into a trap invisible from either one's own source.** ⚠ **`close()` does not resolve halt alerts either**, so a `stopped` route built over it today would release the capital and leave both alerts open. **Archive itself is TWO ROWS** — one boolean plus `updated_at`, one audit entry; it touches no alerts, no capital, no status, no Durable Object, and `archived` is **orthogonal to `status` by explicit design**. **Entry 25 RE-CONFIRMED and now known to be SCHEMA-ENFORCED:** `allocated_capital` is never zeroed **and cannot be** — `CHECK (allocated_capital > 0)` in migration 0001 — so `status = 'stopped'` is the marker, not a preference that could be revisited. ⚠ **That exclusion rule has never run against real data.** **Five interpretations enumerated (A–E), NONE chosen**, the discriminating question named as whether **irreversibility** is acceptable on the control an operator reaches for most often. **PART 3 — LIVE, RUN BY THE OPERATOR, not by the session** (which ran no wrangler, no deploy, no HTTP, no D1, no browser), money read via `CAST(... AS TEXT)` for the D1 INTEGER-precision reason. **Both bots: `status=halted`, `archived=1`, `allocated_capital=5000000000` ($50.00) each, `halt_reason` `manual: verification bot, no longer needed`** — the brief was right and the log was stale (**entry 10.14's rule, fourth recorded time**). **Exactly one unresolved `halt_manual`/`critical` alert each, `source=bot-instance`** — the source scope that makes these **the closable case**, closable by a mechanism the archive gate prevents anyone from reaching. **`audit_log` for both: `capital.allocated` → `bot.started` → `bot.halted` (reason: manual) → `bot.archived`, actor `d.vidya381@gmail.com` throughout** — **a human, by hand, confirmed; not the kill switch.** **`capital_ledger` `total_allocated = 570806000000` ($5,708.06), which the operator's own dashboard screenshot ALREADY showed as ALLOCATED** — a live cross-check between two genuinely independent read paths (the ledger's compare-and-swap column vs `accountTotals.ts`, which **touches no database at all**) **agreeing to the cent**. **GROUP BY status: halted `475806000000` ($4,758.06) + running `95000000000` ($950.00) = `570806000000` EXACTLY** — no ledger drift, and (⚠ **a deduction from the returned sums, not a row count**) no `stopped` and no `created` rows on the account. ⚠ **THE $100 IS INVISIBLE:** not separated, not flagged, not distinguishable anywhere — blended inside the $4,758.06 halted bucket. **Not an error anywhere; just money no view has any reason to point at.** **PART 4 — THE OPEN QUESTION, NOT RESOLVED:** the operator's actual goal is **interpretation D** — *archive releases capital immediately, the bot is not destroyed, unarchive RE-ALLOCATES to the SAME bot if available and allows resume, and blocks with a clear message if not*. **Whether that is buildable is STRUCTURALLY UNDETERMINED:** **can capital be re-allocated to an existing, previously-closed bot at all?** `createBotInstanceWithCapital` owns the reservation pipeline and the DO README warns *"a second writer to that row breaks step 5's ordering guarantees"*; whether `resizeBotCapital` or anything may act on a `stopped` row is **not established**. **A NEW investigation session is scoped to answer exactly that**, plus whether **"clone this bot's config into a new bot"** is a viable simpler substitute if reanimation is unsound. **Nothing decided here.** **NOT DONE:** nothing committed; **the trap is FOUND, NOT FIXED** and every bot archived from `halted` until then adds another unresolvable `halt_*` row; DO-reanimation **UNKNOWN**; whether **open positions block immediate-release-on-archive UNCHECKED** (`close()` cancels orders but does not flatten inventory); A/B/C/D/E **UNDECIDED**; Part 1's rendering **never seen on a screen**; `bot-ri6iml`'s `126.00` step still **reconstructed, not read**, and its stored `breakoutThresholdPct` **never confirmed null by query**; `gridStepOf` **has never rendered against a live bot** |
| [56.md](56.md) | 56 | **Archiving now CLOSES: Stage 1 of the archive/capital-release redesign — built, mutation-tested, live-verified; plus a pre-existing liquidation failure found and NOT investigated.** **STAGE 1 ONLY — Stage 2 (clone-via-prefill for unarchive reuse) is NOT built, not started, and nothing here commits to it. NOT COMMITTED**; six files on disk. Fixes the trap entry 55 found and declined to fix: archiving never released capital, and **there was no route to `close()` at all** — it had existed since **step 6**, `releaseBotCapital` since **step 5**, both correct, both reachable only from tests, which left `status = 'stopped'` **unreachable in production and an allocation, once made, unreturnable forever**. **The operator's decision: no auto-selling, ever.** A bot holding a position cannot be archived; the operator clicks the existing **Liquidate** first; only once flat does close-and-archive proceed as **one click**. New `POST /api/bots/:id/close`; **`assertFlatBeforeRelease`** reads held quantity from **DO state, not the row** (§8.1), and checks **`position.quantity` for DCA and `ladder.heldQuantity` for grid SEPARATELY** — the single most load-bearing detail, since a DCA-shaped-only gate would have passed every other test while **silently letting every grid bot release capital over live inventory** (mutation 1 confirms only the grid-specific test catches it). `archiveBot` is **gate → close → flag, in that exact order**: every partial failure must leave the bot **MORE visible**, because flag-first would leave a **hidden bot silently still holding its allocation** — migration 0007's *"one outcome archiving must never produce"*. Dashboard shows a **disabled Archive button plus an amber panel naming the exact held amount and pointing at Liquidate**, deliberately breaking `LiquidateAction`'s *"don't render an invalid action"* rule because *halted, finished, and still refusing* looks like a broken button. **Deviation, flagged not buried:** the close route was made **stricter than specified**, sharing archive's gate — an ungated release route beside a gated one is a hole through the decision; **confirmed by the operator afterward**. **Orphan exception:** an orphaned bot is archived **without release** (gate skipped, `capitalReleased: false`) because `close()` throws `not_created` on one and step 26 guarantees orphans stay archivable — **its capital has NO recovery path; known gap, not fixed**. Entry 26's *"archiving removes nothing"* deep compare was **split, not weakened**: it now guards the **refused** archive with **zero fields permitted to differ**. Stale prose corrected in `client.ts`, `types.ts`, the dialog and an error message; **`migrations/0007` still says the allocation is "untouched" and was deliberately NOT edited** (convention against editing applied migrations) — **now misleading, flagged here**. **Round-trip consequence, intentional and pinned:** archiving sets `stopped`, `resume()` takes only `halted`, so **unarchive → resume no longer works for newly archived bots**; the operator's **`v-spot-1` and `v-perp-1` are UNAFFECTED** — halted with capital intact, never closed. **Suite 2956 → 2965 (+9), 0 failed, 106/107 files both runs**, the one error **pre-existing miniflare teardown noise**, exit 0, both typechecks clean. **3 mutants, 3 caught** — gate-accepts-position (4 tests), archive-skips-close (5 tests), and **release-without-status-flip, whose first result was a FALSE CATCH self-caught and fixed**: the test had **recomputed the invariant's arithmetic inline** instead of calling the real `reconcileAccount`, then **failed on a precondition before its own subject ran**. Fixed, it fails honestly from the real auditor — the **first empirical confirmation** of entry 55's invariant reasoning. **Operator's live verification:** `bot-f170nr` (flat, halted) archived — ALLOCATED rollup **5708.06 → 3708.06 USD, exactly −2000** (two bots' worth, per the operator), live D1 showing `status='stopped'`, `archived=1`, `allocated_capital=100000000000` (**1000.00 at scale 8 — kept as history, never zeroed; the release marker is `status`, not the column**, entry 55's inference now confirmed by real query); and `bot-b23y63` (halted, holding **0.00158519 BTC**) rendering the **disabled button and the panel naming the exact quantity** — the real safety property, confirmed live. **⚠ NEW, SEPARATE FINDING:** `bot-b23y63`'s **liquidation had previously failed — last sell order `CANCELLED`, position unliquidatable**. **Pre-existing, NOT introduced here** (nothing in this step touches the liquidate path). The bot is **stuck**: halted, holding, unable to liquidate, and therefore **correctly and permanently blocked from archiving by the new gate — the gate working right against a bot in a genuinely bad state**. **Not investigated, not fixed, no cause claimed**; open item, may take priority over Stage 2. Also deferred: the **"Originally allocated" stat-box relabel** for stopped bots — cosmetic, not a defect. |
| [57.md](57.md) | 57 | **The halt's cancel sweep no longer destroys a fill it has not recorded — fix 1 of a three-part chain, built and mutation-tested. NOT COMMITTED**; two files on disk. **It does NOT and CANNOT recover `bot-b23y63` or `bot-bl4e7c`**, the two live bots that exposed it: their orders are already terminal in DO storage, `ALLOWED_TRANSITIONS` gives every terminal state an empty successor list, so `applyFill` throws `fill_after_terminal` on them forever regardless of this change — this stops NEW instances only. The bug: `#recordCancellation` marked a cancelled order permanently terminal at whatever fill quantity the bot LOCALLY believed, **even while detecting in its very next lines that the exchange reported more** — the `cancel_fill_discrepancy` alert fired *after* the damage. Terminal is unrecoverable in three directions at once: the poll can no longer apply the real execution, `applyMissedFills` cannot reach an order that left `openOrderIds`, and reconciliation stops re-detecting it (`liveOrderFindings` iterates the same list), so the standing alert auto-resolves. Confirmed as the actual loss-causing step on two live bots, where reconciliation found GENUINE drift and the halt that followed locked the understated number in. **The codebase already had the correct pattern for the other door:** `#foldTerminalState` refuses exactly this for the poll's trigger and its docblock says why — closing on an understated count is "permanent and unrepairable… not repeated here" — and it WAS repeated here, because `#recordCancellation` had no equivalent gate. **Built:** that predicate copied **verbatim** (`order.filledQuantity < remote.filledQuantity`) rather than reworded, because a second independently-worded comparison of the same two quantities is the thing that drifts from the original while both still look correct alone; one-sided (local ahead still closes), refusing *before* any write so neither the DO record nor the D1 mirror moves, returning `false` so the caller keeps the id in `openOrderIds` — which keeps the 30s alarm armed, so the next poll applies the fill under the exchange's OWN trade id and `#foldTerminalState` then closes it properly. The refusal is a deferral to the path that can repair it, not a dead end, and test 3 drives that end to end. **`cancel_fill_discrepancy` reused, not invented** (the condition already had a name; only its message changed), and the halt is not swallowed — status, alert, audit row and feed unsubscribe all proceed, asserted explicitly. **A self-caught correction to the build brief, recorded rather than absorbed: there is NO tolerance in this comparison.** The brief assumed one existed and asked to reuse it; source says exact `<` on scale-8 `bigint`, so one satoshi refuses. Built to the real comparison — inventing a threshold would have created a band of "small enough to destroy silently" that nothing in the design supports. **3 tests, 2965 → 2968 (106/107 files both runs), zero failures either side, typecheck clean.** Mutation: gate reverted to exact pre-fix behaviour → **2 of 3 new tests failed**; the ordinary-case test **correctly did not**, since the mutant is identical on that path and a failure there would have meant it was testing something other than the gate. The persistent 1 error is entry 21's open question 4 verbatim (uncounted file + "close timed out" teardown), identical in both runs, still uninvestigated. **Found, not fixed:** `#closePass` has the same unconditional `openOrderIds: []` wipe on a DIFFERENT trigger — a genuinely smaller problem, because the record stays non-terminal with its fills intact, so information is only *unobserved*, not destroyed; flagged for fix 3. **NOT DONE:** fix 2 (`resume()` silently proceeding after a drift halt), **fix 3 (a real repair action for already-stuck orders — the next priority, and the only one that would unstick the two live bots)**, fix 4 (reconciliation's zero-tolerance timing mismatch with the 30s poll) and the Gemini `updatedAt = createdAt` gap: none built, none started, no timeline committed. The two preceding investigation sessions have **no decision-log entries yet**, so none are cited by number anywhere in the entry |
| [58.md](58.md) | 58 | **`resume()` refuses a bot whose own books are known to be wrong — fix 2 of the chain, built and mutation-tested. NOT COMMITTED**; four files on disk (plus entry **57**'s two). **Does NOT recover `bot-b23y63`/`bot-bl4e7c`**: their orders are already terminal, and fix 2 touches no terminal state — it gates a lifecycle transition and reads an alert table. Only fix 3 could act on them. The bug: `#resumePass` never inspected WHY a bot halted — status check, two risk latches, feed, write `running`, clear `haltReason`, resolve halt alerts, audit, and **not one order read**. Confirmed live: reconciliation halted two real bots for a meaningful `order_state_drift` finding, an operator resumed both, and resuming cleared the halt and the alert while **correcting nothing** — silently converting *known-wrong, halted, alerted* into *known-wrong, running, unalerted*. Section 9 never auto-corrects, so the halt WAS the mechanism and resume dismantled it in one click. **The brief's premise was structurally false, and that is the entry's centre:** it assumed halt reasons could distinguish a drift halt from an ordinary one. There are exactly **seven** `HaltReason` values (`stop_loss`, `take_profit`, `breakout_take_profit`, `take_profit_reached`, `order_rejected`, `unhandled_error`, `manual`) and **no drift reason exists** — `workers/reconciliation.ts` halts via `halt("manual", detail, "reconciliation")`, the global kill switch does the same, and so does a human. All three land as `manual`; only the free-text `detail` differs. **String-matching that prose was considered and REJECTED**: safety would rest on a sentence nothing tests, defeated by any reword, **failing open into trading on known-wrong numbers**. **The fix reframes the question** from "why did it halt" to "are this bot's books currently trustworthy" — which the codebase already answers via `ORDER_STATE_DRIFT_ALERT_TYPES`, a curated set tied to the real `FindingKind` union by a type assertion so a rename fails the typecheck instead of silently emptying it, already imported by `dashboard/src/driftAlerts.ts` to gate the "Apply missed fills" control. An unresolved row of one of those types **is** the signal, and `resolveHaltAlerts` has **always excluded** it — so it is a condition resume cannot dismiss by running, and the gate and the repair button now agree by construction. **Dependency check, done explicitly: NO new flag was invented** that only a future fix 3 could clear — that would have made every drift-halted bot **permanently unresumable until fix 3 ships**, a worse trap than the bug, and a partial fix 3 through the back door. **Built:** new `BotInstanceErrorCode` `position_unverified` → **409** beside `invalid_status`/`account_tripped`/`globally_tripped` (a conflict with current state, not a 403 — no actor may resume a bot whose books are in question), and **a distinct code rather than `invalid_status` because the status is not the problem**: the bot IS halted and resuming a halted bot is what the method is for — what is wrong is the state it would resume ONTO. The gate sits after both risk latches and before `#subscribeToFeed` — the third and narrowest latch (**global → account → this bot's own books**) — refusing before any side effect or status write. Queried across the whole set and **deliberately not scoped by `source`**: both the bot's own `#onOrderStateError` and reconciliation write these, and either standing means the same thing about the same books. Reading the condition instead of the reason is **strictly wider on purpose** — a bot halted on `stop_loss` with a drift row open is just as unsafe. **The message deliberately does not promise an unbuilt fix:** it names the types and count, says resume neither checks nor corrects the number, names what CAN close the row today (reconciliation ceasing to re-find it; `apply-missed-fills`, which is built and wired) and then states the limit in the message itself — that repair reaches only orders still in `openOrderIds`, and a locally-terminal order has **no automated correction today** and needs a human decision. **5 tests + 1 endpoint test**: refuses on reconciliation's alert (bot still halted in DO *and* D1, halt alert still open); refuses on the bot's OWN alert (not source-scoped); resumes once the row is resolved by its owner (a latch on a condition, not a brand); does not refuse on another bot's row; **resumes normally for all seven ordinary reasons** — driving grid-only reasons through a DCA bot to prove the gate never consults the reason; and 409/`position_unverified` through the real endpoint, which also proves the code survives the DO RPC boundary. **One EXISTING test modified and recorded rather than passed over:** it seeded an `order_state_drift` alert and then asserted a **successful** resume — it was asserting exactly the hole this closes. The row was swapped to `cancel_fill_discrepancy` (same "reconciliation's, ingested" category in the test's own words, deliberately outside the gating set) with a comment; the original property is now asserted **more strongly** in the new refusal test. **2965 → 2968 (entry 57) → 2974, +6 exactly matching tests added, 106/107 files and zero failures throughout, typecheck clean.** Mutation: gate forced never to fire → **4 of 6 failed**; the 2 that **correctly did not** are the negative-path pair (another bot's alert; ordinary reasons), which the mutant shares — a failure there would have meant those tests were not testing the gate. The persistent 1 error is **entry 21's open question 4** verbatim (uncounted file + "close timed out" teardown), identical in all three runs. **Found, not fixed:** the disarmed poll alarm — a resumed bot with empty `openOrderIds` stays unobserved — **confirmed real but it is `#pollArmed`'s existing behaviour, not something `resume()` causes and not changed by this gate**; and the stale `exitOrderId` permanently blocking `#placeTakeProfitSell` with no equivalent guard on `#placeBuy`, which is **fix 5's** territory. **NOT DONE:** fix 3 (**the priority — the only one that would unstick the two live bots**), fix 4 (reconciliation's zero-tolerance timing) and the Gemini `updatedAt` gap, the disarmed-alarm gap, fix 5, and the alert-lifecycle holes the gate's strength depends on: none built, none started, no timeline committed. The two investigation sessions still have **no decision-log entries**, so none is cited by number |
| [59.md](59.md) | 59 | **`repairPosition` — the repair tool that RECOVERED the two stuck live bots, designed in a separate session before a line of code, and verified end to end on the exchange by the operator. NOT COMMITTED**; six files on disk. Fix 3 of the chain (entries **57**, **58**), and **the one that actually worked on `bot-b23y63`/`bot-bl4e7c`** — but **it is a correction tool for already-damaged state, NOT a fix to the underlying bug**: fix 5 (the position-decrement fix in `dca.ts`) is **not built**, so a future bot can drift the same way and this tool would have to be run again. Fix 4 also not built. **Designed before built, deliberately**, because it is the only thing in this system that OVERWRITES a position rather than deriving one forward — everything else here reports and refuses to auto-correct. **The design session corrected the chain's own premise:** the two bots were believed to have LOST fill data; `orders.filled_quantity` and `SUM(trades.quantity)` agree exactly on both, so the records were whole and the real damage was fix 5's — fills recorded against the ORDER, never subtracted from the POSITION. That made the build a subtraction over records the bot already held, not a reconstruction. **One decision was escalated to the operator rather than taken silently: the cost model for a partially-exited position**, which the model has no defined meaning for. Proportional (scales `cost` with the remaining fraction, **preserving `averageEntryPrice`**) versus unchanged (**inflating it up to 3.7×** on bot-b23y63, moving both risk trigger prices with it). **The operator chose proportional** — fixing the quantity while corrupting the prices it is judged against would have been a worse bot. **Built:** `repairPosition(actor, {commit})`, **report-by-default**; reads config, state, the full order history, `bot.cycle_completed` audit rows, and **a fresh live `getOrderStatus` per live-cycle order** — not optional, because local records are exactly what this bug corrupts and a repair trusting only them would reproduce the drift while looking authoritative. **Ten gates, refuse-whole:** strategy (DCA only, grid excluded), status (halted), unsettled orders, unreadable exchange, local/remote disagreement, partition sanity, audit cross-check, non-negative result, cost-within-allocation, safe `exitOrderId` clearing. **Multi-cycle partition** boundaries at a fully-filled sell — sound because `filled` is reachable only through `applyFill` (`closeOrder` takes only cancelled/rejected/expired, and 57 extended `#foldTerminalState`'s refusal to assert it to the second door) — ordered by the monotonic `clientOrderId` sequence, and **corroborated against `audit_log`'s cycle-completion rows as a HARD GATE**, not derived once and trusted. Sold-leg PnL booked from **actual fill notionals** rather than `#completeCycle`'s limit-price approximation — a deliberate approved improvement. `additionalBuysUsed` **preserved, never recomputed** (it depends on a runtime observation at fill time, unrecoverable after the fact). **A race guard added during the build beyond the design** — multiple live calls means multiple suspension points, so state is re-checked immediately before writing and the write refuses if anything moved — **deliberately NOT numbered as an eleventh gate**, so "ten gates" stays accurate. **A separate real finding, detected and reported but NOT fixed:** completed cycles leak unmodelled base, because exit sells round quantity DOWN while `#completeCycle` zeroes the position anyway — deliberately not absorbed, to avoid making one number wrong in a new way while fixing another. **22 tests**: 4 live-bot shapes, 2 report-mode-never-writes, 3 safety (healthy `no_change`, idempotency, refuse-whole), **all 10 gates in isolation**, 1 dust finding, 2 endpoint (including reachability while entry 58's drift alert stands). Mutation: gates 5, 8, 10 each → **1 failed \| 155 passed**; the **idempotency hinge → 2 failed \| 154 passed**, the most valuable one tested, since forcing it open broke both the double-booking guard and the healthy-bot path. **2965 → 2968 (57) → 2974 (58) → 2996, +22 exact, 106/107 files and zero failures across the whole chain**; the persistent 1 error is entry 21's open question 4, re-read before citing. **LIVE VERIFICATION, the operator's — no live call was made from the authoring session.** (1) **No deploy had happened since the chain began** — all three fixes were working-tree only and testnet still ran pre-fix code; caught and deployed before a route-not-found could be misread as a routing bug in correct code. (2) Report mode on both bots returned `would_repair` with `agrees: true` on **every** order — **settling live and affirmatively the one fact unknowable from source: Gemini's sandbox still answers order-status for orders cancelled days earlier**, so gate 4's fail-closed path never had to fire. (3) The numbers matched **three independent derivations** — operator's manual arithmetic, the design projection, and live data: `bot-b23y63` 0.00158519 → **0.00043085**, `bot-bl4e7c` 0.00126687 → **0.00092962**, both exact. (4) `commit: true` on both → `repaired`, `committed: true`, `exitOrderId`/`exitKind` cleared. (5) **THE END-TO-END PROOF:** a real Liquidate on bot-b23y63 against the repaired quantity **FILLED — 0.00043085 BTC at 68169.88**, real trade, position genuinely zero — **and step 56's archive gate then correctly recognised the bot as flat and re-offered Archive**, proving fix 3 and that gate **compose**, not merely work alone. **`bot-bl4e7c`'s liquidation was PLACED AND PENDING** at the time of writing — not filled, not claimed. **NOT DONE:** fix 5 (**the permanent fix; without it this drift can recur and the tool must be re-run**), fix 4, the dust leak, grid repair (out of scope by gate 1), and bl4e7c's pending fill |
| [60.md](60.md) | 60 | **`applyExit` — the inverse `applyEntry` never had; the ROOT CAUSE of the whole chain, closed. NOT COMMITTED**; three files on disk. Fix 5, completing entries **57**/**58**/**59**: `DcaPosition` had `applyEntry` and **no inverse**, and `#applyFillToOrder`'s mutate was `if (!isExit) { applyEntry(...) }` with **no `else` at all** — so an exit fill touched only `openOrderIds`, only on full completion, and a PARTIAL exit (still resting, or later cancelled) left `position` **completely untouched**. That is how `bot-b23y63` came to believe it held 3.7× what it did, with both risk thresholds computed from the inflated figure. 57 stopped the damage becoming permanent, 58 stopped it being traded on, 59 repaired the two bots after the fact; **none made the number right AS IT HAPPENED.** Read fresh first: the branch was unchanged by 57–59, so nothing had gone stale. **Same proportional cost model as 59, with one deliberate refinement:** 59 measured the sold leg as a PRODUCT (`soldQty × averageEntryPrice`) — fine, it runs ONCE over a settled total; this runs on EVERY fill, and only the REMAINDER form (`cost − newCost`) is **self-balancing**, reaching exactly ZERO across any number of partials instead of accumulating rounding residue per fill. **Realized profit now books INCREMENTALLY**, because the alternative is incoherent, not merely untidy: `applyExit` removes the cost basis in the same step, so a figure deferred to completion would have **nothing left to measure against**. Recorded as a **real user-visible change** — `realizedGross` now MOVES DURING a cycle rather than jumping once at its end — and it finally gives the partial-then-cancelled leg's PnL a home. **THE DEVIATION FROM THE BRIEF, and it was not minor:** the brief assumed `#completeCycle` could stay mostly as-is. **FALSE** — with `cost` already at zero from the incremental decrements, the old `proceeds − position.cost` would have evaluated to the **ENTIRE exit's proceeds and booked it a SECOND time**, roughly doubling every completed cycle's reported profit. Both `#completeCycle` and `#completeLiquidation` became **report-only cleanup**: no longer adding to `realizedGross`, only deriving the audit figure from `entries[]` and the exit's own fills; `EMPTY_POSITION` still runs, now clearing `entries`/`additionalBuysUsed`/`lastEntryPrice` rather than performing any decrement. **A self-caught catastrophic double-count.** `entries[]`, `additionalBuysUsed` and `lastEntryPrice` confirmed buy-side by definition against **every** reader in the codebase, not assumed; `exitOrderId`/`exitKind` clearing **unchanged**; the dust leak **confirmed orthogonal** (it arises at sell SIZING; `applyExit` only ever sees quantities actually filled). **THE HEADLINE: two entry-59 tests FAILED on first run, and that is the proof.** Both built the damaged shape by running the ORDINARY path and asserted the damage as the repair tool's starting point — and **that shape can no longer be produced**: the position is already correct by the time the cancellation happens. Updated with comments to assert the COMPOSED behaviour, where they now prove more than before: the repair finds `before.quantity === after.quantity` (`0.00043085`, `0.00092962`), still reports `would_repair` only because the stale exit pointer remains (fix 5's explicitly unchanged behaviour), and **does NOT re-book `realizedGross`** — 59's `reducing` hinge holds across the two fixes. **The clean demonstration that fixes 1, 3 and 5 agree rather than silently conflicting.** 59's tool is unchanged and still correct for bots damaged under the OLD code, which is how the two live bots got there. **4 tests:** partial fill on a still-resting exit (`0.00158519 → 0.00108519 → 0.00078519`, average entry held at 60000); `realizedGross` across MULTIPLE partials at DIFFERENT actual fill prices (`0.6 → 0.96 → 1.36`, third fill at 62000 not the 61200 limit); the ORIGINAL bug shape resolving **inline** to bot-b23y63's exact repaired numbers (`0.00043085`, cost `25.851`, realized `1.385208`) **without the repair tool**; and a full exit still zeroing correctly (`gross_profit: "1.90222800"`). **Fix 1's gate asserted, not assumed compatible** — genuinely exercised and correctly does NOT fire when local and remote agree, with zero `cancel_fill_discrepancy` rows. Pre-existing assertions held unchanged: `realizedGross` `m("2")` / `gross_profit "2.00000000"` on the standard cycle, and `m("-20")` in `liquidate-position.test.ts`. **Mutation: `applyExit` reverted to a no-op → 10 failed \| 158 passed — TEN, not the four written for it**, the other six being a stop-loss-holding-position completion test, an autoRestart test, and **four `repairPosition` tests** — breadth that shows the missing inverse is **load-bearing well beyond its own suite**, a root-cause fix rather than a narrow patch. **2965 → 2968 (57) → 2974 (58) → 2996 (59) → 3000, +4 exact, 106/107 files, zero failures across the entire chain**; the persistent 1 error is entry 21's open question 4, re-read before citing. **NOT DONE:** fix 4 (reconciliation's zero-tolerance timing) — **now LOWER priority, because the damage pathway a false-alarm halt opened is closed by 57/58/60, leaving an operational nuisance rather than a route to unrecoverable state — but still a real, separate, unresolved finding**; the dust leak; and **grid, which this entire DCA-only chain never touched and has NOT been investigated for an equivalent risk** |
| [61.md](61.md) | 61 | **Reconciliation stops halting a bot for a drift its own poll is about to fix — the live-order timing tolerance, and the CLOSE of the five-part chain. NOT COMMITTED**; three files on disk (entries 57–60 ARE committed). **Process facts recorded, not hidden: this session was interrupted by a usage limit** and continued via a general continuation prompt, with state re-established by observation (`git status`, full suite, typecheck, re-reading open files) rather than from the pre-interruption description — which mattered, because the four prior fixes had been committed in the meantime. **The bug:** two observers watch the same order at different cadences — the bot's own poll every **30s**, reconciliation every **5 min**. The terminated-order branch already forgave the gap (60s `timingWindowMs`, "a late fill, not drift; the bot's own fill path will record it"); **the LIVE branch forgave nothing**, so any disagreement became `order_state_drift` at `meaningful` and halted the bot — for a fill the poll would apply by its real id seconds later. That asymmetry produced the false-alarm halts on `bot-b23y63`/`bot-bl4e7c`. **Q1: `driftAgainst` is SHARED by both branches, but the 60s window sits OUTSIDE it**, so they were separable — **parameterised by `kind`, not duplicated**, because two independently-worded comparisons of the same quantities is the exact drift entry **57** was built to eliminate; a test asserts the terminated branch still reports real drift on the first run. **Q2, a real architectural finding: reconciliation is structurally BLIND to the poll** — `BotSnapshot` carries config/state/orders, `PollSchedule` lives under its own DO storage key, so nothing here can ask when a bot last polled or whether its poll is healthy. That rules out the obvious tolerance outright. **Q3: the memory is the EXISTING `audit_log` run history** — every run already writes its full findings list (the fact `standing.ts` names as why alerts may be deduplicated at all), so persistence came from reading recent runs: **no new table, column or alert type**. Matching is **STRUCTURAL** — a real `Finding.clientOrderId` plus `client_order_id` in the audit payload — because matching on the `detail` sentence would put a safety decision on text nothing tests, **the exact trap entry 58 identified and refused one entry earlier**. **Q4:** first sighting becomes `order_drift_unconfirmed`, **floor AND ceiling both `minor`** — no alert row, no halt, still written to the findings list, with the ceiling **pinned** so magnitude can never promote what the kind exists to call insufficient evidence. **Q5, THE SAFETY PROPERTY: escalation on a second sighting is UNCONDITIONAL** — no severity check, no poll-health check, no retry budget — so a genuine persistent drift halts **at most one ~5-minute cycle later than before, never indefinitely**. The **600s window** (two turns of the cron) exists for a safety reason, not tuning: §5.6 makes an unreachable-venue run skip order reconciliation entirely, and without a window spanning more than one turn such a run would silently reset the count to "first sighting", letting a flapping venue hold real drift unescalated forever. Both bounds tested by name. **Gemini `updatedAt` gap: found, NOT fixed, correctly deferred** — Gemini supplies no last-update time and both substitutes make the EXISTING terminated tolerance worse in opposite directions (creation time → never applies; receipt time → always applies, **silencing real drift on every Gemini order**). **This fix does not inherit it: built on run-to-run memory, reading no venue timestamp, so venue-independent by construction.** **5 tests added:** no halt on first sighting (the exact false-alarm shape, asserting the structural `client_order_id` in the audit row); DOES halt on a surviving second sighting; forgives again once the poll catches up; no escalation past the window; terminated branch unchanged. **5 existing tests updated and reported, not silently patched** — all five asserted an IMMEDIATE halt on live-order drift, i.e. exactly the changed behaviour; the confirming pass was deliberately NOT folded into `passWithDrift()` because two other tests count audit rows per pass. **A fixture bug self-caught:** an early draft omitted the D1 `orders` row, so `mirrorFindings` fired and **four tests failed for a reason unrelated to what they test** — recorded because a test failing for the wrong reason is how one quietly starts asserting the wrong thing. **Mutation, including a process error of my own recorded rather than omitted:** tolerance reverted → **5 failed \| 56 passed**; window bound removed → **1 failed \| 60 passed** — and that second figure is a **CORRECTION**: its first run reported 5 failed \| 56 passed because the second mutant was applied **without restoring the first**, measuring a doubly-mutated file. The invalid number would have **overstated coverage fivefold, in the flattering direction**; the tell was two different mutations producing byte-identical failure lists. **Suite, including the interrupted state: 3000 at fix 5's commit → mid-interruption 2995 passed / 5 FAILED (source complete, tests not) → 3005 after**, +5 exact, zero failures in the final state, typecheck clean, 3 files / 354 insertions / 8 deletions; the persistent 1 error is entry 21's open question 4, re-read before citing. **CHAIN CLOSED, all five built** — origin per **56.md**: `bot-b23y63` found STUCK while testing step 56's archive-gate refusal path. Permanent damage on cancellation prevented (57), resuming into known-bad state refused (58), both bots repaired live and verified by a real completed trade — 0.00043085 BTC at 68169.88, with step 56's gate independently agreeing the bot was flat (59), the underlying miscalculation closed for all future bots (60), and false alarms delayed rather than acted on while genuine persistent drift still halts (this entry). **NOT DONE:** the Gemini `updatedAt` gap and the dust leak — the honest remaining edges, neither scheduled; entry 60's open item (incremental `realizedGross` **still never observed on a live bot**); and **grid, which this entire five-fix chain has NEVER investigated** — the largest genuinely unknown edge left |
| [62.md](62.md) | 62 | **AVAILABLE capital on the bot list — the subtraction the operator was doing by hand, made a real dashboard figure. NOT COMMITTED**; ten files on disk (eight modified, two new). Read-only display: **no capital-mutating code touched** — not `adjustAllocated`, not `createBotInstanceWithCapital`, not reconciliation. `available = total_balance - total_allocated`, and **this is not a new calculation** — it already exists verbatim as the binding gate in `createBotInstanceWithCapital`, as `AllocationResult.availableAfter`, and in `research/capital.ts`'s own words: *"`total_balance - total_allocated` is not a risk check, it is the definition of the word 'available'."* **Investigation first: there is no third capital bucket.** Only `src/capital/ledger.ts` writes `total_allocated`, all through one function; the reserve-before-write ordering rule puts every in-flight reservation **inside** the column already; proposals reserve nothing. **Two real caveats recorded, neither breaking the formula and neither mitigated:** unreconciled manual adjustments can briefly **overstate** headroom, deployed inventory can **understate** it — opposite directions, which is why the subtext says **"what a new bot can reserve"** and not "cash available" (shipped with **U+2212**, matching the Net tile, not an ASCII hyphen). Three pre-build decisions: extend `GET /api/accounts` reusing the tested `readAccountCapital` rather than mint an endpoint; **one card per (account, asset), never summed** (a blended total is unspendable — a bot draws from one ledger row); over-allocated shown **negative, amber, unclamped**, following the IDLE tile and migration 0001's deliberate absence of a `CHECK`. **ALLOCATED untouched.** Two flagged extensions beyond the brief: `readAccountCapital` **throws**, and unhandled would have **500'd the whole endpoint the create-bot dropdown depends on** — caught narrowly, giving **three distinct states** (real headroom / `{assets: []}` unseeded / `null` unreadable, the last two never collapsed); and a new **`CapitalOnlyStrip`** so a funded-but-botless asset has somewhere to show a figure, **reversible**. **Structural test limit, stated honestly:** no jsdom, no testing-library (entries 44–46, 48), so all 23 dashboard tests assert **logic, not pixels** — the **amber CSS class reaching the screen and the tile's grid position are proven by no automated test**, a gap every other tile in that row already carries. **Suite 3005 (106/107) → 3034 (107/108), +29 tests exactly matching what was added, +1 file, zero regressions**; the single persistent error is the same **pre-existing** miniflare teardown flake. Both typechecks clean, deploy-guard 9/9, dashboard build succeeds. Fixture reproduces the operator's live figure with `available` **computed, not hardcoded**: 9995669131000 − 260806000000 = **9734863131000 = $97,348.63131000**. **LIVE-CONFIRMED BY THE OPERATOR** on testnet — AVAILABLE rendered **97,348.63 USD** beside ALLOCATED **2,608.06 USD**, subtext as built; **this session made no deploy and no live call**. **Not verified:** the live check is **manual confirmation, not a substitute for the missing test infrastructure**, and covered the healthy case only — **the amber state was not observed live**; **`CapitalOnlyStrip` has never rendered live**, the fleet having bots on every funded asset; ALLOCATED and the ledger's `total_allocated` remain independently computed and can drift (`ledger_allocation_drift`), **deliberately not reconciled here**. |
| [63.md](63.md) | 63 | **Does grid have DCA's position bug? NO — checked from source, and the reason is not the one it looks like.** A closing INVESTIGATION, not a fix: entries **57**–**61** each ended by recording that grid had never been checked, and this answers it so that line stops being carried forward. **No file modified, no code written, no live system queried. NOT COMMITTED**; two doc files. **Verdict: grid was never vulnerable** to a partial-then-cancelled exit fill leaving the tracked position overstated — and this is a genuine "checked, clean" result, because the session was told to PROVE immunity from source rather than infer it, which earned its keep: **the obvious explanation is wrong.** Grid is NOT immune because it is per-line — `GridLadder` (`grid.ts:202`) is a **hybrid** carrying real aggregates (`heldQuantity` *"sum of filled buys not yet sold"*, `heldCost`, `realizedGross`) alongside `slots[]`, and `heldQuantity` is a direct structural analogue of `DcaPosition.quantity` that the stop-loss and breakout both read. **The real reason:** grid's equivalent of `applyEntry` is **`planFill` (`grid.ts:781`), ONE function handling both directions** — DCA had an adder with no subtractor. The property, exactly: **on a partial fill the position has already moved BEFORE the early return, on both sides**; `fullyFilled` gates only slot-clearing and replacement planning, **never the position arithmetic** — precisely what fix 5 had to ADD to DCA, where the branch was `if (!isExit) { applyEntry(...) }` with no `else` at all. **Grid's exit path is separate code and also correct:** `#applyGridFillToOrder` (`bot-instance.ts:4341`) dispatches to `#applyGridExitFill` (`:4596`), which decrements `heldQuantity`/`heldCost` on EVERY fill, prorating cost against the current held quantity and booking realized PnL incrementally — **the same proportional model fix 5 introduced for DCA, already present on grid's liquidation path, arrived at independently and earlier**. Confirming check from history: `git show 84a7b6e -- bot-instance.ts | grep -icE 'grid|ladder'` returns **0** — fix 5's own commit touched zero grid lines. **Entry 09 records this as DELIBERATE, not a lucky accident** (*"Realized profit lives in `ladder.realizedGross`, and `state.realizedGross` (the field DCA also uses) is kept equal to it, so the shared field is meaningful for both strategies and nothing is double-counted"*) — which matters, because a model that merely happens to avoid a bug can lose that property to a refactor by someone who never knew it was load-bearing. **Grid is ALSO covered by the generic fixes** — 57's `#recordCancellation` gate (`#gridExit` → `#halt` → `#cancelOpenOrders`), 58's strategy-agnostic resume refusal, 61's live-order tolerance via `openOrderIds` — stated as an independent second layer, **not** as the reason grid is clean, so even a hypothetical future gap could not become PERMANENT damage. **Two unrelated findings.** (a) **Recorded, NOT fixed, NOT scheduled:** a plain `halt()` (manual/kill-switch/reconciliation, as opposed to `#gridExit`, which clears slots deliberately) leaves ladder slots stale, and the three `openOrderIds: ladderOpenOrderIds(ladder)` rebuild sites can re-add a locally-terminal cancelled id that `#foldTerminalState` then never removes (it returns early on `isTerminal`), so the poll re-reads it every 30s forever — **costs rate-limit budget and log noise, NOT correctness and NOT money**; it never touches position accounting. (b) **Checked and RULED OUT:** whether fix 1's order-retention conflicts with grid deriving `openOrderIds` from slots — it does not, because `#placeLiquidationSell` **appends** rather than recomputing and a retained ladder order keeps its slot. Recorded because a negative result on a plausible hole is worth as much as a positive one. **VERIFICATION, honestly: SOURCE-ONLY.** Three D1 queries were specified and offered — the partial-then-cancelled grid order shape, the `uncovered_held_inventory`/`order_state_drift` alert corroboration, and plain-halted non-archived grid bots for finding (a) — and **as of writing NONE has been run and no result reported, so nothing here rests on live data.** Stated in advance so the result stays falsifiable: **a matching row in query 1 would NOT itself indicate a bug**, only a place to look, since `planFill` means the position should already be correct (D1 cannot see `heldQuantity` at all — it lives in DO storage). **NOT DONE:** no live confirmation; finding (a) unfixed and unscheduled; the Gemini `updatedAt` gap (61) and dust leak (59) still open; entry 60's incremental `realizedGross` still never observed on a live bot. **AND EXPLICITLY: this is NOT a general grid audit** — exactly one question was asked and answered, and grid's ladder construction, thresholds, slot-collision handling, replacement queueing and capital accounting were read only as far as that question required, with no claim made about any of them |
| [64.md](64.md) | 64 | **`close()` refuses rather than wiping an unresolved order — and the investigation that caught a fix which would NOT have worked. NOT COMMITTED**; four files on disk. Closes a gap **first found during fix 1's build (entry 57)** and recorded there as found-not-fixed: `#closePass` wiped `openOrderIds` unconditionally (`bot-instance.ts:3376`, **not** the 2606 the brief carried — the file has moved). **The bug:** `close()` could leave a bot permanently `stopped` with an order never confirmed resolved — either an **unrecorded fill** (entry 57's gate refused it) or an **unconfirmed cancellation** (`cancel_failed`, where the order **may still be live on the exchange**). And once stopped, **NOTHING ever looks at it again**: checked exhaustively, **every observer is gated on bot STATUS, not on the list** — `#pollArmed` excludes `stopped`, `checkOpenOrders` refuses it, `applyMissedFills` and `repairPosition` require `halted`, and reconciliation's `RECONCILED_STATUSES` is `created`/`running`/`halted`. The order is not forgotten, it is **permanently unobservable**, on a bot whose capital was just returned. **TWO WRONG ASSUMPTIONS IN THE BRIEF, corrected BEFORE any code — the substantive part of this entry.** (1) That the flat-position requirement (`assertFlatBeforeRelease`, step 56) made this harmless. **The reasoning is backwards and the gate is structurally blind:** an unobserved fill leaves the position flat **PRECISELY BECAUSE the fill was never applied**, so the check reads **the exact number a missing fill would have changed** and cannot detect a problem defined by that number staying wrong — "looks safe" and "is unobserved" give the **identical read**. A guard whose input is disabled by the condition it is asked about. Reachable plainly: `autoRestart` bot, cycle done, position flat, base buy resting, it part-fills between poll passes, operator clicks Close. (2) That the fix should **retain** the id, mirroring entry 57. **Retention would have been INERT** — no code path reads `openOrderIds` on a stopped bot at all. Entry 57's retention works because a *halted* bot stays observable and hands the order to the poll; after a close there is nobody to hand it to. **A fix built to spec would have compiled, looked correct, matched an established pattern, and changed ZERO behaviour — caught before being built, not after.** Also corrected factually: `assertFlatBeforeRelease` lives in the **API handler layer**, not inside `close()`, which has no flat precondition of its own. **BUILT:** `#closePass` **refuses** while the sweep leaves any id on `openOrderIds`, placed after the cancel attempt and **before `releaseBotCapital`** — the real point of no return, since refusing after it would leave a half-closed bot. New `orders_unresolved` → **409**, beside `position_unverified`/`invalid_status`/`account_tripped`. **DELIBERATE DEVIATION:** rather than re-running fix 1's local-vs-remote comparison, the gate reads the **residual list** — `#cancelOpenOrders` already removes every id it resolved, so a survivor got there for exactly one of two reasons and the list **IS that comparison's outcome**; re-querying would be a **second independently-worded comparison of the same quantities**, the drift entries **57** and **61** were both built to eliminate. And it **catches `cancel_failed`, which the specified method would have missed entirely** — a fill comparison says nothing about an order that may still be genuinely live — a real, wider safety property **gained** by deviating. **5 tests:** refuses on an unrecorded fill (bot still `running`, `total_allocated` untouched at 400, order still listed); refuses on an unconfirmed cancellation (with its alert asserted); closes normally once it resolves (a latch, not a dead end); **still wipes and closes when the sweep resolves everything** (common path, unregressed); and the endpoint returning **409 `orders_unresolved`** with an actionable message and **zero `capital.released` rows** — that test's comment recording that the flat gate passes precisely *because* the position is flat, asserting Part 2's finding end to end. **A self-caught test-authoring mistake recorded, not omitted:** an early draft nested a new `it()` **inside an existing test body**, breaking an unrelated test — and it **typechecked cleanly**; only running the suite exposed it. A structural error in test code can look healthy to the compiler while quietly disabling an assertion. **Mutation:** gate reverted to the unconditional wipe → **4 failed \| 413 passed (417)**; the fifth (the ordinary path) **correctly did not fail**, since the mutant is identical there and a failure would have meant it wasn't exercising the gate. **3034 (107/108) → 3039 (107/108), +5 exact, zero failures, typecheck clean, 4 files / 173 insertions / 1 deletion**; the persistent 1 error is entry 21's open question 4, re-read before citing. **NOT DONE:** **no live bot has exercised this refusal path** — test fixtures only, no operator verification claimed; the refusal's real-world frequency is unmeasured; nothing committed |
