{
  "name": "trading-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rule-based multi-strategy crypto trading bot on Cloudflare Workers",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev --env testnet",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "build:dashboard:testnet": "VITE_ENVIRONMENT=testnet npm --prefix dashboard run build",
    "build:dashboard:production": "VITE_ENVIRONMENT=production npm --prefix dashboard run build",
    "deploy:testnet": "npm run build:dashboard:testnet && wrangler deploy --env testnet",
    "deploy:production": "npm run build:dashboard:production && wrangler deploy --env production",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:deploy-guard": "node --test scripts/verify-dashboard-build.test.mjs",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  },
  "engines": {
    "node": ">=20"
  }
}
